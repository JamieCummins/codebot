[
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "1",
    "dimension": "Test Specification",
    "value": "##start = nrow(table_1)+1\n    end = nrow(table_1)+length(levels)\n    table_1[start:end,1] <- rep(cov_factor_names[i], length(levels)) # covariate name\n    table_1[start:end,2] <- c(levels)            # subgroup level\n    table_1[start:end,3] <- c(table(input_factor_vars[,i]))  # number\n    table_1[start:end,4] <- 100*round(c(table(input_factor_vars[,i]))/nrow(input_factor_vars),4)  # percentage\n    print(levels)\n    \n    ## small number suppression by variable\n    table_1$number[start:end] = redactor2(table_1$number[start:end])\n  }\n  \n  ## numerical variables: number and percentage of observations, mean and standard deviations\n  input_num_vars <- input[,cov_num_names]\n  if(length(cov_num_names) == 1){\n    index = nrow(table_1)+1\n    table_1[index,1] <- cov_num_names\n    table_1[index,3] <- length(which(!is.na(unlist(input_num_vars)))) # number of observations\n    table_1[index,5] <- round(mean(unlist(input_num_vars)),2) # mean\n    table_1[index,6] <- round(sd(unlist(input_num_vars)),2) # sd\n    table_1[index,7] <- round(IQR(unlist(input_num_vars)),2)  # IQR\n    ## small number suppression by variable\n    table_1$number[index] = redactor2(table_1$number[index])\n  }\n  if(length(cov_num_names)>1){\n    for(i in 1:length(cov_num_names)){\n      index = nrow(table_1)+1\n      table_1[index,1] <- cov_num_names[i]\n      table_1[index,3] <- length(which(!is.na(unlist(input_num_vars[,i])))) # number of observations\n      table_1[index,5] <- round(mean(unlist(input_num_vars[,i])),2) # mean\n      table_1[index,6] <- round(sd(unlist(input_num_vars[,i])),2) # sd\n      table_1[index,7] <- round(IQR(unlist(input_num_vars[,i])),2)  # IQR\n      table_1$number[index] = redactor2(table_1$number[index])\n    }\n  }\n\n  write.csv(table_1, file=paste0(\"output/review/descriptives/table_1_\",cohort, \".csv\"), row.names = F)\n  \n  rmarkdown::render(\"analysis/compilation/compiled_table1_results.Rmd\",\n                    output_file=paste0(\"table_1_\", cohort),output_dir=\"output/review/descriptives\")\n}\n\nif (cohort == \"all_cohorts\") {\n  table1_creation(\"all\")\n  table1_creation(\"vaccinated\")\n  table1_creation(\"infected\")\n} else{\n  table1_creation(cohort)\n}\n\n\n# Comment 1:\n# Multi-morbidity could be redefined as discussed in a previous meeting\n# Possibly to a Multi-morbidity (yes/no) with yes if 2 or more diseases\n\n## Response 1:\n## Multi-morbidity now updated to three categories: \n## 0 (no disease), 1(one disease), 2(two or more diseases)\n## This change is in stage0_data_cleaning\n\n# Comment 2:\n# Why is there a need to have both GP consultation and GP consultation truncated\n# Why the truncated version is truncated at 12?\n\n## Response 2:\n## Good question.\n## This was intended to describe whether the GP consultation is frequent or not\n\n# Note:\n# inter_quartile_range for GP consultation or *_truncated is 0 which is odd.\n# However when looking at the dummy data, both GP consultation and its truncated version\n# are made of many 0s. Could there be an issue in the study definition?\n\n## Response to note: this is due to the dummy data generation mechanism for this variable\n## not necessarily the case for real data",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_1.R",
      "code_lines": "61-135"
    },
    "llm_raw": "{\"keep_indices\":[0],\"note\":\"Index 0 contains the executable R code that generates the descriptive baseline summaries for Table 1 (counts, percentages, means, SD, IQR) for the cohorts. It shows that only descriptive statistics are computed and no inferential statistical test is applied, which is the relevant test specification (descriptive only, no formal test) for this analysis. Other snippets focus on hazard ratio modeling or a separate post hoc chi-square analysis and are not directly tied to this baseline characteristics analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "1",
    "dimension": "Variable Specification",
    "value": "##start = nrow(table_1)+1\n    end = nrow(table_1)+length(levels)\n    table_1[start:end,1] <- rep(cov_factor_names[i], length(levels)) # covariate name\n    table_1[start:end,2] <- c(levels)            # subgroup level\n    table_1[start:end,3] <- c(table(input_factor_vars[,i]))  # number\n    table_1[start:end,4] <- 100*round(c(table(input_factor_vars[,i]))/nrow(input_factor_vars),4)  # percentage\n    print(levels)\n    \n    ## small number suppression by variable\n    table_1$number[start:end] = redactor2(table_1$number[start:end])\n  }\n  \n  ## numerical variables: number and percentage of observations, mean and standard deviations\n  input_num_vars <- input[,cov_num_names]\n  if(length(cov_num_names) == 1){\n    index = nrow(table_1)+1\n    table_1[index,1] <- cov_num_names\n    table_1[index,3] <- length(which(!is.na(unlist(input_num_vars)))) # number of observations\n    table_1[index,5] <- round(mean(unlist(input_num_vars)),2) # mean\n    table_1[index,6] <- round(sd(unlist(input_num_vars)),2) # sd\n    table_1[index,7] <- round(IQR(unlist(input_num_vars)),2)  # IQR\n    ## small number suppression by variable\n    table_1$number[index] = redactor2(table_1$number[index])\n  }\n  if(length(cov_num_names)>1){\n    for(i in 1:length(cov_num_names)){\n      index = nrow(table_1)+1\n      table_1[index,1] <- cov_num_names[i]\n      table_1[index,3] <- length(which(!is.na(unlist(input_num_vars[,i])))) # number of observations\n      table_1[index,5] <- round(mean(unlist(input_num_vars[,i])),2) # mean\n      table_1[index,6] <- round(sd(unlist(input_num_vars[,i])),2) # sd\n      table_1[index,7] <- round(IQR(unlist(input_num_vars[,i])),2)  # IQR\n      table_1$number[index] = redactor2(table_1$number[index])\n    }\n  }\n\n  write.csv(table_1, file=paste0(\"output/review/descriptives/table_1_\",cohort, \".csv\"), row.names = F)\n  \n  rmarkdown::render(\"analysis/compilation/compiled_table1_results.Rmd\",\n                    output_file=paste0(\"table_1_\", cohort),output_dir=\"output/review/descriptives\")\n}\n\nif (cohort == \"all_cohorts\") {\n  table1_creation(\"all\")\n  table1_creation(\"vaccinated\")\n  table1_creation(\"infected\")\n} else{\n  table1_creation(cohort)\n}\n\n\n# Comment 1:\n# Multi-morbidity could be redefined as discussed in a previous meeting\n# Possibly to a Multi-morbidity (yes/no) with yes if 2 or more diseases\n\n## Response 1:\n## Multi-morbidity now updated to three categories: \n## 0 (no disease), 1(one disease), 2(two or more diseases)\n## This change is in stage0_data_cleaning\n\n# Comment 2:\n# Why is there a need to have both GP consultation and GP consultation truncated\n# Why the truncated version is truncated at 12?\n\n## Response 2:\n## Good question.\n## This was intended to describe whether the GP consultation is frequent or not\n\n# Note:\n# inter_quartile_range for GP consultation or *_truncated is 0 which is odd.\n# However when looking at the dummy data, both GP consultation and its truncated version\n# are made of many 0s. Could there be an issue in the study definition?\n\n## Response to note: this is due to the dummy data generation mechanism for this variable\n## not necessarily the case for real data\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 1. Baseline characteristics of patients. \n#          Categorical variables: number and percentage\n#          Continuous variables:  number and percentage of observations, \n#                                 mean and standard deviation\n# Output:  table1.csv, table1.html\n\nlibrary(readr); library(dplyr); library(lubridate)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  ##cohort <- \"all\"          # all eligible population\n  ##cohort <- \"vaccinated\"   # vaccinated population\n  cohort <- \"infected\"      # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ntable1_creation <- function(cohort){\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  cov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\n  sub_factor_names <- names(input)[grepl(\"sub_cat\", names(input))]\n  cov_factor_names <- c(cov_factor_names, sub_factor_names)   # to include sub_cat_covid_phenotype\n  cov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n  sub_num_names <- names(input)[grepl(\"sub_num\", names(input))]\n  ## to include sub_num_gp_consultation which stores the original values before truncation\n  cov_num_names <- c(cov_num_names, sub_num_names) \n  \n  ## summary table for practice id - for exploration only\n  print(\"summary statistics for practice id\")\n  dtable <- table(input$practice_id)\n  ##print(table(input$practice_id))\n  print(\"number of practice id\")\n  print(nrow(dtable))\n  print(\"number of missing practice id\")\n  print(length(which(is.na(input$practice_id))))\n  \n  ## Create an empty data frame ---------------------------------------------------\n  table_1 <- data.frame(variable = character(),\n                        subgroup_level = character(),\n                        number  = numeric(),  \n                        percent = numeric(), \n                        mean    = numeric(),\n                        sd      = numeric(),\n                        inter_quartile_range     = numeric(),\n                        stringsAsFactors = FALSE)\n  \n  ## factor variables: number and percentage---------------------------------------\n  input_factor_vars <- input[, cov_factor_names]\n  for(i in 1:length(cov_factor_names)){\n    levels = names(table(input_factor_vars[,i]))\n    start = nrow(table_1)+1\n    ##table_1[start,1] = cov_factor_names[i]\n    ##start = nrow(table_1)+1\n    end = nrow(table_1)+length(levels)\n    table_1[start:end,1] <- rep(cov_factor_names[i], length(levels)) # covariate name\n    table_1[start:end,2] <- c(levels)            # subgroup level\n    table_1[start:end,3] <- c(table(input_factor_vars[,i]))  # number\n    table_1[start:end,4] <- 100*round(c(table(input_factor_vars[,i]))/nrow(input_factor_vars),4)  # percentage\n    print(levels)\n    \n    ## small number suppression by variable\n    table_1$number[start:end] = redactor2(table_1$number[start:end])\n  }\n  \n  ## numerical variables: number and percentage of observations, mean and standard deviations\n  input_num_vars <- input[,cov_num_names]\n  if(length(cov_num_names) == 1){\n    index = nrow(table_1)+1\n    table_1[index,1] <- cov_num_names\n    table_1[index,3] <- length(which(!is.na(unlist(input_num_vars)))) # number of observations\n    table_1[index,5] <- round(mean(unlist(input_num_vars)),2) # mean\n    table_1[index,6] <- round(sd(unlist(input_num_vars)),2) # sd\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: define eligible population\n## Output:  input_stage1_all.rds; input_stage1_vaccinated.rds; input_stage1_infected.rds\n\nlibrary(readr); library(dplyr); library(htmlTable)\nsource(\"analysis/functions/redactor2.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  cohort <- \"infected\"     # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\n\nstage1_eligibility <- function(cohort){\n  input <- read_rds(paste0(\"output/input_stage0_\", cohort, \".rds\"))\n  ## Specify study period\n  cohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\n  ## YW: in some cohort the cohort start date is different from the index date\n  ## for the vaccinated population and infected population, their index dates were 2nd vaccination + 14 days, and first covid dates during the follow-up\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  input$cohort_end_date = cohort_end\n  ##RK - I would move any hard coded dates to to the top of the script so you don't have to be searching through\n  ##scripts to find/check what dates you've been using\n  ##YW: this has now been moved to the top of the script\n  ################################################################################\n  ## Part 1. Define eligible population                                          #\n  ################################################################################\n  \n  steps <- c(\"starting point\",\"dead before index date\", \"missing region\", \"missing sex\", \n             \"missing age\", \"age <18y\", \"age>105y\",  \n             \"covid before index date\", \"long covid before index date\")\n  \n  ## starting point\n  flow_chart_n <- nrow(input)\n  \n  ## Dead: removed if dead before index date\n  input <- input%>%filter(death_date > index_date | is.na(death_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n\n  ## Region: remove if missing\n  ##input <- input%>%filter(!is.na(cov_cat_region))\n  input <- input%>%filter(cov_cat_region!=\"Missing\") # keep if not missing\n  table(input$cov_cat_region)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ##RK - in stage 0 you set NA's to 'Missing' so do you want to be removing 'Missing' instead?\n  ##YW - this is now changed to remove the \"Missing\" category for region and sex\n  ## Sex: remove if missing\n  input <- input%>%filter(cov_cat_sex!=\"Missing\")\n  table(input$cov_cat_sex)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n\n!is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n    input_select <- input_select %>% mutate(lcovid_cens_vax_c = ifelse((out_first_long_covid_date <= fup_end_date_vax_c & \n                                                                          out_first_long_covid_date >= index_date &\n                                                                          !is.na(out_first_long_covid_date)), 1, 0))\n  }\n  #############################################################################################\n  ## Define time to post-viral fatigue defined between 29 January 2020 and 29 November 2020  ##\n  #############################################################################################\n  if(cohort == \"all\"){\n    input_select <- input_select %>% mutate(fup_end_date_fatigue=min(out_first_post_viral_fatigue_date_after_pandemic,\n                                                                     death_date, \n                                                                     cohort_end_date,\n                                                                     na.rm = TRUE))\n    ## time to post-viral fatigue diagnosis date\n    input_select$fatigue_surv<- as.numeric(input_select$fup_end_date_fatigue - input_select$index_date)+1\n    input_select <- input_select %>% mutate(fatigue_cens = ifelse((out_first_post_viral_fatigue_date_after_pandemic <= fup_end_date_fatigue &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= out_covid_date &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= index_date &\n                                                                     !is.na(out_first_post_viral_fatigue_date_after_pandemic) &\n                                                                     !is.na(out_covid_date)), 1, 0))\n  }\n  ################################################################################\n  ## Part 3. Create and output datasets                                         ##\n  ################################################################################\n  ## Create data set for analysis 1 and analysis 2\n  \n  if(cohort == \"all\"){\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\",\"lcovid_surv_vax_c\", \"lcovid_cens_vax_c\",\n                          \"fup_end_date_vax_c\", \"vax2_surv\", \"fatigue_surv\",\"fatigue_cens\")\n  }else{\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\", \"vax2_surv\")\n  }\n  \n  ## warnings if non-neg follow-up time\n  if (!all(input_select$lcovid_surv>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  if (!all(input_select$lcovid_cens>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  \n  ## warnings if follow-up days greater than maximum cohort days\n  if (!all(input_select$lcovid_surv <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  if (!all(input_select$lcovid_cens <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  \n  input_select <- input_select[,variables_to_keep]\n  \n  input <- input%>%dplyr::select(-index_date)\n  ## left join: keep all observations in input_select\n  input <- merge(x = input_select, y = input, by = \"patient_id\", all.x = TRUE)\n  \n  rm(input_select)\n  \n  if(cohort == \"infected\"){\n    input <- input %>% mutate(cov_cat_covid_phenotype = sub_cat_covid_phenotype)\n  }\n  \n  saveRDS(input, file = paste0(\"output/input_stage1_\", cohort, \".rds\"))\n  print(paste0(\"Stage 1 date set for \", cohort, \" created successfully!\"))\n  ################################################################################\n  ## Part 4. Flowchart output                                                   ##\n  ################################################################################",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_1.R\nanalysis/table_1.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "61-135\n1-80\n1-80\n1-80\n181-260"
    },
    "llm_raw": "{\"keep_indices\":[0,2,3,5,9],\"note\":\"Selected snippets that define and operationalize variables used as baseline characteristics and key analysis variables. Indices 2 and 0 (table_1.R) specify which variables are treated as categorical vs numerical (cov_cat_*, cov_num_*, sub_cat_*, sub_num_*) and generate descriptive summaries for them, directly matching the baseline characteristics description. Index 3 (stage0_data_cleaning.R) lays out the variable naming scheme (covariates, outcomes, exploratory variables) and defines key exposure/phenotype variables such as out_covid and sub_cat_covid_phenotype. Index 5 (stage1_define_eligible_population.R) shows how demographic and clinical variables (age, sex, region, etc.) are used to define the eligible adult cohorts, clarifying their roles as inclusion/exclusion criteria. Index 9 (stage1_define_eligible_population.R later section) specifies outcome and time-to-event variables (lcovid_surv, lcovid_cens, fatigue_surv, etc.) and the set of variables retained for analysis, directly addressing variable roles (outcome, time, censoring). Other snippets either concern plotting, file/list management, or commented-out code, and are less directly informative about variable specification for this analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "1",
    "dimension": "Parameter Specification",
    "value": "input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 1. Baseline characteristics of patients. \n#          Categorical variables: number and percentage\n#          Continuous variables:  number and percentage of observations, \n#                                 mean and standard deviation\n# Output:  table1.csv, table1.html\n\nlibrary(readr); library(dplyr); library(lubridate)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  ##cohort <- \"all\"          # all eligible population\n  ##cohort <- \"vaccinated\"   # vaccinated population\n  cohort <- \"infected\"      # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ntable1_creation <- function(cohort){\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  cov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\n  sub_factor_names <- names(input)[grepl(\"sub_cat\", names(input))]\n  cov_factor_names <- c(cov_factor_names, sub_factor_names)   # to include sub_cat_covid_phenotype\n  cov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n  sub_num_names <- names(input)[grepl(\"sub_num\", names(input))]\n  ## to include sub_num_gp_consultation which stores the original values before truncation\n  cov_num_names <- c(cov_num_names, sub_num_names) \n  \n  ## summary table for practice id - for exploration only\n  print(\"summary statistics for practice id\")\n  dtable <- table(input$practice_id)\n  ##print(table(input$practice_id))\n  print(\"number of practice id\")\n  print(nrow(dtable))\n  print(\"number of missing practice id\")\n  print(length(which(is.na(input$practice_id))))\n  \n  ## Create an empty data frame ---------------------------------------------------\n  table_1 <- data.frame(variable = character(),\n                        subgroup_level = character(),\n                        number  = numeric(),  \n                        percent = numeric(), \n                        mean    = numeric(),\n                        sd      = numeric(),\n                        inter_quartile_range     = numeric(),\n                        stringsAsFactors = FALSE)\n  \n  ## factor variables: number and percentage---------------------------------------\n  input_factor_vars <- input[, cov_factor_names]\n  for(i in 1:length(cov_factor_names)){\n    levels = names(table(input_factor_vars[,i]))\n    start = nrow(table_1)+1\n    ##table_1[start,1] = cov_factor_names[i]\n    ##start = nrow(table_1)+1\n    end = nrow(table_1)+length(levels)\n    table_1[start:end,1] <- rep(cov_factor_names[i], length(levels)) # covariate name\n    table_1[start:end,2] <- c(levels)            # subgroup level\n    table_1[start:end,3] <- c(table(input_factor_vars[,i]))  # number\n    table_1[start:end,4] <- 100*round(c(table(input_factor_vars[,i]))/nrow(input_factor_vars),4)  # percentage\n    print(levels)\n    \n    ## small number suppression by variable\n    table_1$number[start:end] = redactor2(table_1$number[start:end])\n  }\n  \n  ## numerical variables: number and percentage of observations, mean and standard deviations\n  input_num_vars <- input[,cov_num_names]\n  if(length(cov_num_names) == 1){\n    index = nrow(table_1)+1\n    table_1[index,1] <- cov_num_names\n    table_1[index,3] <- length(which(!is.na(unlist(input_num_vars)))) # number of observations\n    table_1[index,5] <- round(mean(unlist(input_num_vars)),2) # mean\n    table_1[index,6] <- round(sd(unlist(input_num_vars)),2) # sd\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: define eligible population\n## Output:  input_stage1_all.rds; input_stage1_vaccinated.rds; input_stage1_infected.rds\n\nlibrary(readr); library(dplyr); library(htmlTable)\nsource(\"analysis/functions/redactor2.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  cohort <- \"infected\"     # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\n\nstage1_eligibility <- function(cohort){\n  input <- read_rds(paste0(\"output/input_stage0_\", cohort, \".rds\"))\n  ## Specify study period\n  cohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\n  ## YW: in some cohort the cohort start date is different from the index date\n  ## for the vaccinated population and infected population, their index dates were 2nd vaccination + 14 days, and first covid dates during the follow-up\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  input$cohort_end_date = cohort_end\n  ##RK - I would move any hard coded dates to to the top of the script so you don't have to be searching through\n  ##scripts to find/check what dates you've been using\n  ##YW: this has now been moved to the top of the script\n  ################################################################################\n  ## Part 1. Define eligible population                                          #\n  ################################################################################\n  \n  steps <- c(\"starting point\",\"dead before index date\", \"missing region\", \"missing sex\", \n             \"missing age\", \"age <18y\", \"age>105y\",  \n             \"covid before index date\", \"long covid before index date\")\n  \n  ## starting point\n  flow_chart_n <- nrow(input)\n  \n  ## Dead: removed if dead before index date\n  input <- input%>%filter(death_date > index_date | is.na(death_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n\n  ## Region: remove if missing\n  ##input <- input%>%filter(!is.na(cov_cat_region))\n  input <- input%>%filter(cov_cat_region!=\"Missing\") # keep if not missing\n  table(input$cov_cat_region)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ##RK - in stage 0 you set NA's to 'Missing' so do you want to be removing 'Missing' instead?\n  ##YW - this is now changed to remove the \"Missing\" category for region and sex\n  ## Sex: remove if missing\n  input <- input%>%filter(cov_cat_sex!=\"Missing\")\n  table(input$cov_cat_sex)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage0_data_cleaning.R\nanalysis/table_1.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage1_define_eligible_population.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "61-140\n1-80\n1-80\n61-140\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[1,2,3,4,7],\"note\":\"Kept snippets that explicitly encode study and model parameters: (1) defines cohort-specific inclusion rules and COVID severity categories (no infection, non-hospitalised, hospitalised within 28 days) and removes variables, which are key parameter specifications; (2) constructs Table 1, specifying which covariates enter as categorical vs numeric and how summaries (counts, percentages, means, SD, IQR) are computed for baseline characteristics; (3) sets cohort start/end dates and implements eligibility/exclusion criteria (age bounds, missingness handling, death before index, etc.), which are core analysis parameters; (4) continues eligible-population definition, including exclusion based on COVID history and previous long COVID and explicitly defines age-group categories (18–39, 40–59, 60–79, 80–105), a key parameterization used in descriptive and hazard-ratio analyses; (7) orchestrates combination of hazard-ratio outputs from different model specifications (full vs age–sex adjusted, spline vs categorical age, time-dependent vaccination), encoding the parameterization of the Cox models underlying Figures 1–4. Other snippets are either purely plotting, largely comments/dated, or generic utility without directly specifying analysis parameters.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "1",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 1. Baseline characteristics of patients. \n#          Categorical variables: number and percentage\n#          Continuous variables:  number and percentage of observations, \n#                                 mean and standard deviation\n# Output:  table1.csv, table1.html\n\nlibrary(readr); library(dplyr); library(lubridate)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  ##cohort <- \"all\"          # all eligible population\n  ##cohort <- \"vaccinated\"   # vaccinated population\n  cohort <- \"infected\"      # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ntable1_creation <- function(cohort){\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  cov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\n  sub_factor_names <- names(input)[grepl(\"sub_cat\", names(input))]\n  cov_factor_names <- c(cov_factor_names, sub_factor_names)   # to include sub_cat_covid_phenotype\n  cov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n  sub_num_names <- names(input)[grepl(\"sub_num\", names(input))]\n  ## to include sub_num_gp_consultation which stores the original values before truncation\n  cov_num_names <- c(cov_num_names, sub_num_names) \n  \n  ## summary table for practice id - for exploration only\n  print(\"summary statistics for practice id\")\n  dtable <- table(input$practice_id)\n  ##print(table(input$practice_id))\n  print(\"number of practice id\")\n  print(nrow(dtable))\n  print(\"number of missing practice id\")\n  print(length(which(is.na(input$practice_id))))\n  \n  ## Create an empty data frame ---------------------------------------------------\n  table_1 <- data.frame(variable = character(),\n                        subgroup_level = character(),\n                        number  = numeric(),  \n                        percent = numeric(), \n                        mean    = numeric(),\n                        sd      = numeric(),\n                        inter_quartile_range     = numeric(),\n                        stringsAsFactors = FALSE)\n  \n  ## factor variables: number and percentage---------------------------------------\n  input_factor_vars <- input[, cov_factor_names]\n  for(i in 1:length(cov_factor_names)){\n    levels = names(table(input_factor_vars[,i]))\n    start = nrow(table_1)+1\n    ##table_1[start,1] = cov_factor_names[i]\n    ##start = nrow(table_1)+1\n    end = nrow(table_1)+length(levels)\n    table_1[start:end,1] <- rep(cov_factor_names[i], length(levels)) # covariate name\n    table_1[start:end,2] <- c(levels)            # subgroup level\n    table_1[start:end,3] <- c(table(input_factor_vars[,i]))  # number\n    table_1[start:end,4] <- 100*round(c(table(input_factor_vars[,i]))/nrow(input_factor_vars),4)  # percentage\n    print(levels)\n    \n    ## small number suppression by variable\n    table_1$number[start:end] = redactor2(table_1$number[start:end])\n  }\n  \n  ## numerical variables: number and percentage of observations, mean and standard deviations\n  input_num_vars <- input[,cov_num_names]\n  if(length(cov_num_names) == 1){\n    index = nrow(table_1)+1\n    table_1[index,1] <- cov_num_names\n    table_1[index,3] <- length(which(!is.na(unlist(input_num_vars)))) # number of observations\n    table_1[index,5] <- round(mean(unlist(input_num_vars)),2) # mean\n    table_1[index,6] <- round(sd(unlist(input_num_vars)),2) # sd",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_1.R",
      "code_lines": "1-80"
    },
    "llm_raw": "{\"keep_indices\":[2],\"note\":\"Kept index 2 (analysis/table_1.R) because it implements the generation of Table 1 baseline descriptive summaries (counts and percentages) for the four cohorts relevant to the specified analysis. The code shows that only descriptive statistics are produced without any hypothesis testing or confidence intervals, which is directly informative for the Inference Specification dimension (i.e., it evidences that no formal inferential criteria were applied for this analysis). All other snippets relate to cohort definition, data cleaning, or hazard ratio modeling/plotting for other analyses, and are not directly tied to inference criteria for the baseline characteristics summaries.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "1",
    "dimension": "Coding Specification",
    "value": "input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n!is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n    input_select <- input_select %>% mutate(lcovid_cens_vax_c = ifelse((out_first_long_covid_date <= fup_end_date_vax_c & \n                                                                          out_first_long_covid_date >= index_date &\n                                                                          !is.na(out_first_long_covid_date)), 1, 0))\n  }\n  #############################################################################################\n  ## Define time to post-viral fatigue defined between 29 January 2020 and 29 November 2020  ##\n  #############################################################################################\n  if(cohort == \"all\"){\n    input_select <- input_select %>% mutate(fup_end_date_fatigue=min(out_first_post_viral_fatigue_date_after_pandemic,\n                                                                     death_date, \n                                                                     cohort_end_date,\n                                                                     na.rm = TRUE))\n    ## time to post-viral fatigue diagnosis date\n    input_select$fatigue_surv<- as.numeric(input_select$fup_end_date_fatigue - input_select$index_date)+1\n    input_select <- input_select %>% mutate(fatigue_cens = ifelse((out_first_post_viral_fatigue_date_after_pandemic <= fup_end_date_fatigue &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= out_covid_date &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= index_date &\n                                                                     !is.na(out_first_post_viral_fatigue_date_after_pandemic) &\n                                                                     !is.na(out_covid_date)), 1, 0))\n  }\n  ################################################################################\n  ## Part 3. Create and output datasets                                         ##\n  ################################################################################\n  ## Create data set for analysis 1 and analysis 2\n  \n  if(cohort == \"all\"){\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\",\"lcovid_surv_vax_c\", \"lcovid_cens_vax_c\",\n                          \"fup_end_date_vax_c\", \"vax2_surv\", \"fatigue_surv\",\"fatigue_cens\")\n  }else{\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\", \"vax2_surv\")\n  }\n  \n  ## warnings if non-neg follow-up time\n  if (!all(input_select$lcovid_surv>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  if (!all(input_select$lcovid_cens>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  \n  ## warnings if follow-up days greater than maximum cohort days\n  if (!all(input_select$lcovid_surv <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  if (!all(input_select$lcovid_cens <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  \n  input_select <- input_select[,variables_to_keep]\n  \n  input <- input%>%dplyr::select(-index_date)\n  ## left join: keep all observations in input_select\n  input <- merge(x = input_select, y = input, by = \"patient_id\", all.x = TRUE)\n  \n  rm(input_select)\n  \n  if(cohort == \"infected\"){\n    input <- input %>% mutate(cov_cat_covid_phenotype = sub_cat_covid_phenotype)\n  }\n  \n  saveRDS(input, file = paste0(\"output/input_stage1_\", cohort, \".rds\"))\n  print(paste0(\"Stage 1 date set for \", cohort, \" created successfully!\"))\n  ################################################################################\n  ## Part 4. Flowchart output                                                   ##\n  ################################################################################\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "61-140\n181-260\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[1,6,9],\"note\":\"Kept snippets that define how key analysis variables are coded: (1) stage0_data_cleaning.R where COVID infection and severity phenotype are encoded as indicator and factor with specific categories; (6) stage1_define_eligible_population.R where the COVID phenotype factor is promoted to a covariate for the infected cohort; and (9) stage1_define_eligible_population.R where age-based inclusion, age group categories, region factor releveling, and COVID history coding are specified. Other snippets primarily handle descriptive tabulation, plotting, or are fully commented-out and thus not substantive executable coding specifications.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "2",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\nfor(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }\n  table_2 <- table_2 %>%filter(outcome == \"long covid\")\n  table(table_2$subgrp)\n\n  ## redaction by subgroup\n  ##RK - the redactor function looks like it's doing what it needs to do.\n  ##Currently you put both the long covid and covid counts in as one but should these be redacted separately\n  ##as these are separate outcomes? Otherwise you might redact one level in long covid then when it tries to \n  ##redact the next smallest it might do it in the covid outcome which won't remove the disclosure.\n  \n  ## YW: You are right! This is now amended by using redactor for covid and long covid separately\n  \n  \n  for(i in demographics){\n    for(j in c(\"covid\", \"long covid\")){\n      print(i)\n      index = which(table_2$subgrp == i & table_2$outcome == j)\n      table_2$event_count[index] <- redactor2(table_2$event_count[index])\n    }\n  }\n  col_names <- c(\"event_count\",\"person_years\",\"ir\", \"ir_lower\", \"ir_upper\")\n  table_2$subgrp <- gsub(\"cov_cat_\", \"\", table_2$subgrp)\n  \n  if(vax_c == TRUE){\n    cohort = \"all_vax_c\"\n  }\n  write.csv(table_2, file=paste0(\"output/review/descriptives/table_2_\", cohort,\".csv\"),row.names=F)\n}\nif(cohort == \"all_cohorts\") {\n  table2_creation(\"all\")\n  table2_creation(\"vaccinated\")\n  table2_creation(\"infected\")\n  table2_creation(\"all_vax_c\")\n} else{\n  table2_creation(cohort)\n}\n\ntable_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n  long_covid_count <- length(which(data$out_first_long_covid_date >= data$index_date &\n                                     data$out_first_long_covid_date <= data$fup_end_date))\n  \n  ## covid count\n  covid_count <- length(which(data$out_covid_date >= data$index_date &\n                                data$out_covid_date <= data$fup_end_date))\n  person_days_total = round(sum(data$person_days, na.rm=TRUE),1)\n  \n  table_2[1,4:8] <- compute_incidence_rate(covid_count, person_days_total)\n  table_2[2,4:8] <- compute_incidence_rate(long_covid_count, person_days_total)\n  table_2$outcome <- outcome\n  table_2$subgrp <- table_2$subgrp_level <- subgrp <- subgrp_level\n  \n  ## extend to subgroups by demographics + post viral fatigue\n  demographics <- c(\"cov_cat_sex\", \"cov_cat_age_group\", \"cov_cat_region\", \n                    \"cov_cat_ethnicity\", \"cov_cat_imd\",\n                    \"cov_cat_post_viral_fatigue_pre_pandemic\", \"cov_cat_bmi\",\n                    \"cov_cat_smoking_status\", \"cov_cat_gp_patient_interaction\")\n\n  \n  ##RK - not sure if this is just a dummy data check but if you think it could be in\n  ##the real data, should this be moved to before calculating the main IR on line 77/78?\n  \n  ##YW - it could be move to before line 77/78 to save some computing time\n  ##although it would not make a difference in results it is before or after\n  \n  \n  ##start.time = Sys.time()\n \n  for(i in demographics){\n    for(outcome in c(\"covid\", \"long covid\")){\n            print(i)\n            level <- names(table(data[,i]))\n            print(level)\n            start = nrow(table_2)+1\n            end = nrow(table_2)+length(level)\n            table_2[start:end,1] = rep(outcome, length(level))\n            table_2[start:end,2] = rep(i, length(level))\n            table_2$subgrp_level[start:end] = level\n            index = start\n            for(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/table_2.R\nanalysis/table_2.R",
      "code_lines": "1-80\n121-175\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,5,9],\"note\":\"Kept snippets from analysis/table_2.R that implement the computation of event counts, person-years, incidence rates, and confidence intervals for long COVID and COVID (the core statistical procedure underlying Table 2). Other snippets either relate to different analyses (e.g., Cox models/hazard ratios or sequence descriptions) or only display precomputed results without specifying the statistical calculation itself.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "2",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\n##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n  input$cov_cat_age_group <- factor(input$cov_cat_age_group, ordered = TRUE)\n  \n  ##RK - this was defined in stage 0 - why are you redefining it here?\n  ##YW - A good question, this is because the dummy data have some observations under 18, and by removing them,\n  ##     the number of levels in cov_cat_age_group has changed\n  \n  #################################################################################\n  ##Part 2. Define follow-up end date and construct survival data for long covid ##\n  #################################################################################\n  \n  ## To improve efficiency: keep only necessary variables\n  variables_to_keep <-c(\"patient_id\", \"out_first_long_covid_date\", \"vax_covid_date1\",\n                        \"vax_covid_date2\", \"death_date\", \"cohort_end_date\", \"index_date\",\n                        \"out_covid_date\",\n                        \"out_first_post_viral_fatigue_date_after_pandemic\")\n  \n  input_select <- input%>% dplyr::select(all_of(variables_to_keep))\n  \n  ####################################################################################\n  ## Cohort 1 - all eligible adults; Cohort 3: post-vaccination; Cohort 4: post-covid\n  ## Construct time to long COVID \n  ## lcovid_cens: indicator for long covid  \n  ## lcovid_surv: days from index date to long COVID, without censoring by vaccination \n  ## for the following cohorts\n  ####################################################################################\n  input_select <- input_select%>% rowwise() %>% mutate(fup_end_date=min(out_first_long_covid_date, \n                                                                        death_date, \n                                                                        cohort_end_date,\n                                                                        na.rm = TRUE))\n  input_select <- input_select %>% filter(fup_end_date >= index_date & fup_end_date != Inf)\n  input_select$lcovid_surv<- as.numeric(input_select$fup_end_date - input_select$index_date)+1\n\n  ## define event indicator, without censoring for vaccination\n  input_select <- input_select %>% mutate(lcovid_cens = ifelse((out_first_long_covid_date <= fup_end_date & \n                                                                  out_first_long_covid_date >= index_date &\n                                                                  !is.na(out_first_long_covid_date)), 1, 0)) %>%\n    ## define 2nd vaccination as an intervening event - a time-dependent variable\n    ## add 14 days as buffer to allow vaccination to take effects\n    mutate(vax2_surv = ifelse((vax_covid_date2 +14)>= index_date & \n                                (vax_covid_date2 + 14) <= fup_end_date &\n                                !is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n!is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n    input_select <- input_select %>% mutate(lcovid_cens_vax_c = ifelse((out_first_long_covid_date <= fup_end_date_vax_c & \n                                                                          out_first_long_covid_date >= index_date &\n                                                                          !is.na(out_first_long_covid_date)), 1, 0))\n  }\n  #############################################################################################\n  ## Define time to post-viral fatigue defined between 29 January 2020 and 29 November 2020  ##\n  #############################################################################################\n  if(cohort == \"all\"){\n    input_select <- input_select %>% mutate(fup_end_date_fatigue=min(out_first_post_viral_fatigue_date_after_pandemic,\n                                                                     death_date, \n                                                                     cohort_end_date,\n                                                                     na.rm = TRUE))\n    ## time to post-viral fatigue diagnosis date\n    input_select$fatigue_surv<- as.numeric(input_select$fup_end_date_fatigue - input_select$index_date)+1\n    input_select <- input_select %>% mutate(fatigue_cens = ifelse((out_first_post_viral_fatigue_date_after_pandemic <= fup_end_date_fatigue &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= out_covid_date &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= index_date &\n                                                                     !is.na(out_first_post_viral_fatigue_date_after_pandemic) &\n                                                                     !is.na(out_covid_date)), 1, 0))\n  }\n  ################################################################################\n  ## Part 3. Create and output datasets                                         ##\n  ################################################################################\n  ## Create data set for analysis 1 and analysis 2\n  \n  if(cohort == \"all\"){\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\",\"lcovid_surv_vax_c\", \"lcovid_cens_vax_c\",\n                          \"fup_end_date_vax_c\", \"vax2_surv\", \"fatigue_surv\",\"fatigue_cens\")\n  }else{\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\", \"vax2_surv\")\n  }\n  \n  ## warnings if non-neg follow-up time\n  if (!all(input_select$lcovid_surv>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  if (!all(input_select$lcovid_cens>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  \n  ## warnings if follow-up days greater than maximum cohort days\n  if (!all(input_select$lcovid_surv <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  if (!all(input_select$lcovid_cens <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  \n  input_select <- input_select[,variables_to_keep]\n  \n  input <- input%>%dplyr::select(-index_date)\n  ## left join: keep all observations in input_select\n  input <- merge(x = input_select, y = input, by = \"patient_id\", all.x = TRUE)\n  \n  rm(input_select)\n  \n  if(cohort == \"infected\"){\n    input <- input %>% mutate(cov_cat_covid_phenotype = sub_cat_covid_phenotype)\n  }\n  \n  saveRDS(input, file = paste0(\"output/input_stage1_\", cohort, \".rds\"))\n  print(paste0(\"Stage 1 date set for \", cohort, \" created successfully!\"))\n  ################################################################################\n  ## Part 4. Flowchart output                                                   ##\n  ################################################################################\n\nfor(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }\n  table_2 <- table_2 %>%filter(outcome == \"long covid\")\n  table(table_2$subgrp)\n\n  ## redaction by subgroup\n  ##RK - the redactor function looks like it's doing what it needs to do.\n  ##Currently you put both the long covid and covid counts in as one but should these be redacted separately\n  ##as these are separate outcomes? Otherwise you might redact one level in long covid then when it tries to \n  ##redact the next smallest it might do it in the covid outcome which won't remove the disclosure.\n  \n  ## YW: You are right! This is now amended by using redactor for covid and long covid separately\n  \n  \n  for(i in demographics){\n    for(j in c(\"covid\", \"long covid\")){\n      print(i)\n      index = which(table_2$subgrp == i & table_2$outcome == j)\n      table_2$event_count[index] <- redactor2(table_2$event_count[index])\n    }\n  }\n  col_names <- c(\"event_count\",\"person_years\",\"ir\", \"ir_lower\", \"ir_upper\")\n  table_2$subgrp <- gsub(\"cov_cat_\", \"\", table_2$subgrp)\n  \n  if(vax_c == TRUE){\n    cohort = \"all_vax_c\"\n  }\n  write.csv(table_2, file=paste0(\"output/review/descriptives/table_2_\", cohort,\".csv\"),row.names=F)\n}\nif(cohort == \"all_cohorts\") {\n  table2_creation(\"all\")\n  table2_creation(\"vaccinated\")\n  table2_creation(\"infected\")\n  table2_creation(\"all_vax_c\")\n} else{\n  table2_creation(cohort)\n}",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R\nanalysis/table_2.R",
      "code_lines": "1-80\n121-200\n1-80\n181-260\n121-175"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,3,5],\"note\":\"Selected snippets define key analysis variables (index dates, cohort membership, long COVID outcome dates, follow-up times, censoring, and incidence rate components) and implement the event counting and incidence rate calculations for long COVID used in Table 2. Other snippets relate to different descriptive tables, plotting, or secondary analyses and are less directly relevant to the specified analysis and variable specification dimension.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "2",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\n##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n  input$cov_cat_age_group <- factor(input$cov_cat_age_group, ordered = TRUE)\n  \n  ##RK - this was defined in stage 0 - why are you redefining it here?\n  ##YW - A good question, this is because the dummy data have some observations under 18, and by removing them,\n  ##     the number of levels in cov_cat_age_group has changed\n  \n  #################################################################################\n  ##Part 2. Define follow-up end date and construct survival data for long covid ##\n  #################################################################################\n  \n  ## To improve efficiency: keep only necessary variables\n  variables_to_keep <-c(\"patient_id\", \"out_first_long_covid_date\", \"vax_covid_date1\",\n                        \"vax_covid_date2\", \"death_date\", \"cohort_end_date\", \"index_date\",\n                        \"out_covid_date\",\n                        \"out_first_post_viral_fatigue_date_after_pandemic\")\n  \n  input_select <- input%>% dplyr::select(all_of(variables_to_keep))\n  \n  ####################################################################################\n  ## Cohort 1 - all eligible adults; Cohort 3: post-vaccination; Cohort 4: post-covid\n  ## Construct time to long COVID \n  ## lcovid_cens: indicator for long covid  \n  ## lcovid_surv: days from index date to long COVID, without censoring by vaccination \n  ## for the following cohorts\n  ####################################################################################\n  input_select <- input_select%>% rowwise() %>% mutate(fup_end_date=min(out_first_long_covid_date, \n                                                                        death_date, \n                                                                        cohort_end_date,\n                                                                        na.rm = TRUE))\n  input_select <- input_select %>% filter(fup_end_date >= index_date & fup_end_date != Inf)\n  input_select$lcovid_surv<- as.numeric(input_select$fup_end_date - input_select$index_date)+1\n\n  ## define event indicator, without censoring for vaccination\n  input_select <- input_select %>% mutate(lcovid_cens = ifelse((out_first_long_covid_date <= fup_end_date & \n                                                                  out_first_long_covid_date >= index_date &\n                                                                  !is.na(out_first_long_covid_date)), 1, 0)) %>%\n    ## define 2nd vaccination as an intervening event - a time-dependent variable\n    ## add 14 days as buffer to allow vaccination to take effects\n    mutate(vax2_surv = ifelse((vax_covid_date2 +14)>= index_date & \n                                (vax_covid_date2 + 14) <= fup_end_date &\n                                !is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ntable_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n  long_covid_count <- length(which(data$out_first_long_covid_date >= data$index_date &\n                                     data$out_first_long_covid_date <= data$fup_end_date))\n  \n  ## covid count\n  covid_count <- length(which(data$out_covid_date >= data$index_date &\n                                data$out_covid_date <= data$fup_end_date))\n  person_days_total = round(sum(data$person_days, na.rm=TRUE),1)\n  \n  table_2[1,4:8] <- compute_incidence_rate(covid_count, person_days_total)\n  table_2[2,4:8] <- compute_incidence_rate(long_covid_count, person_days_total)\n  table_2$outcome <- outcome\n  table_2$subgrp <- table_2$subgrp_level <- subgrp <- subgrp_level\n  \n  ## extend to subgroups by demographics + post viral fatigue\n  demographics <- c(\"cov_cat_sex\", \"cov_cat_age_group\", \"cov_cat_region\", \n                    \"cov_cat_ethnicity\", \"cov_cat_imd\",\n                    \"cov_cat_post_viral_fatigue_pre_pandemic\", \"cov_cat_bmi\",\n                    \"cov_cat_smoking_status\", \"cov_cat_gp_patient_interaction\")\n\n  \n  ##RK - not sure if this is just a dummy data check but if you think it could be in\n  ##the real data, should this be moved to before calculating the main IR on line 77/78?\n  \n  ##YW - it could be move to before line 77/78 to save some computing time\n  ##although it would not make a difference in results it is before or after\n  \n  \n  ##start.time = Sys.time()\n \n  for(i in demographics){\n    for(outcome in c(\"covid\", \"long covid\")){\n            print(i)\n            level <- names(table(data[,i]))\n            print(level)\n            start = nrow(table_2)+1\n            end = nrow(table_2)+length(level)\n            table_2[start:end,1] = rep(outcome, length(level))\n            table_2[start:end,2] = rep(i, length(level))\n            table_2$subgrp_level[start:end] = level\n            index = start\n            for(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: define eligible population\n## Output:  input_stage1_all.rds; input_stage1_vaccinated.rds; input_stage1_infected.rds\n\nlibrary(readr); library(dplyr); library(htmlTable)\nsource(\"analysis/functions/redactor2.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  cohort <- \"infected\"     # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\n\nstage1_eligibility <- function(cohort){\n  input <- read_rds(paste0(\"output/input_stage0_\", cohort, \".rds\"))\n  ## Specify study period\n  cohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\n  ## YW: in some cohort the cohort start date is different from the index date\n  ## for the vaccinated population and infected population, their index dates were 2nd vaccination + 14 days, and first covid dates during the follow-up\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  input$cohort_end_date = cohort_end\n  ##RK - I would move any hard coded dates to to the top of the script so you don't have to be searching through\n  ##scripts to find/check what dates you've been using\n  ##YW: this has now been moved to the top of the script\n  ################################################################################\n  ## Part 1. Define eligible population                                          #\n  ################################################################################\n  \n  steps <- c(\"starting point\",\"dead before index date\", \"missing region\", \"missing sex\", \n             \"missing age\", \"age <18y\", \"age>105y\",  \n             \"covid before index date\", \"long covid before index date\")\n  \n  ## starting point\n  flow_chart_n <- nrow(input)\n  \n  ## Dead: removed if dead before index date\n  input <- input%>%filter(death_date > index_date | is.na(death_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n\n  ## Region: remove if missing\n  ##input <- input%>%filter(!is.na(cov_cat_region))\n  input <- input%>%filter(cov_cat_region!=\"Missing\") # keep if not missing\n  table(input$cov_cat_region)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ##RK - in stage 0 you set NA's to 'Missing' so do you want to be removing 'Missing' instead?\n  ##YW - this is now changed to remove the \"Missing\" category for region and sex\n  ## Sex: remove if missing\n  input <- input%>%filter(cov_cat_sex!=\"Missing\")\n  table(input$cov_cat_sex)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage0_data_cleaning.R\nanalysis/table_2.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n121-200\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,4,6,8],\"note\":\"Selected snippets that define cohort/index/follow-up dates, eligibility/exclusion rules, event indicators, and incidence rate (per person-years) calculations for long COVID across cohorts, which directly capture the parameter specifications underlying the descriptive incidence analyses in Table 2.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "2",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\ntable_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n  long_covid_count <- length(which(data$out_first_long_covid_date >= data$index_date &\n                                     data$out_first_long_covid_date <= data$fup_end_date))\n  \n  ## covid count\n  covid_count <- length(which(data$out_covid_date >= data$index_date &\n                                data$out_covid_date <= data$fup_end_date))\n  person_days_total = round(sum(data$person_days, na.rm=TRUE),1)\n  \n  table_2[1,4:8] <- compute_incidence_rate(covid_count, person_days_total)\n  table_2[2,4:8] <- compute_incidence_rate(long_covid_count, person_days_total)\n  table_2$outcome <- outcome\n  table_2$subgrp <- table_2$subgrp_level <- subgrp <- subgrp_level\n  \n  ## extend to subgroups by demographics + post viral fatigue\n  demographics <- c(\"cov_cat_sex\", \"cov_cat_age_group\", \"cov_cat_region\", \n                    \"cov_cat_ethnicity\", \"cov_cat_imd\",\n                    \"cov_cat_post_viral_fatigue_pre_pandemic\", \"cov_cat_bmi\",\n                    \"cov_cat_smoking_status\", \"cov_cat_gp_patient_interaction\")\n\n  \n  ##RK - not sure if this is just a dummy data check but if you think it could be in\n  ##the real data, should this be moved to before calculating the main IR on line 77/78?\n  \n  ##YW - it could be move to before line 77/78 to save some computing time\n  ##although it would not make a difference in results it is before or after\n  \n  \n  ##start.time = Sys.time()\n \n  for(i in demographics){\n    for(outcome in c(\"covid\", \"long covid\")){\n            print(i)\n            level <- names(table(data[,i]))\n            print(level)\n            start = nrow(table_2)+1\n            end = nrow(table_2)+length(level)\n            table_2[start:end,1] = rep(outcome, length(level))\n            table_2[start:end,2] = rep(i, length(level))\n            table_2$subgrp_level[start:end] = level\n            index = start\n            for(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }\n\nfor(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }\n  table_2 <- table_2 %>%filter(outcome == \"long covid\")\n  table(table_2$subgrp)\n\n  ## redaction by subgroup\n  ##RK - the redactor function looks like it's doing what it needs to do.\n  ##Currently you put both the long covid and covid counts in as one but should these be redacted separately\n  ##as these are separate outcomes? Otherwise you might redact one level in long covid then when it tries to \n  ##redact the next smallest it might do it in the covid outcome which won't remove the disclosure.\n  \n  ## YW: You are right! This is now amended by using redactor for covid and long covid separately\n  \n  \n  for(i in demographics){\n    for(j in c(\"covid\", \"long covid\")){\n      print(i)\n      index = which(table_2$subgrp == i & table_2$outcome == j)\n      table_2$event_count[index] <- redactor2(table_2$event_count[index])\n    }\n  }\n  col_names <- c(\"event_count\",\"person_years\",\"ir\", \"ir_lower\", \"ir_upper\")\n  table_2$subgrp <- gsub(\"cov_cat_\", \"\", table_2$subgrp)\n  \n  if(vax_c == TRUE){\n    cohort = \"all_vax_c\"\n  }\n  write.csv(table_2, file=paste0(\"output/review/descriptives/table_2_\", cohort,\".csv\"),row.names=F)\n}\nif(cohort == \"all_cohorts\") {\n  table2_creation(\"all\")\n  table2_creation(\"vaccinated\")\n  table2_creation(\"infected\")\n  table2_creation(\"all_vax_c\")\n} else{\n  table2_creation(cohort)\n}",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/table_2.R\nanalysis/table_2.R",
      "code_lines": "1-80\n61-140\n121-175"
    },
    "llm_raw": "{\"keep_indices\":[0,5,2],\"note\":\"Kept R scripts that implement the calculation of incidence rates and their 95% confidence intervals via the compute_incidence_rate function (using 1.96 * standard error), which directly encodes the inference specification for incidence-rate CIs used in Table 2. Other snippets are descriptive setup, data management, or reporting without explicit inference/CI computation logic.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "2",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\n##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n  input$cov_cat_age_group <- factor(input$cov_cat_age_group, ordered = TRUE)\n  \n  ##RK - this was defined in stage 0 - why are you redefining it here?\n  ##YW - A good question, this is because the dummy data have some observations under 18, and by removing them,\n  ##     the number of levels in cov_cat_age_group has changed\n  \n  #################################################################################\n  ##Part 2. Define follow-up end date and construct survival data for long covid ##\n  #################################################################################\n  \n  ## To improve efficiency: keep only necessary variables\n  variables_to_keep <-c(\"patient_id\", \"out_first_long_covid_date\", \"vax_covid_date1\",\n                        \"vax_covid_date2\", \"death_date\", \"cohort_end_date\", \"index_date\",\n                        \"out_covid_date\",\n                        \"out_first_post_viral_fatigue_date_after_pandemic\")\n  \n  input_select <- input%>% dplyr::select(all_of(variables_to_keep))\n  \n  ####################################################################################\n  ## Cohort 1 - all eligible adults; Cohort 3: post-vaccination; Cohort 4: post-covid\n  ## Construct time to long COVID \n  ## lcovid_cens: indicator for long covid  \n  ## lcovid_surv: days from index date to long COVID, without censoring by vaccination \n  ## for the following cohorts\n  ####################################################################################\n  input_select <- input_select%>% rowwise() %>% mutate(fup_end_date=min(out_first_long_covid_date, \n                                                                        death_date, \n                                                                        cohort_end_date,\n                                                                        na.rm = TRUE))\n  input_select <- input_select %>% filter(fup_end_date >= index_date & fup_end_date != Inf)\n  input_select$lcovid_surv<- as.numeric(input_select$fup_end_date - input_select$index_date)+1\n\n  ## define event indicator, without censoring for vaccination\n  input_select <- input_select %>% mutate(lcovid_cens = ifelse((out_first_long_covid_date <= fup_end_date & \n                                                                  out_first_long_covid_date >= index_date &\n                                                                  !is.na(out_first_long_covid_date)), 1, 0)) %>%\n    ## define 2nd vaccination as an intervening event - a time-dependent variable\n    ## add 14 days as buffer to allow vaccination to take effects\n    mutate(vax2_surv = ifelse((vax_covid_date2 +14)>= index_date & \n                                (vax_covid_date2 + 14) <= fup_end_date &\n                                !is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n\n!is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1\n    input_select <- input_select %>% mutate(lcovid_cens_vax_c = ifelse((out_first_long_covid_date <= fup_end_date_vax_c & \n                                                                          out_first_long_covid_date >= index_date &\n                                                                          !is.na(out_first_long_covid_date)), 1, 0))\n  }\n  #############################################################################################\n  ## Define time to post-viral fatigue defined between 29 January 2020 and 29 November 2020  ##\n  #############################################################################################\n  if(cohort == \"all\"){\n    input_select <- input_select %>% mutate(fup_end_date_fatigue=min(out_first_post_viral_fatigue_date_after_pandemic,\n                                                                     death_date, \n                                                                     cohort_end_date,\n                                                                     na.rm = TRUE))\n    ## time to post-viral fatigue diagnosis date\n    input_select$fatigue_surv<- as.numeric(input_select$fup_end_date_fatigue - input_select$index_date)+1\n    input_select <- input_select %>% mutate(fatigue_cens = ifelse((out_first_post_viral_fatigue_date_after_pandemic <= fup_end_date_fatigue &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= out_covid_date &\n                                                                     out_first_post_viral_fatigue_date_after_pandemic >= index_date &\n                                                                     !is.na(out_first_post_viral_fatigue_date_after_pandemic) &\n                                                                     !is.na(out_covid_date)), 1, 0))\n  }\n  ################################################################################\n  ## Part 3. Create and output datasets                                         ##\n  ################################################################################\n  ## Create data set for analysis 1 and analysis 2\n  \n  if(cohort == \"all\"){\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\",\"lcovid_surv_vax_c\", \"lcovid_cens_vax_c\",\n                          \"fup_end_date_vax_c\", \"vax2_surv\", \"fatigue_surv\",\"fatigue_cens\")\n  }else{\n    variables_to_keep <-c(\"patient_id\", \"fup_end_date\",\"index_date\",\n                          \"lcovid_surv\", \"lcovid_cens\", \"vax2_surv\")\n  }\n  \n  ## warnings if non-neg follow-up time\n  if (!all(input_select$lcovid_surv>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  if (!all(input_select$lcovid_cens>=0)) warning(\"lcovid_surv should be  >= 0 in input_select\")\n  \n  ## warnings if follow-up days greater than maximum cohort days\n  if (!all(input_select$lcovid_surv <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  if (!all(input_select$lcovid_cens <= cohort_days)) warning(\"lcovid_surv should be  <= cohort_days in input_select\")\n  \n  input_select <- input_select[,variables_to_keep]\n  \n  input <- input%>%dplyr::select(-index_date)\n  ## left join: keep all observations in input_select\n  input <- merge(x = input_select, y = input, by = \"patient_id\", all.x = TRUE)\n  \n  rm(input_select)\n  \n  if(cohort == \"infected\"){\n    input <- input %>% mutate(cov_cat_covid_phenotype = sub_cat_covid_phenotype)\n  }\n  \n  saveRDS(input, file = paste0(\"output/input_stage1_\", cohort, \".rds\"))\n  print(paste0(\"Stage 1 date set for \", cohort, \" created successfully!\"))\n  ################################################################################\n  ## Part 4. Flowchart output                                                   ##\n  ################################################################################\n\nfor(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }\n  table_2 <- table_2 %>%filter(outcome == \"long covid\")\n  table(table_2$subgrp)\n\n  ## redaction by subgroup\n  ##RK - the redactor function looks like it's doing what it needs to do.\n  ##Currently you put both the long covid and covid counts in as one but should these be redacted separately\n  ##as these are separate outcomes? Otherwise you might redact one level in long covid then when it tries to \n  ##redact the next smallest it might do it in the covid outcome which won't remove the disclosure.\n  \n  ## YW: You are right! This is now amended by using redactor for covid and long covid separately\n  \n  \n  for(i in demographics){\n    for(j in c(\"covid\", \"long covid\")){\n      print(i)\n      index = which(table_2$subgrp == i & table_2$outcome == j)\n      table_2$event_count[index] <- redactor2(table_2$event_count[index])\n    }\n  }\n  col_names <- c(\"event_count\",\"person_years\",\"ir\", \"ir_lower\", \"ir_upper\")\n  table_2$subgrp <- gsub(\"cov_cat_\", \"\", table_2$subgrp)\n  \n  if(vax_c == TRUE){\n    cohort = \"all_vax_c\"\n  }\n  write.csv(table_2, file=paste0(\"output/review/descriptives/table_2_\", cohort,\".csv\"),row.names=F)\n}\nif(cohort == \"all_cohorts\") {\n  table2_creation(\"all\")\n  table2_creation(\"vaccinated\")\n  table2_creation(\"infected\")\n  table2_creation(\"all_vax_c\")\n} else{\n  table2_creation(cohort)\n}\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage1_define_eligible_population.R\nanalysis/table_2.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n121-200\n181-260\n121-175\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,4,6],\"note\":\"Kept snippets that define variable coding and cohort/time-at-risk construction for long COVID incidence (index dates, follow-up, event indicators, age groups, regions) and the Table 2 incidence rate calculations. Excluded others that focus mainly on sequence description, plotting, or SNOMED summaries not directly used for the incidence rate analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "3",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/table_2.R",
      "code_lines": "1-60\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[3,6,9],\"note\":\"Indices 3 and 6 contain the Kaplan–Meier survival analysis code (survfit-based KM estimation and derivation of cumulative incidence/probability by age and sex), directly matching the KM test specification described. Index 9 contains the computation of incidence rates per person-years (and confidence intervals), corresponding to the descriptive incidence rate analysis. Other snippets are primarily plotting wrappers around precomputed KM data or unrelated utilities and add little about the statistical test specification itself.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "3",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/externals/ext_figure_km.R",
      "code_lines": "1-80\n1-80\n1-80\n1-60\n1-79"
    },
    "llm_raw": "{\"keep_indices\":[3,5,8,7,4],\"note\":\"Kept scripts that directly specify and use the long COVID outcome and key stratifying variables (age group, sex, etc.) to compute incidence rates or cumulative incidence/probability: table_2.R (event counts, person-years, incidence rates), figure_kaplan_meier_cohorts.R and figure_kaplan_meier.R (KM-based cumulative incidence by age and sex across cohorts), figure_cum_prob_km.R and externals/ext_figure_km.R (KM and plotting code using cov_cat_age_group and sex).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "3",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R",
      "code_lines": "1-80"
    },
    "llm_raw": "{\"keep_indices\":[5],\"note\":\"Kept index 5 (table_2.R) because it implements the incidence rate calculations (event counts, person-years, incidence per 1000 person-years) used in Table 2 and encodes key analysis parameters such as cohort start/end dates, follow-up time, censoring rules, and the function that computes incidence rates. Other snippets mainly produce Kaplan–Meier plots or specify Cox model formulas and are less directly tied to the parameter specification of the descriptive stratified incidence rate analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "3",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\ntable_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n  long_covid_count <- length(which(data$out_first_long_covid_date >= data$index_date &\n                                     data$out_first_long_covid_date <= data$fup_end_date))\n  \n  ## covid count\n  covid_count <- length(which(data$out_covid_date >= data$index_date &\n                                data$out_covid_date <= data$fup_end_date))\n  person_days_total = round(sum(data$person_days, na.rm=TRUE),1)\n  \n  table_2[1,4:8] <- compute_incidence_rate(covid_count, person_days_total)\n  table_2[2,4:8] <- compute_incidence_rate(long_covid_count, person_days_total)\n  table_2$outcome <- outcome\n  table_2$subgrp <- table_2$subgrp_level <- subgrp <- subgrp_level\n  \n  ## extend to subgroups by demographics + post viral fatigue\n  demographics <- c(\"cov_cat_sex\", \"cov_cat_age_group\", \"cov_cat_region\", \n                    \"cov_cat_ethnicity\", \"cov_cat_imd\",\n                    \"cov_cat_post_viral_fatigue_pre_pandemic\", \"cov_cat_bmi\",\n                    \"cov_cat_smoking_status\", \"cov_cat_gp_patient_interaction\")\n\n  \n  ##RK - not sure if this is just a dummy data check but if you think it could be in\n  ##the real data, should this be moved to before calculating the main IR on line 77/78?\n  \n  ##YW - it could be move to before line 77/78 to save some computing time\n  ##although it would not make a difference in results it is before or after\n  \n  \n  ##start.time = Sys.time()\n \n  for(i in demographics){\n    for(outcome in c(\"covid\", \"long covid\")){\n            print(i)\n            level <- names(table(data[,i]))\n            print(level)\n            start = nrow(table_2)+1\n            end = nrow(table_2)+length(level)\n            table_2[start:end,1] = rep(outcome, length(level))\n            table_2[start:end,2] = rep(i, length(level))\n            table_2$subgrp_level[start:end] = level\n            index = start\n            for(j in level){\n             # index = which(data[,i]==j)\n              print(j)\n              sub_data <- data[which(data[,i]==j),]\n              if(outcome == \"covid\"){\n                count <- length(which(sub_data$out_covid_date >= sub_data$index_date &\n                                            sub_data$out_covid_date <= sub_data$fup_end_date))\n              }\n              if(outcome==\"long covid\"){\n                count <- length(which(sub_data$out_first_long_covid_date >= sub_data$index_date &\n                                      sub_data$out_first_long_covid_date <= sub_data$fup_end_date))\n  \n              }\n              # calculate total follow-up days\n              person_days_total = round(sum(sub_data$person_days, na.rm=TRUE),1)\n              table_2[index,4:8] <- compute_incidence_rate(count, person_days_total)\n              index = index+1\n            }\n      }\n  }",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_2.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/table_2.R",
      "code_lines": "1-80\n1-60\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[5,6,9],\"note\":\"Kept snippets that implement the incidence rate calculations with 95% confidence intervals for Table 2 (indices 5 and 9) and the Kaplan–Meier cumulative probability estimation with specified confidence type (index 6), all directly relevant to the descriptive incidence and cumulative probability analyses and their inferential specifications (CI calculation).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "3",
    "dimension": "Coding Specification",
    "value": "## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Count and pie chart of snomed code for long COVID diagnosis\n## Output:  suppl_table_1.csv, suppl_table_1.html, suppl_figure_pie.svg\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\n## Read in data and identify factor variables and numerical variables------------\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n## keep only observations where long covid indicator is 1\ninput <- input %>% filter(lcovid_cens == 1)\n\n## computational efficiency: only keep the needed variable\ninput <- input %>% dplyr::select(\"out_first_long_covid_code\")\n\nsnomed_code <- input$out_first_long_covid_code\n\ncount_data <-table(snomed_code)\n\ncount_data <- data.frame(count_data)\n\n\nnames(count_data) <- c(\"snomed_code\", \"count\")\ncount_data\ncount_data$percent = round(count_data$count / sum(count_data$count),3)\ncount_data\npercent_function <- function(x, digits = 1, format = \"f\", ...) {\n  paste0(formatC(100 * x, format = format, digits = digits, ...), \"%\")\n}\n\ncount_data$labels = percent_function(count_data$percent)\n\ncount_data$count <- redactor2(count_data$count)\nindex = which(is.na(count_data$count))\ncol_names <- c(\"count\",\"percent\",\"labels\")\ncount_data[index,col_names]= NA\n\n## use redactor for small number suppression\n## index <- which(count_data$count < 6)\n## count_data$count[index] = count_data$percent[index] = count_data$labels[index] = NA\n\ncount_data_active = count_data%>%filter(count>5)\n## Pie Chart\nsuppl_figure_pie <- ggplot(count_data_active, aes(x = \"\", y = count, fill = snomed_code)) +\n  geom_bar(width = 1, stat = \"identity\") +\n  coord_polar(theta = \"y\") +\n  labs(x = \"\", y = \"\", fill = \"SNOMED Code\") + \n  geom_text(aes(label = labels),\n            position = position_stack(vjust = 0.5)) +\n  theme(plot.title = element_text(hjust = 0.5), \n        legend.title = element_text(hjust = 0.5, face=\"bold\", size = 10), \n        axis.ticks = element_blank(), \n        axis.text.y = element_blank(),\n        axis.text.x = element_blank()) \n\nsuppl_figure_pie\n\n## supplementary figure - pie chart\nggsave(file=\"output/not_for_review/descriptives/snomed_code_pie.svg\", plot=suppl_figure_pie, width=16, height=8)\n\n## output underlying count data for supplementary figure - pie chart\n\n## small number suppression - indicate NA as redacted\ncount_data[which(is.na(count_data$count)),col_names]=\"[redacted]\" \n\nwrite.csv(count_data, file=\"output/not_for_review/descriptives/snomed_code_table.csv\")\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Table 2: Event count, person years and incidence rate\n# Output:  table_2.csv, table_2.html\n\nlibrary(readr); library(dplyr);library(lubridate)\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"          # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"      # infected population\n  #cohort <- \"all_vax_c\"     # all eligible population but follow-up censored by 1st vaccination\n}else{\n  cohort <- args[[1]]\n}\n\ntable2_creation <- function(cohort){\n  vax_c = FALSE\n  if(cohort == \"all_vax_c\"){\n    cohort = \"all\"\n    vax_c = TRUE\n  }\n  ## Read in data and identify factor variables and numerical variables------------\n  input <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\n  if(vax_c == TRUE){\n    input$fup_end_date = input$fup_end_date_vax_c\n    input$lcovid_surv = input$lcovid_surv_vax_c\n  }\n  ## define variables to keep\n  vax <- names(input)[grepl(\"vax_covid_\", names(input))]\n  keep <- names(input)[!names(input)%in%(vax)]\n  data <- input[,keep]\n  \n  ## calculate follow-up days\n  data <- data %>% rename(person_days = lcovid_surv) # days from time origin to follow-up end date, previously calculated\n  hist(data$person_days)\n\n\n  \n  ## function to calculate incidence rate for covid infection, do not calculate if event count <= 5\n  compute_incidence_rate <- function(event_count, person_days_total){\n      person_years_total = round(person_days_total / 365.2,4)\n      # ir = incidence rate\n      ir = round(event_count/person_years_total,4)\n      ir_lower = round(ir - 1.96 * sqrt(event_count/person_years_total^2),4)\n      ir_upper = round(ir + 1.96 * sqrt(event_count/person_years_total^2),4)\n    return(c(event_count,person_years_total, ir, ir_lower, ir_upper))\n  }\n  \n  ## create an empty data frame\n  table_2 <- data.frame(outcome = character(),\n                        subgrp = character(),\n                        subgrp_level = character(),\n                        event_count = numeric(),\n                        person_years = numeric(),\n                        ir = numeric(),\n                        ir_lower = numeric(),\n                        ir_upper = numeric()\n                        )\n  \n  outcome <- c(\"covid\", \"long covid\")\n  subgrp <- subgrp_level <- c(\"main\",\"main\")\n  # #hist(data$person_days)\n  # YW: 21/09/2022, ideally the following filter should be removed or placed in stage_1_eligibility\n  print(\"before applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  data <- data %>% filter(person_days >= 0 & person_days <= cohort_days)\n  print(\"after applying filter [0, cohort_days]!\")\n  print(nrow(data))\n  ## long covid count\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/table_snomed_code.R\nanalysis/table_2.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/dated code/figure_cum_prob_km.R",
      "code_lines": "1-74\n1-80\n1-80\n1-80\n1-60"
    },
    "llm_raw": "{\"keep_indices\":[0,4,5,6,8],\"note\":\"Kept snippets that define or implement how variables are coded or stratified for the descriptive incidence / KM analyses of long COVID. Index 0 codes the outcome via SNOMED categories before counting. Index 4 constructs Table 2, including incidence rate computation by outcome and subgroups, reflecting coding of analysis strata. Indices 5, 6, and 8 explicitly set factor levels for age-group and sex variables and then use those coded categories in Kaplan–Meier / cumulative incidence or probability plots, directly matching the described age–sex stratified incidence and cumulative probability results.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "4",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R\nanalysis/externals/ext_figure_km.R",
      "code_lines": "1-60\n1-80\n1-80\n61-111\n1-79"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,4,7],\"note\":\"Kept snippets that implement Kaplan–Meier survival estimation and/or plotting of cumulative incidence/probability of long COVID by age group and sex (test specification for KM curves). Other snippets focus on Cox models, validation, or generic survival summaries without KM by age/sex and were excluded.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "4",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R",
      "code_lines": "1-60\n1-80\n1-80\n61-111"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,6],\"note\":\"Kept snippets that implement Kaplan–Meier survival analyses and cumulative probability/incidence plots for coded long COVID using the key variables: survival time (lcovid_surv), censoring indicator (lcovid_cens), age group (cov_cat_age_group), and sex (cov_cat_sex), directly matching the variable specification for the described KM analysis by age group and sex.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "4",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n\"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  # ggsave(file= \"km_plot.png\",\n  #        path = paste0(output_dir, \"figures\"),\n  #        plot=p2, width=10, height=8)\n  dev.off()\n# }\n  \n  sub_dat <- dat_age %>% filter(cov_cat_age_group == \"40-59\")\n  index_m <- which(sub_dat$sex == \"Male\")\n  index_f <- which(sub_dat$sex == \"Female\")\n  \n  summary(sub_dat$y_prob[index_m])\n  summary(sub_dat$y_prob[index_f])\n  \n# cohort = \"all\"\n# km_plot(results_dir, output_dir, cohort)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R\nanalysis/externals/ext_figure_km_v2.R\nanalysis/externals/ext_figure_km.R",
      "code_lines": "1-60\n1-80\n61-111\n61-92\n1-79"
    },
    "llm_raw": "{\"keep_indices\":[0,2,3,8,9],\"note\":\"Kept snippets that implement Kaplan–Meier-based cumulative incidence/probability of coded long COVID stratified by age group and sex, including key parameter settings (e.g., survfit type/confidence, transformation to cumulative probability, age-group factor levels) and downstream derivations/summaries used to obtain age–sex specific cumulative probabilities (notably for ages 40–59).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "4",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/figure_kaplan_meier_cohorts.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier_cohorts.R",
      "code_lines": "1-80\n1-60\n1-80\n61-111\n61-123"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,3,8],\"note\":\"Kept Kaplan–Meier code that computes or uses KM estimates for cumulative incidence/probability of long COVID by age/sex, including the survfit-based script with explicit confidence interval specification (conf.type) and closely related plotting scripts; these are directly relevant to the KM-based cumulative probability analysis and its inference aspects.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "4",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))",
    "source": "code",
    "evidence": {
      "code_path": "analysis/figure_kaplan_meier_cohorts.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier.R",
      "code_lines": "1-80\n1-60\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2],\"note\":\"Kept snippets that implement the Kaplan–Meier cumulative probability analysis stratified by age group and sex and explicitly show how age and sex variables are coded (factor levels, groupings). Other snippets were either downstream plotting-only code or related to different analyses (e.g., Cox models, workflow orchestration) rather than the requested KM analysis coding specifications.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "5",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R\nanalysis/externals/ext_figure_km.R",
      "code_lines": "1-60\n1-80\n1-80\n61-111\n1-79"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,4,5],\"note\":\"Kept the Kaplan–Meier survival analysis scripts that compute or plot cumulative probability/incidence of long COVID by age group and sex, directly specifying or deriving from Kaplan–Meier estimates. Excluded files focused on Cox models, general survival summaries, or pipeline definitions without explicit KM test implementation for this analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "5",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: summarise /explore  survival data\n# Output:  summary of survival data\n\nlibrary(prodlim);library(readr); library(dplyr); library(survival)\n\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\n\ndata <- read_rds(\"output/input_stage1_all.rds\")\ndata <- data %>% dplyr::select(lcovid_surv, lcovid_cens)\nattach(data)\n\n# Summarise follow-up, reverse the event indicator to summarise follow-up\nfup <- quantile(prodlim(Hist(lcovid_surv,lcovid_cens)~1,reverse=TRUE))\na <- fup[1]$quantiles.survival[3,] # median follow-up time\n\n# Median time to long covid diagnosis\nsurv_time <- quantile(prodlim(Hist(lcovid_surv,lcovid_cens)~1,reverse=FALSE))\nb <- surv_time[1]$quantiles.survival[3,] # median survival time\n\nresults <- rbind(a,b)\n# return NA if not observing 50% people experiencing the event of interest by the end of the follow-up time\n\nrownames(results) <- c(\"median follow-up\", \"median survival time\")\n#colnames(results) <- c(\"quantile\")\nwrite.csv(results, file=\"output/review/descriptives/summarise_survival_data.csv\")",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R\nanalysis/stage2_summarise_survival_data.R",
      "code_lines": "1-60\n1-80\n1-80\n61-111\n1-27"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,6,5],\"note\":\"Kept Kaplan–Meier survival scripts and related survival-summary code that specify the time-to-event (lcovid_surv), censoring (lcovid_cens), and stratification variables (age group, sex) used to estimate cumulative probability of coded long COVID; excluded Cox model and other unrelated utilities.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "5",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  # ggsave(file= \"km_plot.png\",\n  #        path = paste0(output_dir, \"figures\"),\n  #        plot=p2, width=10, height=8)\n  dev.off()\n# }\n  \n  sub_dat <- dat_age %>% filter(cov_cat_age_group == \"40-59\")\n  index_m <- which(sub_dat$sex == \"Male\")\n  index_f <- which(sub_dat$sex == \"Female\")\n  \n  summary(sub_dat$y_prob[index_m])\n  summary(sub_dat$y_prob[index_f])\n  \n# cohort = \"all\"\n# km_plot(results_dir, output_dir, cohort)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/externals/ext_figure_km_v2.R\nanalysis/externals/ext_figure_km.R",
      "code_lines": "1-60\n1-80\n1-80\n61-92\n1-79"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,7,8],\"note\":\"These snippets implement the Kaplan–Meier estimation and cumulative probability/incidence calculations for long COVID stratified by age group and sex, and set key parameters (age group categorization, sex stratification, KM options, probability vs incidence scale). Snippet 7 additionally extracts and summarizes cumulative probabilities for the 40–59 age group by sex, directly underlying the reported 1.3% and 0.9% values. Snippet 8 and 2/1/0 handle KM-derived datasets and plotting with specified parameterizations; other snippets relate to different endpoints or non-KM analyses and are not directly relevant to this Kaplan–Meier parameter specification.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "5",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n\"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability.svg\", width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier_cohorts.R",
      "code_lines": "1-60\n1-80\n1-80\n61-111\n61-123"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,3,9],\"note\":\"Kept Kaplan–Meier survival analysis scripts that estimate cumulative probability/incidence of long COVID by age group and sex, directly implementing the KM-based inference described in the analysis. Other snippets were less directly tied to this specific KM stratified analysis or focused on different outcomes or pipeline wiring.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "5",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,\"output/review/descriptives/tbl_km_dat_age.csv\", row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier.R\nanalysis/figure_kaplan_meier_cohorts.R",
      "code_lines": "1-60\n1-80\n1-80\n61-123"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,7],\"note\":\"Indices 0, 1, 2, and 7 contain substantive executable R code that implements Kaplan–Meier survival analyses of time to first coded long COVID, stratified by age group and sex. They explicitly define the coding of age-group and sex variables (factor levels, groupings such as 18–39, 40–59, 60–79, 80+/80–105; sex coded as M/F then relabeled Female/Male) and apply Surv(lcovid_surv, lcovid_cens) in KM fits and plotting. This directly matches the requested analysis (KM cumulative probability of first long COVID code by age/sex in the post-COVID diagnosis cohort) and the Coding Specification dimension. Other snippets are more focused on Cox modeling, general survival summarization, or unrelated tasks and are less directly tied to the KM age/sex coding specification.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "6",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-10-31\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n#results_dir = paste0(common_dir, \"2022-10-10/\")\n#output_dir <- paste0(common_dir, \"2022-10-10/\")\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n#-------------------------------------------------------------------------------------------------------------------\n#- read in data\nHR <- read.csv(paste0(results_dir, \"HR_combined.csv\"))\n\n#- separate out data - full model with age splines\nHR_full_model_age_spline_all.csv <- HR[HR$file ==\"full_model_age_spline_all\",]                # primary\nHR_full_model_age_spline_all_vax_c.csv <- HR[HR$file ==\"full_model_age_spline_all_vax_c\",]    # pre-vax\nHR_full_model_age_spline_vaccinated.csv <- HR[HR$file ==\"full_model_age_spline_vaccinated\",]  # post-vax                                                # post-vax\nHR_full_model_age_spline_infected.csv <- HR[HR$file ==\"full_model_age_spline_infected\",]      # post-covid\n\nfile_name1 <- c(\"HR_full_model_age_spline_all.csv\", \"HR_full_model_age_spline_all_vax_c.csv\",\n                \"HR_full_model_age_spline_vaccinated.csv\", \"HR_full_model_age_spline_infected.csv\")\n\n#- separate out data -  full model with age categorical\nHR_full_model_categorical_all.csv <- HR[HR$file ==\"full_model_categorical_all\",]                # primary\nHR_full_model_categorical_all_vax_c.csv <- HR[HR$file ==\"full_model_categorical_all_vax_c\",]    # pre-vax\nHR_full_model_categorical_vaccinated.csv <- HR[HR$file ==\"full_model_categorical_vaccinated\",]  # post-vax                                                # post-vax\nHR_full_model_categorical_infected.csv <- HR[HR$file ==\"full_model_categorical_infected\",]      # post-covid\n\nfile_name2 <- c(\"HR_full_model_categorical_all.csv\", \"HR_full_model_categorical_all_vax_c.csv\",\n                \"HR_full_model_categorical_vaccinated.csv\", \"HR_full_model_categorical_infected.csv\")\n\n#- separate out data - age-sex adjusted model\nHR_age_sex_adjusted_all.csv        <- HR[HR$file ==\"age_sex_adjusted_all\",]                # primary\nHR_age_sex_adjusted_all_vax_c.csv  <- HR[HR$file ==\"age_sex_adjusted_all_vax_c\",]          # pre-vax\nHR_age_sex_adjusted_vaccinated.csv <- HR[HR$file ==\"age_sex_adjusted_vaccinated\",]     # post-vax\nHR_age_sex_adjusted_infected.csv   <- HR[HR$file ==\"age_sex_adjusted_infected\",]       # post-covid\n\nfile_name3 <- c(\"HR_age_sex_adjusted_all.csv\", \"HR_age_sex_adjusted_all_vax_c.csv\",\n                \"HR_age_sex_adjusted_vaccinated.csv\", \"HR_age_sex_adjusted_infected.csv\")\n\n# - separate out data - age-sex model with age splines\nHR_age_sex_model_all.csv <- HR[HR$file ==\"age_sex_model_all\",]                    # primary\nHR_age_sex_model_all_vax_c.csv  <- HR[HR$file ==\"age_sex_model_all_vax_c\",]       # pre-vax\nHR_age_sex_model_vaccinated.csv <- HR[HR$file ==\"age_sex_model_vaccinated\",]      # post-vax\nHR_age_sex_model_infected.csv   <- HR[HR$file ==\"age_sex_model_infected\",]        # post-covid\n\nfile_name4 <- c(\"HR_age_sex_model_all.csv\", \"HR_age_sex_model_all_vax_c.csv\",\n                \"HR_age_sex_model_vaccinated.csv\", \"HR_age_sex_model_infected.csv\")\n\n# - separate out data - age-sex model with categorical age\nHR_age_sex_model_categorical_all.csv    <- HR[HR$file ==\"age_sex_model_categorical_all\",]                 # primary\nHR_age_sex_model_categorical_all_vax_c.csv  <- HR[HR$file ==\"age_sex_model_categorical_all_vax_c\",]           # pre-vax\nHR_age_sex_model_categorical_vaccinated.csv <- HR[HR$file ==\"age_sex_model_categorical_vaccinated\",]      # post-vax\nHR_age_sex_model_categorical_infected.csv   <- HR[HR$file ==\"age_sex_model_categorical_infected\",]        # post-covid\n\nfile_name5 <- c(\"HR_age_sex_categorical_all.csv\", \"HR_age_sex_categorical_all_vax_c.csv\",\n                \"HR_age_sex_categorical_vaccinated.csv\", \"HR_age_sex_categorical_infected.csv\")\n\nfile_list <- c(file_name1, file_name2, file_name3, file_name4, file_name5)\n\n\n#----------------------------------------------------------------------------------------------------------------------\n\ndf_list_full <- list(# full model with age splines\n  HR_full_model_age_spline_all.csv,         # primary\n  HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n  HR_full_model_age_spline_vaccinated.csv,  # post-vax\n  HR_full_model_age_spline_infected.csv     # post-covid\n)\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv     # post-covid\n)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/stage3_model_development.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R\nanalysis/stage3_fatigue_survival_analysis.R",
      "code_lines": "1-80\n1-80\n1-63\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,4,6,8,9],\"note\":\"Kept snippets that explicitly define or fit Cox proportional hazards models or organize hazard ratio outputs (age–sex adjusted vs fully adjusted, spline vs categorical age), which directly document the test specification underlying the hazard ratio estimates described in the analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "6",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-78\n1-60\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[1,3,7,8,9],\"note\":\"Kept snippets that define or use analysis variables explicitly in survival/Cox models or related hazard ratio outputs for long COVID. Index 1 specifies outcome and covariate structure (including age–sex and full models). Index 3 and 9 define and transform key variables (including outcome, covariate prefixes, and derived phenotypes) used later in models. Index 8 uses the long COVID survival outcome and age/sex covariates in a Kaplan–Meier framework, clarifying variable roles. Index 7 processes hazard ratio outputs by decomposing covariate terms, reinforcing the covariate naming scheme and predictor roles. Other snippets were more focused on plotting, workflow orchestration, or different outcomes (e.g., fatigue) and were less directly informative for variable specification of the requested analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "6",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/stage3_model_development.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R",
      "code_lines": "1-80\n1-60\n1-63\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,5,6,7,9],\"note\":\"Kept snippets that define or operate on Cox/survival model specifications and hazard-ratio model types for long COVID: (2) constructs Cox survival formulas specifying age handling (spline vs linear), covariates, and stratification; (5) defines the Surv() object and KM setup for long COVID by age and sex; (6) fits Cox models (age spline vs linear, selected model) and outputs HRs/AIC; (7) and (9) organize HR outputs for fully adjusted vs age–sex adjusted models and different age parameterizations, corresponding to the age–sex adjusted and fully adjusted hazard-ratio analyses used in the figures.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "6",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_fatigue_survival_analysis.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R\nanalysis/stage3_model_development.R\nanalysis/dated code/figure_cum_prob_km.R",
      "code_lines": "1-80\n1-80\n1-80\n1-63\n1-60"
    },
    "llm_raw": "{\"keep_indices\":[2,4,6,8,9],\"note\":\"Kept snippets that define/fit Cox models or KM survival objects and thus directly implement the study’s inferential procedures (hazard ratios with CIs, modeling choices, and inverse probability weighting). Index 2 specifies the Cox model formulas (age–sex and fully adjusted), index 4 shows the sampling and inverse probability weighting used to handle case–control sampling (key to the stated inference approach), index 6 assembles age–sex adjusted and fully adjusted hazard ratio outputs used in the figures, index 8 fits Cox models and outputs hazard ratios and 95% CIs (and compares models via AIC), and index 9 constructs Kaplan–Meier estimates with a specified CI type (conf.type='log'). Other snippets are primarily plotting or workflow orchestration and are less central to the inference specification dimension.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "6",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/stage0_data_cleaning.R\nanalysis/figure_kaplan_meier_cohorts.R",
      "code_lines": "1-80\n1-60\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[3,6,7,8],\"note\":\"Kept snippets that specify how variables are coded and entered into survival/KM analyses for long COVID. Index 3 defines the Cox model formulas, including coding of sex, age (restricted cubic spline), region (strata), and treatment contrasts, directly informing how hazard ratios are estimated. Index 7 is the main data-cleaning script that lays out the variable naming conventions (cov_cat_, cov_num_, sub_), index dates, COVID and long COVID indicators, and COVID severity categories, giving core coding specifications. Indices 6 and 8 define factor levels and groupings for age and sex used in Kaplan–Meier and cumulative probability plots (e.g., age groups 18–39, 40–59, 60–79, 80+ / 80–105 and recoding of sex), which are directly related to how demographic variables are categorized in the analyses and figures referenced in the description.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "7",
    "dimension": "Test Specification",
    "value": "## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model with age and sex as predictors\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n######\n# Part 1: Assess if non-linear term is needed for continuous age               #\n#         and redefine the survival analysis formula                           #\n################################################################################\n\n# Age Sex model - age as splines\nfit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_age_sex_model_splines_\", analysis,\".rds\"))\n# \n# # age as linear predictor\n# fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n#                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n# \n# # a crude comparison\n# if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n#   surv_formula = surv_formula_age_linear_sex\n#   #surv_formula_predictors = surv_formula_predictors_lp\n#   fit_cox_model <- fit_cox_model_linear\n#   print(\"Linear term is selected for age!\")\n# } else{\n#   surv_formula <- surv_formula_age_spl_sex\n#   fit_cox_model <- fit_cox_model_splines\n#   print(\"Restricted cubic splines is selected for age!\")\n# }\n# print(\"The selected model is\")\n# print(fit_cox_model)\n\n# # save model as rds file\n# readr::write_rds(\n#   fit_cox_model,\n#   paste0(\"output/not_for_review/model/fit_cox_model_age_sex_\", analysis, \".rds\")\n#   #compress = \"gz\"\n# )\n\nprint(\"Part 1. Finished fitting age and sex models: age splines and age linear!\")\n\n################################################################################\n# Part 2: Fit models with categorical age                                       #\n################################################################################\n# Age sex model - age as categorical\nfit_cox_model_categorical <-rms::cph(formula= as.formula(surv_formula_age_cat_sex),\n                                data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nsaveRDS(fit_cox_model_categorical, file=paste0(\"output/not_for_review/model/fit_cox_model_age_sex_model_categorical_\", analysis,\".rds\"))\n\n# Full model - age as categorical\n\nfit_cox_model_full_categorical <-rms::cph(formula= as.formula(surv_formula_age_categorical),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\n\nsaveRDS(fit_cox_model_full_categorical, file=paste0(\"output/not_for_review/model/fit_cox_model_full_model_categorical_\", analysis,\".rds\"))\n\nprint(\"Part 2. Finished fitting Cox models with categorical age!\")\n\n################################################################################\n# Part 3: Output results from the Cox Model                                    #\n################################################################################\nwhich_model = \"model\"\noutput_file = paste0(\"output/review/model/HR_age_sex_\", which_model, \"_\", analysis)\ncox_output2(fit_cox_model, which_model, output_file, save_output = TRUE)\n\nwhich_model = \"model_categorical\"\noutput_file = paste0(\"output/review/model/HR_age_sex_\", which_model, \"_\", analysis)\ncox_output2(fit_cox_model_categorical, which_model, output_file, save_output = TRUE)\n\nwhich_model = \"full_model_categorical\" # this is a fully adjusted model, but put it in this script for convenience\noutput_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis) \ncox_output2(fit_cox_model_full_categorical, which_model, output_file, save_output = TRUE)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\"\n  # \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\"\n)\n\n## Full model\nsurv_formula <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\"\n)\n\n## full model: age is added as a linear predictor\nsurv_formula_lp <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+ cov_num_age\"\n)\n\nprint(\"Part 3: define survival analysis formula is completed!\")\n\nprint(\"End of stage3_model_input_setup.R\")\n\n################################################################################\n## Part 4: Fit Cox model with post-viral fatigue as an outcome                ##\n################################################################################\n## age sex model\n\nfit_age_sex_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = \"fatigue_age_sex_splines\"\noutput_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nfit_age_sex_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = \"fatigue_age_sex_linear\"\noutput_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_linear, which_model, output_file, save_output = TRUE)\n\n## full model\nfit_full_cox_model_splines <-rms::cph(formula= as.formula(surv_formula),\n                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nwhich_model = \"fatigue_full_splines\"\noutput_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nfit_full_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_lp),\n                                data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = \"fatigue_full_linear\"\noutput_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\n## model selection",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage3_4_model_dev_eval_age_sex_adjusted.R\nanalysis/stage3_model_development.R\nanalysis/stage3_4_model_dev_eval_age_sex.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_fatigue_survival_analysis.R",
      "code_lines": "1-80\n1-63\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\": [1,2,4,7,8], \"note\": \"Selected snippets that implement Cox proportional hazards models (rms::cph) with age as restricted cubic spline or categories and other covariates, directly corresponding to the paper’s fully adjusted and age–sex adjusted hazard ratio analyses for demographic predictors. Excluded snippets that only plot KM curves or manipulate results without specifying the core survival model.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "7",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates\n\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n}\n\n## handling variables\n\n## remove region as a covariate\n## input <- input %>% rename(sub_cat_region = \"cov_cat_region\")  %>%\n##   rename(sub_cat_age_group = \"cov_cat_age_group\")\n\ninput <- input %>% \n  rename(sub_cat_age_group = \"cov_cat_age_group\")\n\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"lcovid_surv\", \"lcovid_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_age_group\") \n\nif(analysis == \"all_vax_td\"){\n  variables_to_keep <- c(variables_to_keep, \"vax2_surv\")\n}\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\n## readr::write_rds(input, paste0(\"output/input_samples_\", analysis, \".rds\")) \n\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate \nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model - age spline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\"\n)\n\n## Age sex model - age linear\nsurv_formula_age_linear_sex <- paste0(",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage3_4_model_dev_eval_age_sex_adjusted.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n61-140\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[1,3,6,8,9],\"note\":\"Kept snippets that directly specify variables for the long COVID Cox models, including outcome (lcovid_surv/lcovid_cens), demographic covariates (sex, age as spline and age groups, region), cohort definitions, and the age- and sex-adjusted modeling framework. Discarded fatigue-focused or generic model-development snippets that did not clearly pertain to the long COVID variable setup.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "7",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_model_development.R\nanalysis/stage3_4_model_dev_eval_age_sex_adjusted.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n1-63\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[1,2,3,7],\"note\":\"Kept snippets that define or apply the Cox model parameterization for the long COVID analyses: the main long COVID Cox model and spline specification (index 1), model development and weighting/AIC-based comparison of spline vs linear age (index 2), age- and sex-adjusted Cox models with additional covariates (index 3), and the cohort eligibility and age-group categorisation (18–39, 40–59, 60–79, 80–105) used as parameter specifications for demographic variables (index 7). Other snippets focus on fatigue or plotting and are less directly relevant to the specified long COVID parameterization.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "7",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage3_model_development.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_4_model_dev_eval_age_sex_adjusted.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-63\n1-80\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,4,7,9],\"note\":\"Kept snippets that implement the Cox proportional hazards modeling and output routines for the long COVID analyses, including hazard ratios with 95% confidence intervals and the inverse-probability-weighted sampling scheme, which together define the practical inference procedures (estimation and uncertainty quantification) for the primary Cox models. Discarded snippets that are primarily plotting, cohort definition, or focused on other outcomes (e.g., fatigue) rather than the specified long COVID Cox inference.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "7",
    "dimension": "Coding Specification",
    "value": "## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\nhist(input_consultation_1e12,breaks=10, main=paste0(\"Histogram of \", i), xlab =i)\n      df_gp <- table(input[,i])\n      df_gp <- cbind(df_gp,prop.table(df_gp))\n      df_gp <- data.frame(df_gp)\n      df_gp[,2] <- redactor2(df_gp[,2])\n      df_gp[which(is.na(df_gp[,2])),3]=NA\n      write.csv(df_gp, file= paste0(\"output/not_for_review/descriptives/table_gp_\", i,\"_\", cohort, \".csv\"))\n    }\n    dev.off()\n  }\n  ## cov_num_gp_patient_interaction\n  input <- input %>% mutate(cov_cat_gp_patient_interaction = ifelse(input$cov_num_gp_patient_interaction > 12,\n                                                             \"13 or more\",\n                                                            ifelse(input$cov_num_gp_patient_interaction >= 9,\n                                                             \"9 to 12\",\n                                                                ifelse(input$cov_num_gp_patient_interaction >=4,\n                                                                      \"4 to 8\",\n                                                                         ifelse(input$cov_num_gp_patient_interaction >=1,\n                                                                                          \"1 to 3\", \"0\")))))\n  table(input$cov_cat_gp_patient_interaction)\n  table(input$cov_num_gp_patient_interaction)\n  input <- input%>% rename(sub_num_gp_patient_interaction = cov_num_gp_patient_interaction) %>% # rename so it is not included in modelling but only for exploration\n    mutate(cov_cat_gp_patient_interaction = as.factor(cov_cat_gp_patient_interaction)) %>%\n    mutate(cov_cat_gp_patient_interaction = relevel(cov_cat_gp_patient_interaction, ref = \"0\"))\n\n  ################################################################################\n  ## Part 3. define variable types: factor or numerical                          #\n  ################################################################################\n  \n  ## For categorical factors, specify the most frequently occurred level as the reference group\n  cat_factors <- colnames(input)[grepl(\"_cat_\",colnames(input))]\n  input[,cat_factors] <- lapply(input[,cat_factors], function(x) factor(x, ordered = FALSE))\n  \n  calculate_mode <- function(x) {\n    uniqx <- unique(na.omit(x))\n    uniqx[which.max(tabulate(match(x, uniqx)))]\n  }\n  \n  input[,cat_factors] <- lapply(input[,cat_factors], \n                                function(x){\n                                  x <- relevel(x, ref = as.character(calculate_mode(x)))\n                                  x\n                                  }\n                                )\n  \n  ## cov_cat_imd by quintile------------------------------------------------------\n  ## RK - do you get any missing deprivations in your real data? I'm not sure it's something we've included\n  ## YW responses - Yes, I do have some missing deprivations in the real data, and they are included as a catgory.\n  ## in other projects as it shoudln't be missing I don't think. Is this just a dummy data thing?\n  table(input$cov_cat_imd)\n  levels(input$cov_cat_imd)[levels(input$cov_cat_imd)==0] <-\"0 (missing)\"\n  levels(input$cov_cat_imd)[levels(input$cov_cat_imd)==1] <-\"1 (most deprived)\"\n  levels(input$cov_cat_imd)[levels(input$cov_cat_imd)==2] <-\"2\"\n  levels(input$cov_cat_imd)[levels(input$cov_cat_imd)==3] <-\"3\"\n  levels(input$cov_cat_imd)[levels(input$cov_cat_imd)==4] <-\"4\"\n  levels(input$cov_cat_imd)[levels(input$cov_cat_imd)==5] <-\"5 (least deprived)\"\n  \n  ## ordered categorical factor, the first level is the reference\n  input$cov_cat_imd <- ordered(input$cov_cat_imd, \n                               levels = c(\"1 (most deprived)\",\"2\",\"3\",\"4\",\"5 (least deprived)\", \"0 (missing)\"))\n  \n  input$cov_cat_bmi<- ordered(input$cov_cat_bmi, \n                              levels = c(\"Not obese\",\"Obese I (30-34.9)\",\n                                          \"Obese II (35-39.9)\", \"Obese III (40+)\"))\n  \n  input$cov_cat_multimorbidity <- ordered(input$cov_cat_multimorbidity, \n                                          levels = c(\"0 (no disease)\", \"1 (one disease)\", \"2 (two or more diseases)\"))\n  input <- input%>%rename(sub_cat_multimorbidity = cov_cat_multimorbidity)   # to keep this for exploration but not in the model \n  ## cov_cat_smoking_status-------------------------------------------------------\n  table(input$cov_cat_smoking_status)\n  input <- input %>% mutate(cov_cat_smoking_status = recode(cov_cat_smoking_status, \"N\" = \"Never smoker\",\n                                                            \"S\" = \"Current smoker\",\n                                                            \"E\" = \"Ever smoker\",\n                                                            \"M\" = \"Missing\"))\n  input$cov_cat_smoking_status <- ordered(input$cov_cat_smoking_status, levels = c(\"Never smoker\",\"Ever smoker\",\"Current smoker\",\"Missing\"))\n  table(input$cov_cat_smoking_status)\n  \n  ## cov_cat_sex -----------------------------------------------------------------\n  ## set Male as the reference level for sex\n  input$cov_cat_sex <- relevel(input$cov_cat_sex, ref = \"M\")\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model with age and sex as predictors\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n######\n# Part 1: Assess if non-linear term is needed for continuous age               #\n#         and redefine the survival analysis formula                           #\n################################################################################\n\n# Age Sex model - age as splines\nfit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_age_sex_model_splines_\", analysis,\".rds\"))\n# \n# # age as linear predictor\n# fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n#                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n# \n# # a crude comparison\n# if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n#   surv_formula = surv_formula_age_linear_sex\n#   #surv_formula_predictors = surv_formula_predictors_lp\n#   fit_cox_model <- fit_cox_model_linear\n#   print(\"Linear term is selected for age!\")\n# } else{\n#   surv_formula <- surv_formula_age_spl_sex\n#   fit_cox_model <- fit_cox_model_splines\n#   print(\"Restricted cubic splines is selected for age!\")\n# }\n# print(\"The selected model is\")\n# print(fit_cox_model)\n\n# # save model as rds file\n# readr::write_rds(\n#   fit_cox_model,\n#   paste0(\"output/not_for_review/model/fit_cox_model_age_sex_\", analysis, \".rds\")\n#   #compress = \"gz\"\n# )\n\nprint(\"Part 1. Finished fitting age and sex models: age splines and age linear!\")\n\n################################################################################\n# Part 2: Fit models with categorical age                                       #\n################################################################################\n# Age sex model - age as categorical\nfit_cox_model_categorical <-rms::cph(formula= as.formula(surv_formula_age_cat_sex),\n                                data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nsaveRDS(fit_cox_model_categorical, file=paste0(\"output/not_for_review/model/fit_cox_model_age_sex_model_categorical_\", analysis,\".rds\"))\n\n# Full model - age as categorical\n\nfit_cox_model_full_categorical <-rms::cph(formula= as.formula(surv_formula_age_categorical),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\n\nsaveRDS(fit_cox_model_full_categorical, file=paste0(\"output/not_for_review/model/fit_cox_model_full_model_categorical_\", analysis,\".rds\"))\n\nprint(\"Part 2. Finished fitting Cox models with categorical age!\")\n\n################################################################################\n# Part 3: Output results from the Cox Model                                    #\n################################################################################\nwhich_model = \"model\"\noutput_file = paste0(\"output/review/model/HR_age_sex_\", which_model, \"_\", analysis)\ncox_output2(fit_cox_model, which_model, output_file, save_output = TRUE)\n\nwhich_model = \"model_categorical\"\noutput_file = paste0(\"output/review/model/HR_age_sex_\", which_model, \"_\", analysis)\ncox_output2(fit_cox_model_categorical, which_model, output_file, save_output = TRUE)\n\nwhich_model = \"full_model_categorical\" # this is a fully adjusted model, but put it in this script for convenience\noutput_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis) \ncox_output2(fit_cox_model_full_categorical, which_model, output_file, save_output = TRUE)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage1_define_eligible_population.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_4_model_dev_eval_age_sex.R",
      "code_lines": "61-140\n181-260\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,3,5,7],\"note\":\"Indices 0 and 3 contain explicit coding of key predictors (age groups, IMD quintiles, BMI, smoking, sex, GP interaction) via factor recoding, ordering, and reference-level selection, which are central to the Coding Specification dimension. Index 5 specifies how age is handled in the Cox models (restricted cubic spline vs linear), includes the contrast coding option (contr.treatment), and defines the fully adjusted multivariable Cox formulas. Index 7 shows implementation of Cox models using age as spline and as categorical (age-sex and fully adjusted models), directly reflecting the alternative codings of age described in the analysis. All four snippets contain substantive, executable R code directly relevant to variable coding in the Cox analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "8",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/create_project_actions.R\nanalysis/stage3_model_development.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R",
      "code_lines": "1-80\n481-560\n1-63\n1-78\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,3,4,7,9],\"note\":\"Kept snippets that define and implement the Cox proportional hazards models and hazard-ratio extraction/plotting, including age–sex and fully adjusted models (test specification). Discarded primarily plotting-only KM code and fatigue-specific scripts as less directly relevant to the requested long COVID Cox HR analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "8",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/create_project_actions.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R",
      "code_lines": "1-80\n481-560\n1-80\n1-60\n1-78"
    },
    "llm_raw": "{\"keep_indices\":[0,5,6,7,8],\"note\":\"Kept snippets that define or operate on the key analysis variables (outcome and covariates) for Cox/survival models and hazard ratios. (0) Specifies Cox model survival formulas including age–sex and full models using covariate variables (cov_*), directly reflecting variable roles. (5) Orchestrates modeling actions including development Cox models and age–sex adjusted Cox models, indicating how variable sets are used in different model variants. (6) Performs data cleaning and construction of analysis variables (prefixes cov_cat_, cov_num_, out_, sub_), including COVID infection and cohort index dates, which is core to variable specification. (7) Uses long COVID survival outcome variables (lcovid_surv, lcovid_cens) and demographic covariates (cov_cat_sex, cov_cat_age_group) in Kaplan–Meier analyses, clarifying coding and roles of age and sex. (8) Processes hazard ratio outputs by parsing predictor terms (cov_cat_*, cov_num_*) into predictor names and levels, reinforcing the variable naming scheme and predictor structure in the hazard models. Other snippets were mainly plotting or non-long-COVID outcomes, or lacked direct connection to the requested variable-specification dimension for the Cox models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "8",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_model_development.R\nanalysis/create_project_actions.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "1-80\n1-63\n481-560\n121-200\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,6,7,8,9],\"note\":\"Kept snippets that define or operationalize the Cox model parameterization for long COVID analyses: (i) explicit survival formulas and spline knot placement (index 2), (ii) model fitting choices distinguishing age-spline vs linear age and use of weights with cph (index 6), (iii) project actions that set up separate age–sex and age–sex–adjusted Cox model workflows (index 7), and (iv) scripts that enumerate and organize outputs from fully adjusted vs age–sex–adjusted models across cohorts, including the post-COVID (infected) cohort (indices 8 and 9). Other snippets focus mainly on KM plotting or fatigue-specific models and are less directly informative for parameter specification of the requested long COVID Cox analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "8",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_model_development.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R\nanalysis/dated code/figure_cum_prob_km.R",
      "code_lines": "1-80\n1-63\n1-80\n1-80\n1-60"
    },
    "llm_raw": "{\"keep_indices\":[2,6,7,8,9],\"note\":\"Kept snippets that define or use Cox models and hazard ratio outputs (including age–sex adjusted vs fully adjusted) and/or specify inference-related settings like confidence interval type for survival estimates. These are directly relevant to how hazard ratios and cumulative probabilities are estimated and summarized for inference, including the post-COVID diagnosis cohort and Figure 1 comparisons.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "8",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/for_future_development/testing_stage5_validation.R\nanalysis/stage3_fatigue_survival_analysis.R\nanalysis/stage0_data_cleaning.R\nanalysis/figure_kaplan_meier_cohorts.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[3,4,5,7,9],\"note\":\"Kept snippets that define how variables are coded or modeled in the Cox analyses (e.g., factor vs numeric coding, age splines vs categories, sex coding, contrast options, and cohort/variable naming conventions). Discarded snippets that only produce plots or tables without specifying variable coding for the models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "9",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n\"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+ cov_num_age\", \n  \"+ strat(sub_cat_region)\"\n)\n\nprint(\"Part 3: define survival analysis formula is completed!\")\n\n################################################################################\n# Part 4: Fit Cox model with long covid code as an outcome                  #\n################################################################################\n# age sex model, age splines\n\nfit_age_sex_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                         data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age splines, completed successfully!\")\n\n# age sex cox model, age linear\nfit_age_sex_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n                                        data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age linear, completed successfully!\")\n\n# full model, age splines\nfit_full_cox_model_splines <-rms::cph(formula= as.formula(surv_formula),\n                                      data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nwhich_model = paste0(outcome, \"_full_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age splines, completed successfully!\")\n\n# full model, age linear\nfit_full_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_lp),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_full_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age linear, completed successfully!\")\n# model selection\n\nfit_cox_model_selected <- fastbw(fit_full_cox_model_splines, sls=0.20)\n\nwhich_model = paste0(outcome, \"_selected_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"selected model:\")\nfit_cox_model_selected$names.kept\n\nselected_covariate_names <- fit_cox_model_selected$names.kept\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_fatigue_survival_analysis.R\nanalysis/stage3_model_development.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_4_model_dev_eval_age_sex_adjusted.R",
      "code_lines": "1-80\n1-80\n1-63\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,2,5,7,8],\"note\":\"Kept snippets that explicitly define and/or fit Cox proportional hazards models (including Surv() specifications, restricted cubic splines, and hazard ratio outputs), which directly document the statistical test specification for the long COVID time-to-event analyses (and closely related fatigue survival analysis).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "9",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Count and pie chart of snomed code for long COVID diagnosis\n## Output:  suppl_table_1.csv, suppl_table_1.html, suppl_figure_pie.svg\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\n## Read in data and identify factor variables and numerical variables------------\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n## keep only observations where long covid indicator is 1\ninput <- input %>% filter(lcovid_cens == 1)\n\n## computational efficiency: only keep the needed variable\ninput <- input %>% dplyr::select(\"out_first_long_covid_code\")\n\nsnomed_code <- input$out_first_long_covid_code\n\ncount_data <-table(snomed_code)\n\ncount_data <- data.frame(count_data)\n\n\nnames(count_data) <- c(\"snomed_code\", \"count\")\ncount_data\ncount_data$percent = round(count_data$count / sum(count_data$count),3)\ncount_data\npercent_function <- function(x, digits = 1, format = \"f\", ...) {\n  paste0(formatC(100 * x, format = format, digits = digits, ...), \"%\")\n}\n\ncount_data$labels = percent_function(count_data$percent)\n\ncount_data$count <- redactor2(count_data$count)\nindex = which(is.na(count_data$count))\ncol_names <- c(\"count\",\"percent\",\"labels\")\ncount_data[index,col_names]= NA\n\n## use redactor for small number suppression\n## index <- which(count_data$count < 6)\n## count_data$count[index] = count_data$percent[index] = count_data$labels[index] = NA\n\ncount_data_active = count_data%>%filter(count>5)\n## Pie Chart\nsuppl_figure_pie <- ggplot(count_data_active, aes(x = \"\", y = count, fill = snomed_code)) +\n  geom_bar(width = 1, stat = \"identity\") +\n  coord_polar(theta = \"y\") +\n  labs(x = \"\", y = \"\", fill = \"SNOMED Code\") + \n  geom_text(aes(label = labels),\n            position = position_stack(vjust = 0.5)) +\n  theme(plot.title = element_text(hjust = 0.5), \n        legend.title = element_text(hjust = 0.5, face=\"bold\", size = 10), \n        axis.ticks = element_blank(), \n        axis.text.y = element_blank(),\n        axis.text.x = element_blank()) \n\nsuppl_figure_pie\n\n## supplementary figure - pie chart\nggsave(file=\"output/not_for_review/descriptives/snomed_code_pie.svg\", plot=suppl_figure_pie, width=16, height=8)\n\n## output underlying count data for supplementary figure - pie chart\n\n## small number suppression - indicate NA as redacted\ncount_data[which(is.na(count_data$count)),col_names]=\"[redacted]\" \n\nwrite.csv(count_data, file=\"output/not_for_review/descriptives/snomed_code_table.csv\")",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/table_snomed_code.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80\n1-74"
    },
    "llm_raw": "{\"keep_indices\":[0,3,4,5,7],\"note\":\"Kept snippets that define or operationalize variables for the long COVID Cox models: (0) specifies outcome and covariates in Cox formulas (including age spline and stratification by region); (3) establishes variable naming conventions and begins construction of key exposure/severity variables from raw data; (4) completes COVID-19 severity categorization (no infection, non-hospitalised, hospitalised) and performs key variable drops/reorganisation; (5) sets up model input for different analyses including the infected (post-COVID diagnosis) cohort and defines which survival/censoring variables are used; (7) uses the long COVID SNOMED code variable to characterize outcome coding, tying the outcome to specific diagnostic codes. Other snippets were more about fatigue models, plotting, workflow orchestration, or generic model fitting without further variable specification detail.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "9",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_model_development.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-63\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,5,7,9],\"note\":\"Kept snippets that specify key modeling parameters for the Cox proportional hazards analyses: (0) defines covariate set and survival formulas including restricted cubic splines for age and region stratification; (5) fits Cox models with age as spline vs linear and uses AIC for model comparison; (7) sets up analysis cohorts, sampling ratio of non-cases to cases, inverse probability weights, and time-origin/censoring choices; (9) performs core data preparation including index date definitions and explicit parameterization of COVID-19 severity (hospitalised vs non-hospitalised within 28 days), which is later used as a covariate. Other snippets were excluded as they focused mainly on plotting, alternative outcomes (fatigue), or general utilities rather than parameter specification for the main long COVID Cox models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "9",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage3_model_development.R",
      "code_lines": "1-63"
    },
    "llm_raw": "{\"keep_indices\":[5],\"note\":\"Only index 5 explicitly concerns the inferential outputs of the Cox model (hazard ratios and 95% confidence intervals) for the long COVID risk factor analysis, matching the Inference Specification dimension. Other snippets focus on data setup, model formula construction, weighting, or plotting without specifying inference criteria such as CI level or p-value thresholds.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "9",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# 01 June 2022\n# background: some exploration on the possibility to create more refined categories for the following covariates:\n# cancer(non-haematological), haematological cancer, asthma, diabetes and reduced kidney function\n\n\n# pre_cov_num_creatinine=patients.with_these_clinical_events(\n#     creatinine_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-02-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 60.0, \"stddev\": 15},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# pre_cov_dialysis_date=patients.with_these_clinical_events(\n#     dialysis_codes, return_first_date_in_period=True, include_month=True,\n# ),\n\n# pre_cov_hba1c_mmol_per_mol=patients.with_these_clinical_events(\n#     hba1c_new_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-12-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 40.0, \"stddev\": 20},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# pre_cov_hba1c_percentage=patients.with_these_clinical_events(\n#     hba1c_old_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-12-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 5, \"stddev\": 2},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# cov_cat_asthma=patients.satisfying(\n#     \"\"\"\n#         recent_asthma_code OR (\n#         asthma_code_ever AND NOT\n#         copd_code_ever\n#         )\n#     \"\"\",\n#     return_expectations={\n#         \"incidence\": 0.05,\n#    },\n#     recent_asthma_code=patients.with_these_clinical_events(\n#         asthma_codes,\n#         # between=[f\"{index_date_variable - 3 years}\",f\"{index_date_variable}\"],\n#         between=[f\"{index_date_variable_3y}\",f\"{index_date_variable}\"],\n#     ),\n#     asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#     copd_code_ever=patients.with_these_clinical_events(\n#         chronic_respiratory_disease_codes\n#     ),\n# ),\n\n# cov_cat_asthma=patients.categorised_as(\n#   {\n#     \"0\": \"DEFAULT\",\n#     \"1\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND (\n#                     prednisolone_last_year = 0 OR \n#                     prednisolone_last_year > 4\n#                     )\n#                 \"\"\",",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/dated_variables_in_study_definition.R",
      "code_lines": "1-80\n61-140\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,3,5,7],\"note\":\"Kept snippets that explicitly define or operationalize variable coding for the Cox models. Index 0 shows how covariates are selected and coded in the Cox formula (use of cov_ prefixes, treatment contrasts, spline for age, stratification). Indices 5 and 3 come from the core data-cleaning script and define naming conventions (cov_cat_, cov_num_, out_, sub_), binary indicators, and categorical coding for COVID-19 severity. Index 7 contains explicit category definitions for comorbidities (e.g., asthma) in the study definition, illustrating how clinical variables are coded into analysis-ready categorical variables. Other snippets focus more on workflow orchestration, plotting, or sampling/weights rather than on variable coding itself.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "10",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/create_project_actions.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n121-200\n1-80\n481-560\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,5,6,8,9],\"note\":\"Kept snippets that directly implement or orchestrate Cox proportional hazards modeling and extraction of hazard ratios, including age–sex adjusted vs fully adjusted models and cohort-specific setups (notably including the pre-vaccination cohort). These are central to the Test Specification dimension. Dropped Kaplan–Meier plotting and pure visualization code that do not define or run the statistical tests themselves.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "10",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,3,6],\"note\":\"Kept snippets that define or set up the variables used in the Cox models for long COVID, including naming conventions and roles (categorical vs numeric covariates), survival outcome and censoring variables, cohort-specific survival definitions (e.g., censoring at vaccination), and the predictor sets/formulas for age–sex and fully adjusted models. Discarded snippets focused mainly on plotting, pipeline orchestration, or different outcomes (fatigue) rather than direct variable specification for the long COVID Cox analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "10",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage2_model_input_set_up.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "1-80\n1-80\n1-80\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,3,4,8],\"note\":\"Indices 0, 3, 4, and 8 contain substantive executable code that directly specifies parameters of the Cox models and cohort definitions relevant to the pre-vaccination and post-vaccination analyses. Snippet 0 defines Cox model survival formulas, including an age–sex adjusted model (surv_formula_age_spl_sex / surv_formula_age_linear_sex) and the full model, with spline knots and regional stratification. Snippets 3 and 4 specify the analysis cohorts and censoring rules, including the pre-vaccination cohort implemented as censoring at first vaccination (all_vax_c), as well as inverse probability weighting and sampling ratios, which are key analysis parameters. Snippet 8 programmatically distinguishes fully adjusted versus age–sex adjusted hazard ratio outputs across cohorts, including the pre-vaccination cohort, and labels age–sex adjusted hazard ratios, reflecting the separate age–sex adjusted Cox models described in the analysis. Other snippets focus mainly on plotting, generic data cleaning, or non-long-COVID outcomes and are less directly tied to the requested age–sex adjusted Cox parameter specification for the pre-vaccination cohort.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "10",
    "dimension": "Inference Specification",
    "value": "## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage2_model_input_set_up.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "1-80\n121-200\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[3,8,9],\"note\":\"Indices 3, 8, and 9 contain substantive executable R code directly related to the inference specification for the Cox models used to estimate hazard ratios. Script 3 (stage2_model_input_set_up.R) implements the case–noncase sampling and inverse probability weighting that underpin the hazard ratio estimation and robust inference. Scripts 8 and 9 (ext_figure_hazard_ratio_read_data_for_v12_plot.R and v11_plot.R) construct and combine the fully adjusted and age–sex adjusted hazard ratio tables (including CIs) across cohorts, explicitly handling the pre-vaccination cohort and producing the benchmark age–sex adjusted HRs used in Figure 2. Other snippets mainly handle plotting, pipeline wiring, or non-long-COVID outcomes and are less directly tied to the requested analysis–dimension (Inference Specification).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "10",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/for_future_development/testing_stage5_validation.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,4,6,7,8],\"note\":\"Kept snippets that explicitly define how variables are coded or treated in the Cox models and related analyses. Index 0 and 6 specify contrasts for categorical variables (contr.treatment) and construct survival formulas using covariate naming conventions (cov_cat_ vs cov_num_), directly informing coding of predictors such as sex and age. Indices 4 and 8 (two parts of stage0_data_cleaning.R) give the core variable coding scheme, including naming conventions, creation of indicators and factors (e.g., COVID infection and phenotype), and dropping/renaming variables, which determine the categorical/continuous structure used later in the Cox models. Index 7 shows additional model input setup, including sampling with inverse probability weights and creation of factor variables (e.g., cov_cat_ie.status), further detailing variable coding in the modeling pipeline. Other snippets focus mainly on plotting, pipeline orchestration, or table assembly and are less directly about coding specification of analysis variables.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "11",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/stage3_model_development.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n121-200\n1-63\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,4,6,9],\"note\":\"Kept scripts that directly specify and fit Cox proportional hazards models (including age as restricted cubic spline/linear, full multivariable formulas, and model development) and the script that assembles hazard ratios from fully adjusted vs age–sex adjusted Cox models. Also kept the model input setup script implementing case sampling and inverse probability weighting, which operationalizes the described 1:20 sampling and IPW for the Cox analyses. Other snippets were mainly plotting or post-processing and less central to the test specification for the multivariable Cox models in the pre-vaccination cohort.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "11",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,3,5,7],\"note\":\"Kept snippets that directly specify variables and their roles in the Cox models: (0) defines outcome and covariate set (cov_*) plus age spline and region stratification in the survival formula; (3) establishes variable naming conventions (cov_cat_, cov_num_, out_, sub_) clarifying which variables are outcomes vs covariates vs subgroup descriptors; (5) sets up the model input, specifying the survival outcome variables (lcovid_surv, lcovid_cens) for different analysis cohorts; (7) defines key variable restrictions and transformations, especially the age variable and its categorical grouping (18–39, 40–59, 60–79, 80–105) used in the analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "11",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,4,5,7,9],\"note\":\"Kept snippets that explicitly define key analysis parameters for the long COVID Cox models: (0) specification of Cox survival formulas, including age as a restricted cubic spline and regional stratification; (4) main model input setup, cohort definitions, and inverse probability weighting with a 1:20 sampling ratio; (5) earlier long COVID survival data setup with similar IPW and sampling parameters (ratio 1:10) and cohort-type specifications; (7) eligibility criteria and demographic parameterization, including age range restrictions, age group categorization, and region factor reference level; (9) core study design parameters including index date, cohort end date, and rules for excluding implausible event dates. Other snippets were more focused on different outcomes, plotting, or downstream HR table assembly rather than primary parameter specification for the requested analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "11",
    "dimension": "Inference Specification",
    "value": "## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage3_fatigue_survival_analysis.R\nanalysis/for_future_development/testing_stage5_validation.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[1,3,4,5,6],\"note\":\"Kept snippets that implement the sampling and inverse probability weighting (indices 1, 3, 4, 5) and the handling of hazard ratios with 95% confidence intervals (index 6), all of which are directly relevant to the study’s inference procedures (IPW, robust-style handling via weights, and 95% CI-based reporting). Other snippets focus more on model/data setup or plotting rather than explicit inference specification.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "11",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage1_define_eligible_population.R\nanalysis/externals/ext_figure_km.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n61-140\n1-79\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,3,6,7,8],\"note\":\"Selected snippets that show explicit coding/recoding of variables and modeling contrasts: (0) specifies covariate selection by prefix, removes age variants, and sets treatment contrasts for Cox models with age as restricted cubic spline and region as a stratification factor; (3) recodes continuous age into categorical age groups and relevels region (factor reference), directly matching age-group hazard ratio reporting; (6) uses age-group categories and sex to structure Kaplan–Meier outputs, reflecting how categorical age/sex variables are encoded for analysis/visualization; (7) sets up model input with case–control weighting and defines a time-dependent vaccination status variable as a factor (cov_cat_ie.status), specifying coding for exposure; (8) defines systematic naming conventions for covariates and outcomes, constructs binary COVID infection indicators and multi-category COVID severity (phenotype) variables, and sets up cohort/index dates, all of which describe how key variables are coded for the analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "12",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\n\n\ntbl_hr_prevax_combined <- merge(tbl_hr_prevax, tbl_hr_as_prevax, by=\"row.num\", all.x=T)\n\n# Post-vaccination cohort\ncsv_index = 3\ncohort = \"Post-Vaccination\"\ndf_hr_as_vax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                          df_list_age_sex_categorical, csv_index,\n                                          cohort)\ntbl_hr_as_vax <- function_HR_df_v2(df_hr_as_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\ntbl_hr_as_vax$age_sex_aHR = tbl_hr_as_vax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_vax <- tbl_hr_as_vax %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R\nanalysis/dated code/figure_hazard_ratio_plot.R",
      "code_lines": "1-80\n121-200\n1-40"
    },
    "llm_raw": "{\"keep_indices\":[5,8,7],\"note\":\"Kept snippets that explicitly specify or operationalize the Cox proportional hazards models and hazard ratios (test type) used across cohorts, including age–sex adjusted and fully adjusted models, and their visualization. Other snippets focus on Kaplan–Meier plots or are near-duplicates of the hazard-ratio extraction logic and are less central to the Test Specification dimension.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "12",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "1-80\n121-200\n1-78\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,6],\"note\":\"Kept core Cox-model code defining outcome and predictor variables (0), code that constructs and labels hazard ratio tables for fully adjusted vs age–sex adjusted models across cohorts including post-vaccination (1,6), and code that parses hazard-ratio terms into predictor names/levels and distinguishes categorical vs numeric predictors (2). These together directly document which variables are used in the Cox analyses and their roles (outcome, predictors, adjustment) for the age–sex and fully adjusted models relevant to Figure 2 and the post-vaccination cohort.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "12",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n\"HR_age_sex_categorical_vaccinated.csv\", \"HR_age_sex_categorical_infected.csv\")\n\nfile_list <- c(file_name1, file_name2, file_name3, file_name4, file_name5)\n\n\n#----------------------------------------------------------------------------------------------------------------------\n\ndf_list_full <- list(# full model with age splines\n  HR_full_model_age_spline_all.csv,         # primary\n  HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n  HR_full_model_age_spline_vaccinated.csv,  # post-vax\n  HR_full_model_age_spline_infected.csv     # post-covid\n)\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv     # post-covid\n)\n\ndf_list_age_sex_adjusted <- list(\n  # age-sex adjusted model\n  HR_age_sex_adjusted_all.csv,        # primary\n  HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n  HR_age_sex_adjusted_vaccinated.csv, # post-vax\n  HR_age_sex_adjusted_infected.csv   # post-covid\n)\n\ndf_list_age_sex <-list(\n  # age-sex model with age splines\n  HR_age_sex_model_all.csv,        # primary\n  HR_age_sex_model_all_vax_c.csv,  # pre-vax\n  HR_age_sex_model_vaccinated.csv, # post-vax\n  HR_age_sex_model_infected.csv   # post-covid\n)\ndf_list_age_sex_categorical <- list(\n  # age-sex model with categorical age\n  HR_age_sex_model_categorical_all.csv,        # primary\n  HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n  HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n  HR_age_sex_model_categorical_infected.csv   # post-covid\n) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-10-31\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n#results_dir = paste0(common_dir, \"2022-10-10/\")\n#output_dir <- paste0(common_dir, \"2022-10-10/\")\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n#-------------------------------------------------------------------------------------------------------------------\n#- read in data\nHR <- read.csv(paste0(results_dir, \"HR_combined.csv\"))\n\n#- separate out data - full model with age splines\nHR_full_model_age_spline_all.csv <- HR[HR$file ==\"full_model_age_spline_all\",]                # primary\nHR_full_model_age_spline_all_vax_c.csv <- HR[HR$file ==\"full_model_age_spline_all_vax_c\",]    # pre-vax\nHR_full_model_age_spline_vaccinated.csv <- HR[HR$file ==\"full_model_age_spline_vaccinated\",]  # post-vax                                                # post-vax\nHR_full_model_age_spline_infected.csv <- HR[HR$file ==\"full_model_age_spline_infected\",]      # post-covid\n\nfile_name1 <- c(\"HR_full_model_age_spline_all.csv\", \"HR_full_model_age_spline_all_vax_c.csv\",\n                \"HR_full_model_age_spline_vaccinated.csv\", \"HR_full_model_age_spline_infected.csv\")\n\n#- separate out data -  full model with age categorical\nHR_full_model_categorical_all.csv <- HR[HR$file ==\"full_model_categorical_all\",]                # primary\nHR_full_model_categorical_all_vax_c.csv <- HR[HR$file ==\"full_model_categorical_all_vax_c\",]    # pre-vax\nHR_full_model_categorical_vaccinated.csv <- HR[HR$file ==\"full_model_categorical_vaccinated\",]  # post-vax                                                # post-vax\nHR_full_model_categorical_infected.csv <- HR[HR$file ==\"full_model_categorical_infected\",]      # post-covid\n\nfile_name2 <- c(\"HR_full_model_categorical_all.csv\", \"HR_full_model_categorical_all_vax_c.csv\",\n                \"HR_full_model_categorical_vaccinated.csv\", \"HR_full_model_categorical_infected.csv\")\n\n#- separate out data - age-sex adjusted model\nHR_age_sex_adjusted_all.csv        <- HR[HR$file ==\"age_sex_adjusted_all\",]                # primary\nHR_age_sex_adjusted_all_vax_c.csv  <- HR[HR$file ==\"age_sex_adjusted_all_vax_c\",]          # pre-vax\nHR_age_sex_adjusted_vaccinated.csv <- HR[HR$file ==\"age_sex_adjusted_vaccinated\",]     # post-vax\nHR_age_sex_adjusted_infected.csv   <- HR[HR$file ==\"age_sex_adjusted_infected\",]       # post-covid\n\nfile_name3 <- c(\"HR_age_sex_adjusted_all.csv\", \"HR_age_sex_adjusted_all_vax_c.csv\",\n                \"HR_age_sex_adjusted_vaccinated.csv\", \"HR_age_sex_adjusted_infected.csv\")\n\n# - separate out data - age-sex model with age splines\nHR_age_sex_model_all.csv <- HR[HR$file ==\"age_sex_model_all\",]                    # primary\nHR_age_sex_model_all_vax_c.csv  <- HR[HR$file ==\"age_sex_model_all_vax_c\",]       # pre-vax\nHR_age_sex_model_vaccinated.csv <- HR[HR$file ==\"age_sex_model_vaccinated\",]      # post-vax\nHR_age_sex_model_infected.csv   <- HR[HR$file ==\"age_sex_model_infected\",]        # post-covid\n\nfile_name4 <- c(\"HR_age_sex_model_all.csv\", \"HR_age_sex_model_all_vax_c.csv\",\n                \"HR_age_sex_model_vaccinated.csv\", \"HR_age_sex_model_infected.csv\")\n\n# - separate out data - age-sex model with categorical age\nHR_age_sex_model_categorical_all.csv    <- HR[HR$file ==\"age_sex_model_categorical_all\",]                 # primary\nHR_age_sex_model_categorical_all_vax_c.csv  <- HR[HR$file ==\"age_sex_model_categorical_all_vax_c\",]           # pre-vax\nHR_age_sex_model_categorical_vaccinated.csv <- HR[HR$file ==\"age_sex_model_categorical_vaccinated\",]      # post-vax\nHR_age_sex_model_categorical_infected.csv   <- HR[HR$file ==\"age_sex_model_categorical_infected\",]        # post-covid\n\nfile_name5 <- c(\"HR_age_sex_categorical_all.csv\", \"HR_age_sex_categorical_all_vax_c.csv\",\n                \"HR_age_sex_categorical_vaccinated.csv\", \"HR_age_sex_categorical_infected.csv\")\n\nfile_list <- c(file_name1, file_name2, file_name3, file_name4, file_name5)\n\n\n#----------------------------------------------------------------------------------------------------------------------\n\ndf_list_full <- list(# full model with age splines\n  HR_full_model_age_spline_all.csv,         # primary\n  HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n  HR_full_model_age_spline_vaccinated.csv,  # post-vax\n  HR_full_model_age_spline_infected.csv     # post-covid\n)\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv     # post-covid\n)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R",
      "code_lines": "1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,8,9],\"note\":\"Index 2 contains the core Cox model specification, including the age–sex model and full model formulas, spline knots for age, and stratification—this is directly about parameter specification for the Cox PH models used across cohorts (including pre- and post-vaccination via the 'analysis' argument). Indices 9 and 8 (two contiguous parts of the same script) define and separate the different model types (full model with age splines vs categorical age, age–sex adjusted models, and age–sex models with splines vs categorical age) and organise them by cohort (primary, pre-vaccination, post-vaccination, post-COVID). These filenames and lists encode the parameterisation decisions (age functional form, adjustment set type, cohort), and they are used to juxtapose age–sex adjusted and fully adjusted hazard ratios in the figures, directly matching the described analysis for the post-vaccination cohort. Other snippets focus mainly on plotting or generic HR plotting without adding new parameter-specification detail for the Cox models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "12",
    "dimension": "Inference Specification",
    "value": "################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR))\n\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% select(c(row.num, age_sex_aHR))\n\ntbl_hr_prevax_combined <- merge(tbl_hr_prevax, tbl_hr_as_prevax, by=\"row.num\", all.x=T)\n\n\"HR_age_sex_categorical_vaccinated.csv\", \"HR_age_sex_categorical_infected.csv\")\n\nfile_list <- c(file_name1, file_name2, file_name3, file_name4, file_name5)\n\n\n#----------------------------------------------------------------------------------------------------------------------\n\ndf_list_full <- list(# full model with age splines\n  HR_full_model_age_spline_all.csv,         # primary\n  HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n  HR_full_model_age_spline_vaccinated.csv,  # post-vax\n  HR_full_model_age_spline_infected.csv     # post-covid\n)\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv     # post-covid\n)\n\ndf_list_age_sex_adjusted <- list(\n  # age-sex adjusted model\n  HR_age_sex_adjusted_all.csv,        # primary\n  HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n  HR_age_sex_adjusted_vaccinated.csv, # post-vax\n  HR_age_sex_adjusted_infected.csv   # post-covid\n)\n\ndf_list_age_sex <-list(\n  # age-sex model with age splines\n  HR_age_sex_model_all.csv,        # primary\n  HR_age_sex_model_all_vax_c.csv,  # pre-vax\n  HR_age_sex_model_vaccinated.csv, # post-vax\n  HR_age_sex_model_infected.csv   # post-covid\n)\ndf_list_age_sex_categorical <- list(\n  # age-sex model with categorical age\n  HR_age_sex_model_categorical_all.csv,        # primary\n  HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n  HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n  HR_age_sex_model_categorical_infected.csv   # post-covid\n) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\n\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\n\n\ntbl_hr_prevax_combined <- merge(tbl_hr_prevax, tbl_hr_as_prevax, by=\"row.num\", all.x=T)\n\n# Post-vaccination cohort\ncsv_index = 3\ncohort = \"Post-Vaccination\"\ndf_hr_as_vax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                          df_list_age_sex_categorical, csv_index,\n                                          cohort)\ntbl_hr_as_vax <- function_HR_df_v2(df_hr_as_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\ntbl_hr_as_vax$age_sex_aHR = tbl_hr_as_vax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_vax <- tbl_hr_as_vax %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R",
      "code_lines": "121-200\n61-140\n61-140\n61-140\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[1,6,7,8,9],\"note\":\"Kept scripts that construct and combine fully adjusted and age–sex adjusted hazard ratios (including for the post-vaccination cohort) and explicitly work with HR and 95% CI fields, which are directly relevant to the inference specification (estimation and presentation of hazard ratios with confidence intervals).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "12",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R",
      "code_lines": "1-80"
    },
    "llm_raw": "{\"keep_indices\":[2],\"note\":\"Index 2 contains the Cox model specification, including how variables are represented in the formula (e.g., cov_cat_sex, cov_num_age, strat(sub_cat_region)) and the contrast options (contr.treatment), which directly relate to coding specification. Other snippets primarily handle reading results, plotting, or workflow orchestration and do not provide details on how variables are coded in the models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "13",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)\n\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\n\n\ntbl_hr_prevax_combined <- merge(tbl_hr_prevax, tbl_hr_as_prevax, by=\"row.num\", all.x=T)\n\n# Post-vaccination cohort\ncsv_index = 3\ncohort = \"Post-Vaccination\"\ndf_hr_as_vax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                          df_list_age_sex_categorical, csv_index,\n                                          cohort)\ntbl_hr_as_vax <- function_HR_df_v2(df_hr_as_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\ntbl_hr_as_vax$age_sex_aHR = tbl_hr_as_vax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_vax <- tbl_hr_as_vax %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R\nanalysis/stage3_model_development.R",
      "code_lines": "1-80\n1-80\n1-78\n121-200\n1-63"
    },
    "llm_raw": "{\"keep_indices\":[2,5,7,8,9],\"note\":\"Kept snippets that explicitly implement or operate on Cox proportional hazards models and their hazard ratio outputs, including model specification (Surv(...) formulas), model fitting with rms::cph, and extraction/combination of fully adjusted and age–sex adjusted hazard ratios for different cohorts. Excluded Kaplan–Meier plotting code as less directly tied to the Cox test specification.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "13",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,1,4,5],\"note\":\"Indices 0, 1, 4, and 5 contain substantive executable R code that directly define or configure the variables used in the Cox models for long COVID, including outcome variables (lcovid_surv, lcovid_cens, long COVID dates), covariate naming conventions (cov_cat_, cov_num_, out_, sub_), cohort/index dates, COVID severity phenotypes, and the inclusion of all covariates with prefix cov_ (age excluded then re-added via spline, region as a stratification factor). These snippets are central for understanding which variables enter the fully adjusted Cox models and how they are designated as outcomes, predictors, or stratification variables. Other snippets focus more on plotting, fatigue-specific models, or post-processing of results and are less directly informative for variable specification of the long COVID Cox models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "13",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n121-200\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,4,6,8],\"note\":\"Kept snippets that directly define or operationalize parameters of the Cox models and cohorts for coded long COVID, including cohort definitions (all / pre-/post-vaccination / infected), outcome/censoring definitions, use of restricted cubic splines for age, stratification by region, inverse probability weighting with specified case:control sampling ratios, and construction of fully adjusted vs age-sex adjusted hazard ratio outputs. Other snippets were more about plotting or validation, or focused on different outcomes (e.g., fatigue) rather than the long COVID Cox models described in the analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "13",
    "dimension": "Inference Specification",
    "value": "## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage3_fatigue_survival_analysis.R\nanalysis/for_future_development/testing_stage5_validation.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n1-80\n121-200\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,4,5,6,8],\"note\":\"Selected snippets that implement key inference procedures: inverse probability weighting and weighted sampling for Cox models (2,4,6,8), and construction/combination of hazard ratio estimates from fully adjusted and age–sex adjusted Cox models across cohorts (5). These directly relate to the study’s inference specification (IPW, model-based HRs with CIs by cohort).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "13",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n1-80\n61-140\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,5,7,8,9],\"note\":\"Kept snippets that explicitly define or manipulate variable codings used in the Cox models: (0) specifies covariate selection by name prefix, age as spline vs linear, contrasts, and stratification; (5) documents naming conventions for categorical vs numeric covariates and creates key derived variables; (7) further codes COVID severity phenotype as a factor and drops certain variables; (8) enforces age bounds, redefines region with a reference level, and constructs age-group categories; (9) sets up model input including outcome variable re-coding for specific analyses and defines inverse-probability weights as a new variable.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "14",
    "dimension": "Test Specification",
    "value": "# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2023-04-15/\")\noutput_dir <- paste0(common_dir, \"2023-04-15/\")\n\ncohort = \"all\"\n#cohort = \"infected\"\n\n# km_plot <- function(results_dir, output_dir, cohort){\n  print(cohort)\n  dat_age <- read.csv(paste0(results_dir, file=paste0(\"tbl_km_dat_age_\",cohort,\".csv\")))\n  dat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n  # Create cumulative incidence plot -----------------------------------------------------------\n  svglite::svglite(paste0(results_dir,\"figures/km_incipence_plot_\", cohort,\".svg\"), width = 10, height = 8)\n  ggplot(dat_age, \n         aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n             linetype = cov_cat_age_group)) +\n    geom_path() + #Plotting\n    geom_line(aes(linetype=cov_cat_age_group),size=1)+\n    scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                   \"40-59\" = \"dotted\",\n                                   \"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(\n      #limits = c(index_date, end_date), \n      date_breaks = \"6 months\", \n      date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  dev.off()\n  \n  # Create cumulative probability plot -----------------------------------------------------------\n  svglite::svglite(paste0(results_dir, \"figures/km_plot_\",cohort,\".svg\"), width = 10, height = 8)\n  \n  ggplot(dat_age, \n         aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n             linetype = cov_cat_age_group)) +\n    geom_path() + #Plotting\n    geom_line(aes(linetype=cov_cat_age_group),size=1)+\n    scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                   \"40-59\" = \"dotted\",\n                                   \"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  # ggsave(file= \"km_plot.png\",\n  #        path = paste0(output_dir, \"figures\"),\n  #        plot=p2, width=10, height=8)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/externals/ext_figure_km_v2.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/create_project_actions.R\nanalysis/dated code/figure_hazard_ratio_plot.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R",
      "code_lines": "1-80\n1-80\n481-560\n1-40\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,2,4,7,9],\"note\":\"Kept snippets that implement or orchestrate the specific survival/hazard-based tests used in the analysis: (1) Kaplan–Meier estimation for cumulative incidence (index 1), (2) Cox proportional hazards model setup including age–sex and full models for long COVID (index 2), (3) project actions that run development Cox models and explicitly age–sex and age–sex–adjusted Cox models for long COVID (index 4), (4) hazard ratio plotting from Cox model outputs (index 7), and (5) external code that reads and organizes hazard ratio outputs for fully adjusted and age–sex–adjusted models across cohorts, including the primary and post-COVID cohorts (index 9). Other snippets were excluded as they focused only on plotting KM curves without broader context, non-long-COVID outcomes, or were less directly tied to the Cox/HR test specification for this analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "14",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,3,7],\"note\":\"Kept snippets that directly specify variables for the long COVID time-to-event analyses: (index 0) constructs Cox model formulas defining the long COVID outcome, age–sex adjusted and fully adjusted predictor sets, and region stratification; (indices 3 and 7) perform data cleaning and variable construction, including index dates, COVID infection and severity phenotypes, renaming and dropping covariates, and enforcing the covariate naming scheme, all of which clarify how variables are defined and designated for analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "14",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/create_project_actions.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80\n481-560"
    },
    "llm_raw": "{\"keep_indices\":[0,5,9,8,6],\"note\":\"Kept snippets that define Cox model formulas and cohort/time-at-risk parameters for the long COVID analyses, including age–sex vs full models and analysis-specific survival definitions. These directly reflect parameter specification for the hazard ratio models described in the paper.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "14",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage2_model_input_set_up.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R",
      "code_lines": "1-80\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,6,9],\"note\":\"Kept snippets that implement or organize the Cox proportional hazards models and hazard ratio outputs underpinning the age–sex adjusted and fully adjusted estimates, which are directly tied to the paper’s inferential approach (hazard ratios with confidence intervals). Other snippets focus on plotting or data cleaning without specifying or extracting inferential quantities.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "14",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/for_future_development/testing_stage5_validation.R\nanalysis/stage3_fatigue_survival_analysis.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[1,2,3,6,9],\"note\":\"Kept scripts that specify how covariates are coded/selected or how contrasts are set in the Cox models, and that contain substantive executable code (e.g., use of cov_ prefixes, exclusion of age variables, treatment contrasts, creation/recoding of indicator and categorical variables). Discarded snippets that are primarily pipeline orchestration, plotting, or commented-out definitions without executable coding logic.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "15",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_model_development.R\nanalysis/stage2_model_input_set_up.R\nanalysis/externals/ext_figure_km.R\nanalysis/dated code/figure_hazard_ratio_plot.R",
      "code_lines": "1-80\n1-63\n1-80\n1-79\n1-40"
    },
    "llm_raw": "{\"keep_indices\":[0,4,6,5,7],\"note\":\"Kept snippets that implement or directly operationalize the specified statistical tests: Cox proportional hazards models for long COVID (indices 0, 4, 6) and Kaplan–Meier cumulative incidence/probability plots (index 5), plus hazard ratio plotting from Cox model outputs (index 7). These contain substantive executable code explicitly tied to survival analysis and hazard ratio estimation/visualization relevant to the test specification dimension.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "15",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n#     asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#     copd_code_ever=patients.with_these_clinical_events(\n#         chronic_respiratory_disease_codes\n#     ),\n# ),\n\n# cov_cat_asthma=patients.categorised_as(\n#   {\n#     \"0\": \"DEFAULT\",\n#     \"1\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND (\n#                     prednisolone_last_year = 0 OR \n#                     prednisolone_last_year > 4\n#                     )\n#                 \"\"\",\n#     \"2\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND\n#                     prednisolone_last_year > 0 AND\n#                     prednisolone_last_year < 5\n#                     \n#                 \"\"\",\n#   },\n#   return_expectations={\"category\": {\"ratios\": {\"0\": 0.8, \"1\": 0.1, \"2\": 0.1}},},\n#   recent_asthma_code=patients.with_these_clinical_events(\n#     #asthma_codes, between=[\"2017-12-01\", \"2020-12-01\"],\n#     asthma_codes, between=[f\"{index_date_variable_3y}\",f\"{index_date_variable}\"],\n#   ),\n#   asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#   copd_code_ever=patients.with_these_clinical_events(\n#     chronic_respiratory_disease_codes\n#   ),\n#   prednisolone_last_year=patients.with_these_medications(\n#     prednisolone_codes,\n#     #between=[\"2019-12-01\", \"2020-12-01\"],\n#     between=[f\"{index_date_variable_1y}\",f\"{index_date_variable}\"],\n#     returning=\"number_of_matches_in_period\",\n#   ),\n# ), \n# pre_cov_diabetes_date=patients.with_these_clinical_events(\n#     diabetes_codes, return_first_date_in_period=True, include_month=True,\n# ),\n# # https://github.com/ebmdatalab/tpp-sql-notebook/issues/32\n# pre_cov_lung_cancer_date=patients.with_these_clinical_events(\n#     lung_cancer_codes, return_first_date_in_period=True, include_month=True,\n# ),\n# pre_cov_haem_cancer_date=patients.with_these_clinical_events(\n#     haem_cancer_codes, return_first_date_in_period=True, include_month=True,\n# ),\n# pre_cov_other_cancer_date=patients.with_these_clinical_events(\n#     other_cancer_codes, return_first_date_in_period=True, include_month=True,\n# ),\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/dated_variables_in_study_definition.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n61-121\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,2,4,6,9],\"note\":\"Kept snippets that define or structure variables for the Cox models: the main long COVID survival formula and covariate set (0), detailed comorbidity/asthma variable definitions used as predictors (2), data cleaning and construction/renaming of key analysis variables including index dates and gp-patient interaction (4,6), and the model input setup specifying outcome variables and cohort-specific inputs for the Cox models (9).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "15",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,5,7,9],\"note\":\"Kept snippets that explicitly specify parameters of the long COVID Cox modelling pipeline: (0) defines the Cox model formulas, including which covariates enter, spline specification for age, and regional stratification; (5) sets key analysis dates (index date, cohort end), cohort definitions, and COVID/long COVID validity filters; (7) configures analysis cohorts, sampling ratio for non-cases, and inverse probability weighting for the long COVID outcome; (9) is an earlier but still relevant long COVID survival data setup script, including cohort selection, sampling parameters, and IPW, plus time-dependent vaccination handling. Other snippets were either about fatigue rather than long COVID, were plotting-only, or were mostly commented-out study-definition code rather than executable parameter specification for the main analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "15",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R",
      "code_lines": "1-78"
    },
    "llm_raw": "{\"keep_indices\":[9],\"note\":\"Kept index 9 because it directly handles inference outputs (hazard ratios with confidence intervals) and explicitly labels them as 95% confidence intervals, which is relevant to the inference specification dimension. Other snippets focus on data preparation, model setup, or variable construction without specifying inference criteria (e.g., alpha levels or CI levels).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "15",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/for_future_development/testing_stage5_validation.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[2,3,6,7],\"note\":\"Kept snippets that contain substantive, executable code directly specifying how variables are coded or treated in the Cox models: (2) and (3) set up covariate sets, handle age coding (spline vs linear), and explicitly define contrast coding (contr.treatment) for categorical variables; (6) and (7) define and recode key analysis variables, including categorical COVID-19 severity (sub_cat_covid_phenotype), an infection indicator (out_covid), date-variable handling, and the systematic naming scheme distinguishing categorical vs numeric covariates and analysis vs non-analysis variables. Commented-out code in other snippets (e.g., detailed asthma categorisation) was not kept because it is not executable as written.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "16",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_4_model_dev_eval_age_sex_adjusted.R\nanalysis/stage3_model_development.R\nanalysis/create_project_actions.R\nanalysis/stage3_fatigue_survival_analysis.R",
      "code_lines": "1-80\n1-80\n1-63\n481-560\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[3,6,7,5,8],\"note\":\"These snippets contain the core Cox proportional hazards model specifications and related pipeline steps, including the age–sex adjusted and fully adjusted Cox models, directly evidencing the test type used to generate the hazard ratios described in the analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "16",
    "dimension": "Variable Specification",
    "value": "## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage0_data_cleaning.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/create_project_actions.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n481-560\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,1,4,6],\"note\":\"Kept snippets that define/prepare analysis variables and specify their roles in Cox models: (0) and (6) for variable creation and cohort/index definitions (including COVID infection and hospitalisation phenotype), and (1) and (4) for Cox model setup and pipeline actions distinguishing full, age–sex, and age–sex-adjusted models. Plotting and validation scripts or fatigue-specific models were excluded as less directly relevant to variable specification for the described long COVID Cox analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "16",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage3_fatigue_survival_analysis.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,1,5,8,9],\"note\":\"Selected snippets that define key analysis parameters and model specifications: (0) sets up Cox model formulas including age–sex and full models with spline vs linear age; (1) specifies survival analysis setup with inverse probability weighting and age–sex model for a related outcome, illustrating parameter choices (knots, sampling ratio); (5) defines cohort index dates, follow-up window, and initial data cleaning rules, which are core analysis parameters; (8) continues the same cleaning script with explicit COVID severity definitions (e.g., 28-day hospitalisation window) and variable exclusion rules; (9) constructs combined hazard ratio tables distinguishing fully adjusted vs age–sex adjusted models across cohorts, encoding the separation between partial and full adjustment used in the paper’s figures.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "16",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage3_model_development.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R",
      "code_lines": "1-80\n1-80\n1-63\n121-200\n1-78"
    },
    "llm_raw": "{\"keep_indices\":[0,6,7,8,9],\"note\":\"Kept snippets that implement or directly use Cox models to generate hazard ratios and 95% confidence intervals, including age–sex adjusted vs fully adjusted estimates across cohorts. Index 0 defines the age–sex and full Cox model formulas; 6 prepares the weighted survival data by cohort (including the post-COVID/infected cohort) for those Cox analyses; 7 fits the Cox models and outputs hazard ratios with 95% CIs; 8 reads and combines fully adjusted and age–sex adjusted HRs (explicitly for Primary, Pre-/Post-vaccination, and Post-COVID cohorts) with 95% CIs; 9 reads HR estimates with conf.low/conf.high and visualizes them, highlighting inference via confidence intervals and reference line at HR=1. Other snippets are more focused on Kaplan–Meier plotting, project orchestration, or non-Cox validations and are less directly tied to the specified inference aspect of the age–sex adjusted Cox analyses and Figure 3.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "16",
    "dimension": "Coding Specification",
    "value": "## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/create_project_actions.R",
      "code_lines": "1-80\n61-140\n1-80\n481-560"
    },
    "llm_raw": "{\"keep_indices\":[1,2,3,0],\"note\":\"Kept snippets that define variable coding and model setup for Cox analyses, including cohort and COVID-severity coding (stage0_data_cleaning) and the Cox model formula/contrasts for age–sex and fully adjusted models (stage3_long_covid_code_cox_model), plus the pipeline actions that invoke age–sex and age–sex adjusted Cox models. Other snippets were excluded as they are either descriptive-only, focused on different outcomes, or consist primarily of commented-out (non-executable) code.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "17",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2023-04-15/\")\noutput_dir <- paste0(common_dir, \"2023-04-15/\")\n\ncohort = \"all\"\n#cohort = \"infected\"\n\n# km_plot <- function(results_dir, output_dir, cohort){\n  print(cohort)\n  dat_age <- read.csv(paste0(results_dir, file=paste0(\"tbl_km_dat_age_\",cohort,\".csv\")))\n  dat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n  # Create cumulative incidence plot -----------------------------------------------------------\n  svglite::svglite(paste0(results_dir,\"figures/km_incipence_plot_\", cohort,\".svg\"), width = 10, height = 8)\n  ggplot(dat_age, \n         aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n             linetype = cov_cat_age_group)) +\n    geom_path() + #Plotting\n    geom_line(aes(linetype=cov_cat_age_group),size=1)+\n    scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                   \"40-59\" = \"dotted\",\n                                   \"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(\n      #limits = c(index_date, end_date), \n      date_breaks = \"6 months\", \n      date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  dev.off()\n  \n  # Create cumulative probability plot -----------------------------------------------------------\n  svglite::svglite(paste0(results_dir, \"figures/km_plot_\",cohort,\".svg\"), width = 10, height = 8)\n  \n  ggplot(dat_age, \n         aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n             linetype = cov_cat_age_group)) +\n    geom_path() + #Plotting\n    geom_line(aes(linetype=cov_cat_age_group),size=1)+\n    scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                   \"40-59\" = \"dotted\",\n                                   \"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  # ggsave(file= \"km_plot.png\",\n  #        path = paste0(output_dir, \"figures\"),\n  #        plot=p2, width=10, height=8)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/ext_figure_km_v2.R\nanalysis/stage3_model_development.R\nanalysis/dated code/figure_hazard_ratio_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "1-80\n1-80\n1-63\n1-40\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,4,7,8,9],\"note\":\"Kept snippets that directly specify or operationalize the statistical tests used (Cox proportional hazards models and associated hazard ratios, plus Kaplan–Meier estimation/plots) for long COVID analyses. Index 0 defines survival formulas for Cox PH models for long COVID. Index 4 implements Kaplan–Meier-based cumulative incidence/probability plots by age group and sex, matching the described KM approach. Index 7 fits Cox PH models via rms::cph and outputs hazard ratios and AIC, corresponding to fully adjusted multivariable Cox models. Indices 8 and 9 read and plot hazard ratio estimates from full and age/sex(-adjusted) Cox models across cohorts (including infected/post-COVID), directly tied to the reported adjusted hazard ratios. Other snippets were more about pipeline orchestration or different outcomes (fatigue) and thus less directly relevant to this analysis–dimension pair.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "17",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,3,4,8],\"note\":\"Selected snippets that explicitly define or structure variables used in the Cox models: (1) constructs the survival formulas and specifies covariates (cov_* predictors, age spline, region stratification); (3) and (4) define key variables including COVID infection indicator and COVID-19 severity phenotype (no infection, non-hospitalised, hospitalised) and show naming conventions for covariates/outcomes; (8) specifies which cohort dataset is used for the infected/post-COVID diagnosis cohort and sets up the long COVID survival outcome variables (lcovid_surv, lcovid_cens). These are most directly relevant to Variable Specification for the described multivariable Cox analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "17",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,3,6,8],\"note\":\"Kept snippets that explicitly specify key model and analysis parameters for the Cox proportional hazards work: (0) defines the Cox survival formulas (including full multivariable model, spline for age, and stratification) and thus the parameterization of the fully adjusted model; (3) sets core study parameters such as index_date, cohort_end, and initial COVID infection/severity flags; (6) fully defines the COVID severity/phenotype variable (no infection, non-hospitalised, hospitalised within 28 days), which underpins the hospitalised vs non-hospitalised parameter in the model; (8) specifies analysis cohorts, sampling ratio (20:1 controls to cases), inverse probability weights, and input restructuring for different Cox analyses, all of which are central parameter choices for the multivariable models. Other snippets focus mainly on plotting or on different outcomes (e.g., fatigue) and are less directly relevant to the requested parameter specification for the long COVID Cox analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "17",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: model development and some evaluation\n# Output:  hazard ratios and 95% CI\n\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\n\n####################################################################################################\n# Part 1: load data, define inverse probability weighting, fit cox model and assess PH assumption  #\n#         variable selection using backward elimination                                            #\n####################################################################################################\n\n# # load data, with defined weight, and import formula for survival analysis \nsource(\"analysis/stage3_model_selection.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nprint(\"Starting stage3_model_development.R\")\n\nif(which_model == \"selected\"){\n  fit_cox_model_selected <-rms::cph(formula= as.formula(surv_formula_selected),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  print(\"The selected model is\")\n  print(fit_cox_model_selected)\n}\n\n# Model 1\nprint(\"Output Cox model with age as spline:\")\nprint(fit_cox_model_splines)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_spline\", \"_\", analysis)\ncox_output2(fit_cox_model_splines, \"full_model_age_splines\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_splines, file=paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis,\".rds\"))\n\n# Model 2\nprint(\"Output Cox model with age linear:\")\nprint(fit_cox_model_linear)\noutput_file = paste0(\"output/review/model/HR_\", \"full_model_age_linear\", \"_\", analysis)\ncox_output2(fit_cox_model_linear, \"full_model_age_linear\",output_file, save_output=TRUE)\nsaveRDS(fit_cox_model_linear, file=paste0(\"output/not_for_review/model/fit_cox_model_linear_\", analysis,\".rds\"))\n\n# Model 3\nif(which_model == \"selected\"){\n  output_file = paste0(\"output/review/model/HR_\", which_model, \"_\", analysis)\n  cox_output2(fit_cox_model_selected, \"selected_model\", output_file, save_output = TRUE)\n  saveRDS(fit_cox_model_selected, file=paste0(\"output/not_for_review/model/fit_cox_model_selected_\", analysis,\".rds\"))\n}\nprint(\"Finished stage3_model_development.R\")\n\n# Output AIC from all three models\n\ndf_aic <- data.frame(analysis,AIC(fit_cox_model_splines), AIC(fit_cox_model_linear))\ndf_aic[,2:3]<- round(df_aic[,2:3],2)\nif(which_model == \"selected\"){\n  df_aic <- cbind(df_aic, round(AIC(fit_cox_model_selected),2))\n}\n\nwrite.csv(df_aic, file=paste0(\"output/review/model/AIC_\",analysis,\".csv\"))\n\n#RK - you're getting large robust SE's in your results - I ran your model without +cluster(practice_id)\n#and the robust SE's were then fine - potentially something to invetsigate?\n\n#YW - That's a good point! I am now trying to use strat(sub_cat_region) instead - it does work well\n# with strat(sub_cat_region). But we now use region as a covariate instead",
    "source": "code",
    "evidence": {
      "code_path": "analysis/stage3_model_development.R",
      "code_lines": "1-63"
    },
    "llm_raw": "{\"keep_indices\":[8],\"note\":\"Only snippet 8 directly concerns inference outputs for the Cox models, explicitly generating hazard ratios and 95% confidence intervals, which aligns with the Inference Specification dimension. Other snippets focus on data setup, weighting, formula construction, or plotting without specifying inference criteria or interval estimation.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "17",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# 01 June 2022\n# background: some exploration on the possibility to create more refined categories for the following covariates:\n# cancer(non-haematological), haematological cancer, asthma, diabetes and reduced kidney function\n\n\n# pre_cov_num_creatinine=patients.with_these_clinical_events(\n#     creatinine_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-02-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 60.0, \"stddev\": 15},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# pre_cov_dialysis_date=patients.with_these_clinical_events(\n#     dialysis_codes, return_first_date_in_period=True, include_month=True,\n# ),\n\n# pre_cov_hba1c_mmol_per_mol=patients.with_these_clinical_events(\n#     hba1c_new_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-12-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 40.0, \"stddev\": 20},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# pre_cov_hba1c_percentage=patients.with_these_clinical_events(\n#     hba1c_old_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-12-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 5, \"stddev\": 2},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# cov_cat_asthma=patients.satisfying(\n#     \"\"\"\n#         recent_asthma_code OR (\n#         asthma_code_ever AND NOT\n#         copd_code_ever\n#         )\n#     \"\"\",\n#     return_expectations={\n#         \"incidence\": 0.05,\n#    },\n#     recent_asthma_code=patients.with_these_clinical_events(\n#         asthma_codes,\n#         # between=[f\"{index_date_variable - 3 years}\",f\"{index_date_variable}\"],\n#         between=[f\"{index_date_variable_3y}\",f\"{index_date_variable}\"],\n#     ),\n#     asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#     copd_code_ever=patients.with_these_clinical_events(\n#         chronic_respiratory_disease_codes\n#     ),\n# ),\n\n# cov_cat_asthma=patients.categorised_as(\n#   {\n#     \"0\": \"DEFAULT\",\n#     \"1\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND (\n#                     prednisolone_last_year = 0 OR \n#                     prednisolone_last_year > 4\n#                     )\n#                 \"\"\",\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/dated_variables_in_study_definition.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[1,3,6,7],\"note\":\"Kept snippets that define or operationalize variable coding relevant to the multivariable Cox model. Index 1 specifies how covariates are included and sets treatment contrasts (contr.treatment) for categorical variables, directly affecting coding of predictors in the Cox model. Indices 3 and 7 (same file, different sections) provide variable naming conventions, index date definition, coding of COVID infection status and severity (no infection/non-hospitalised/hospitalised) as a factor, and removal/recoding of certain covariates, all central to coding specification. Index 6 contains (dated but executable-style) OpenSAFELY study-definition code that specifies categorisation logic for covariates such as asthma (cov_cat_asthma), illustrating how indicator/categorical variables are constructed. Other snippets focus on modeling workflow, plotting, or sampling but do not provide additional detail on how variables themselves are coded.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "18",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/figure_hazard_ratio_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "1-80\n1-40\n1-80\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,2,7,9],\"note\":\"Kept snippets that define or organize the Cox proportional hazards models and hazard ratio outputs, directly specifying or structuring the test type (hazard ratios from Cox models, fully adjusted vs age–sex adjusted, and across cohorts including pre-vaccination/post-vaccination). Snippet 0 sets up the Cox survival formulas; snippet 2 generates hazard ratio plots by cohort; snippets 7 and 9 read and combine fully adjusted and age–sex adjusted hazard ratio CSVs for multiple cohorts, including the pre-vaccination cohort, explicitly organizing the HR-based test results.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "18",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,7,8],\"note\":\"Selected snippets that (1) explicitly define or structure the survival outcome vs predictor variables for the Cox models, and/or (2) define and classify covariates (health/clinical variables, COVID phenotype, GP interaction, etc.) and cohort-specific survival variables used in the long COVID analyses. These are directly relevant to variable specification for the pre-vaccination-style cohorts and contain substantive executable R code.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "18",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n1-80\n1-80\n121-200\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,3,6,7,8],\"note\":\"Kept snippets that define key model and cohort parameters for the long COVID Cox analyses, especially for the pre-vaccination-like cohort (all_vax_c): (0) specifies Cox model formulas including age–sex adjusted and full models with spline for age and region stratification; (3) sets index and end dates, cohort-specific index definitions, and key derived variables (e.g., COVID phenotype, GP interaction variable rename); (6) sets up survival data for long COVID code including cohort definitions (all, all_vax_c, vaccinated, infected), inverse probability weighting, and sampling ratio; (7) combines and structures fully adjusted vs age–sex adjusted hazard ratios across cohorts including the Pre-vaccination cohort; (8) similar to (6), specifying analysis cohort options, censoring at vaccination for all_vax_c, inverse probability weighting, and time-dependent vaccination setup—directly reflecting parameter choices used in the analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "18",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "1-80\n1-80\n1-80\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,5,7,9],\"note\":\"Kept snippets that (a) implement Cox models and age–sex vs fully adjusted specifications for long COVID (0), and (b) load/organize hazard ratio outputs, including age–sex adjusted and fully adjusted HRs with 95% CIs for the primary, pre-vaccination, post-vaccination, and post-COVID cohorts (5,7,9). These are directly relevant to how inference (HRs and CIs) is specified and contrasted across adjustment sets and cohorts.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "18",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage3_fatigue_survival_analysis.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,2,6,7,9],\"note\":\"Kept snippets that specify how variables are coded/structured for the Cox models or analysis datasets: (0) sets treatment contrasts and defines survival formulas using covariate coding conventions; (2) documents variable naming conventions and recodes/derives variables like gp interaction and COVID indicators; (6) similarly defines covariate sets and age spline coding for the fatigue model; (7) configures outcome variable coding and cohort-specific survival variables (e.g., recoding lcovid_surv / lcovid_cens for different analyses); (9) derives and factorizes the 3-level COVID severity phenotype and handles date-variable coding. Other snippets were more about workflow or descriptive outputs and not directly about variable coding for the analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "19",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R\nanalysis/externals/ext_figure_hazard_ratio_read_data.R\nanalysis/dated code/figure_hazard_ratio_plot.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n1-78\n1-80\n1-40\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,2,5,6,9],\"note\":\"Selected snippets that define or use Cox proportional hazards models and hazard ratios underpinning the fully adjusted multivariable Cox PH analysis: (0) constructs Surv() formulas and specifies full/age–sex Cox models; (9) sets up survival outcome, censoring, cohorts, and weights for the Cox model; (2) reads hazard_ratio estimates and prepares them for plotting, confirming HR as the effect measure from the Cox model; (6) similarly processes hazard_ratio estimates and generates hazard ratio plots; (5) organizes inputs from multiple HR_* files for fully adjusted and age–sex adjusted models across cohorts, explicitly handling hazard ratio outputs. These directly support the test specification (Cox PH with adjusted hazard ratios) for the described analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "19",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R",
      "code_lines": "1-80\n1-80\n61-140\n1-80\n1-78"
    },
    "llm_raw": "{\"keep_indices\":[0,1,3,4,8],\"note\":\"Kept snippets that (1) specify which variables enter the Cox models and how (outcome vs predictors vs stratification) and/or (2) define and prepare the covariate variables used in those models, with substantive executable R code. Index 0 defines the Surv() outcome and full predictor set (all cov_* variables plus age spline and region stratification). Indices 1 and 3 perform data cleaning and construct key variables (e.g., covid phenotype, gp interaction renaming, index dates) that become covariates. Index 4 sets up the model input, including survival time and censoring variables (lcovid_surv, lcovid_cens) and sampling/weights, clarifying the outcome specification by analysis cohort. Index 8 processes hazard ratio outputs by predictor term, mapping variable names and levels, which reflects the predictor specification used in the Cox models. Commented or non-executable snippets and those focused on other outcomes (e.g., fatigue) or only plotting without variable-role information were excluded.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "19",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,2,3,4,5],\"note\":\"Kept snippets that directly specify parameters of the Cox analyses: model formulas and covariate structure (0), study start/end dates and cohort/index-date rules plus COVID severity categorisation including hospitalised vs non-hospitalised (2,4), and analysis-level sampling/weighting parameters and cohort definitions for the survival models (3,5). Other snippets were either non-executable (commented study-def code) or focused on different outcomes or plotting rather than parameter specification for the requested long COVID Cox analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "19",
    "dimension": "Inference Specification",
    "value": "################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`",
    "source": "code",
    "evidence": {
      "code_path": "analysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "121-200\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[7,8],\"note\":\"Kept snippets that explicitly handle inference outputs (hazard ratios with 95% confidence intervals) and related processing of CI bounds; other snippets focus on data prep, cohort definition, or model setup without specifying inference criteria.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "19",
    "dimension": "Coding Specification",
    "value": "#     asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#     copd_code_ever=patients.with_these_clinical_events(\n#         chronic_respiratory_disease_codes\n#     ),\n# ),\n\n# cov_cat_asthma=patients.categorised_as(\n#   {\n#     \"0\": \"DEFAULT\",\n#     \"1\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND (\n#                     prednisolone_last_year = 0 OR \n#                     prednisolone_last_year > 4\n#                     )\n#                 \"\"\",\n#     \"2\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND\n#                     prednisolone_last_year > 0 AND\n#                     prednisolone_last_year < 5\n#                     \n#                 \"\"\",\n#   },\n#   return_expectations={\"category\": {\"ratios\": {\"0\": 0.8, \"1\": 0.1, \"2\": 0.1}},},\n#   recent_asthma_code=patients.with_these_clinical_events(\n#     #asthma_codes, between=[\"2017-12-01\", \"2020-12-01\"],\n#     asthma_codes, between=[f\"{index_date_variable_3y}\",f\"{index_date_variable}\"],\n#   ),\n#   asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#   copd_code_ever=patients.with_these_clinical_events(\n#     chronic_respiratory_disease_codes\n#   ),\n#   prednisolone_last_year=patients.with_these_medications(\n#     prednisolone_codes,\n#     #between=[\"2019-12-01\", \"2020-12-01\"],\n#     between=[f\"{index_date_variable_1y}\",f\"{index_date_variable}\"],\n#     returning=\"number_of_matches_in_period\",\n#   ),\n# ), \n# pre_cov_diabetes_date=patients.with_these_clinical_events(\n#     diabetes_codes, return_first_date_in_period=True, include_month=True,\n# ),\n# # https://github.com/ebmdatalab/tpp-sql-notebook/issues/32\n# pre_cov_lung_cancer_date=patients.with_these_clinical_events(\n#     lung_cancer_codes, return_first_date_in_period=True, include_month=True,\n# ),\n# pre_cov_haem_cancer_date=patients.with_these_clinical_events(\n#     haem_cancer_codes, return_first_date_in_period=True, include_month=True,\n# ),\n# pre_cov_other_cancer_date=patients.with_these_clinical_events(\n#     other_cancer_codes, return_first_date_in_period=True, include_month=True,\n# ),\n\n# 01 June 2022\n# background: some exploration on the possibility to create more refined categories for the following covariates:\n# cancer(non-haematological), haematological cancer, asthma, diabetes and reduced kidney function\n\n\n# pre_cov_num_creatinine=patients.with_these_clinical_events(\n#     creatinine_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-02-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 60.0, \"stddev\": 15},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# pre_cov_dialysis_date=patients.with_these_clinical_events(\n#     dialysis_codes, return_first_date_in_period=True, include_month=True,\n# ),\n\n# pre_cov_hba1c_mmol_per_mol=patients.with_these_clinical_events(\n#     hba1c_new_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-12-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 40.0, \"stddev\": 20},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# pre_cov_hba1c_percentage=patients.with_these_clinical_events(\n#     hba1c_old_codes,\n#     find_last_match_in_period=True,\n#     on_or_before=\"2020-12-01\",\n#     returning=\"numeric_value\",\n#     include_date_of_match=True,\n#     include_month=True,\n#     return_expectations={\n#         \"float\": {\"distribution\": \"normal\", \"mean\": 5, \"stddev\": 2},\n#         \"incidence\": 0.95,\n#     },\n# ),\n# cov_cat_asthma=patients.satisfying(\n#     \"\"\"\n#         recent_asthma_code OR (\n#         asthma_code_ever AND NOT\n#         copd_code_ever\n#         )\n#     \"\"\",\n#     return_expectations={\n#         \"incidence\": 0.05,\n#    },\n#     recent_asthma_code=patients.with_these_clinical_events(\n#         asthma_codes,\n#         # between=[f\"{index_date_variable - 3 years}\",f\"{index_date_variable}\"],\n#         between=[f\"{index_date_variable_3y}\",f\"{index_date_variable}\"],\n#     ),\n#     asthma_code_ever=patients.with_these_clinical_events(asthma_codes),\n#     copd_code_ever=patients.with_these_clinical_events(\n#         chronic_respiratory_disease_codes\n#     ),\n# ),\n\n# cov_cat_asthma=patients.categorised_as(\n#   {\n#     \"0\": \"DEFAULT\",\n#     \"1\": \"\"\"\n#                     (\n#                     recent_asthma_code OR (\n#                         asthma_code_ever AND NOT\n#                         copd_code_ever\n#                     )\n#                     ) AND (\n#                     prednisolone_last_year = 0 OR \n#                     prednisolone_last_year > 4\n#                     )\n#                 \"\"\",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/dated_variables_in_study_definition.R\nanalysis/dated code/dated_variables_in_study_definition.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R",
      "code_lines": "61-121\n1-80\n1-80\n1-80\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,1,2,3,4],\"note\":\"Kept snippets that explicitly define or recode analysis variables and their categories: (0,1) detailed categorisation logic for asthma (cov_cat_asthma) including multi-level coding based on events and medications; (2,4) coding of COVID infection phenotype and other covariates, including creation/renaming of covariate variables and factor coding; (3) specification of contrasts and construction of model formulas referencing covariate encodings (cov_cat_*, cov_num_*). Other snippets focus more on sampling, weighting, or descriptive outputs rather than variable coding specifications.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "20",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/figure_hazard_ratio_plot.R\nanalysis/create_project_actions.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "1-80\n1-40\n481-560\n121-200\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,4,7,9],\"note\":\"Selected snippets that (a) are directly tied to the long COVID hazard ratio analyses across cohorts (including pre-/post-vaccination) and (b) contain substantive executable code specifying or organizing the Cox proportional hazards models and their age–sex adjusted vs fully adjusted hazard ratio outputs. Other snippets were excluded for focusing on different outcomes (e.g., fatigue) or on Kaplan–Meier plots rather than the Cox/HR test specification central to this analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "20",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Count and pie chart of snomed code for long COVID diagnosis\n## Output:  suppl_table_1.csv, suppl_table_1.html, suppl_figure_pie.svg\n\n## function for small number suppression\nsource(\"analysis/functions/redactor2.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\n## Read in data and identify factor variables and numerical variables------------\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n## keep only observations where long covid indicator is 1\ninput <- input %>% filter(lcovid_cens == 1)\n\n## computational efficiency: only keep the needed variable\ninput <- input %>% dplyr::select(\"out_first_long_covid_code\")\n\nsnomed_code <- input$out_first_long_covid_code\n\ncount_data <-table(snomed_code)\n\ncount_data <- data.frame(count_data)\n\n\nnames(count_data) <- c(\"snomed_code\", \"count\")\ncount_data\ncount_data$percent = round(count_data$count / sum(count_data$count),3)\ncount_data\npercent_function <- function(x, digits = 1, format = \"f\", ...) {\n  paste0(formatC(100 * x, format = format, digits = digits, ...), \"%\")\n}\n\ncount_data$labels = percent_function(count_data$percent)\n\ncount_data$count <- redactor2(count_data$count)\nindex = which(is.na(count_data$count))\ncol_names <- c(\"count\",\"percent\",\"labels\")\ncount_data[index,col_names]= NA\n\n## use redactor for small number suppression\n## index <- which(count_data$count < 6)\n## count_data$count[index] = count_data$percent[index] = count_data$labels[index] = NA\n\ncount_data_active = count_data%>%filter(count>5)\n## Pie Chart\nsuppl_figure_pie <- ggplot(count_data_active, aes(x = \"\", y = count, fill = snomed_code)) +\n  geom_bar(width = 1, stat = \"identity\") +\n  coord_polar(theta = \"y\") +\n  labs(x = \"\", y = \"\", fill = \"SNOMED Code\") + \n  geom_text(aes(label = labels),\n            position = position_stack(vjust = 0.5)) +\n  theme(plot.title = element_text(hjust = 0.5), \n        legend.title = element_text(hjust = 0.5, face=\"bold\", size = 10), \n        axis.ticks = element_blank(), \n        axis.text.y = element_blank(),\n        axis.text.x = element_blank()) \n\nsuppl_figure_pie\n\n## supplementary figure - pie chart\nggsave(file=\"output/not_for_review/descriptives/snomed_code_pie.svg\", plot=suppl_figure_pie, width=16, height=8)\n\n## output underlying count data for supplementary figure - pie chart\n\n## small number suppression - indicate NA as redacted\ncount_data[which(is.na(count_data$count)),col_names]=\"[redacted]\" \n\nwrite.csv(count_data, file=\"output/not_for_review/descriptives/snomed_code_table.csv\")\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/table_snomed_code.R\nanalysis/stage2_model_input_set_up.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n1-80\n1-74\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,6,7,9],\"note\":\"Kept snippets that define or operationalize core variables for the long COVID Cox models: outcome variables (lcovid_surv/lcovid_cens, out_first_long_covid_code), exposure/covariate structures (cov_ prefixes, cov_cat_sex, cov_num_age, gp-patient interaction), and cohort-/vaccination-specific survival variables. These directly inform which variables enter the models and how they are designated as outcomes or predictors in the analyses, including pre-/post-vaccination cohort setups. Discarded snippets that focus mainly on plotting or pipeline orchestration without additional variable-specification detail.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "20",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/stage0_data_cleaning.R\nanalysis/create_project_actions.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n121-200\n1-80\n481-560\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,5,6,7,9],\"note\":\"Kept snippets that define key analysis parameters for the Cox models and cohorts relevant to long COVID hazard ratios, including: (i) model formulas and spline/knot specifications for age–sex and fully adjusted Cox models (index 0); (ii) construction of HR tables for fully adjusted vs age–sex adjusted models across cohorts including pre- and post-vaccination (index 5); (iii) cohort/index-date/censoring definitions for vaccinated vs other cohorts (index 6); (iv) pipeline actions explicitly running development, age–sex, and age–sex–adjusted Cox models for each analysis cohort (index 7); and (v) survival data setup and inverse probability weighting parameters, including cohort-specific survival definitions (index 9). These are directly tied to parameter specification for the Cox analyses underlying Figure 4 and related text.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "20",
    "dimension": "Inference Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-09-05\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\nsource(\"analysis/externals/ext_function_HR_df_v2.R\")\ncommon_dir = \"C:/Users/yingh_/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\nresults_dir = paste0(common_dir, \"2022-08-31/\")\noutput_dir <- paste0(common_dir, \"2022-08-31/\")\nfile_list=list.files(path = results_dir, pattern = \"HR_*\")\nfor (i in 1:length(file_list)){\n  #temp = gsub(\".csv\", \"\", file_list[i])\n  assign(file_list[i], \n         read.csv(paste(results_dir, file_list[i], sep=''))\n  )\n}\n\ndf_list_full <- list(# full model with age splines\n                HR_full_model_age_spline_all.csv,         # primary\n                HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n                HR_full_model_age_spline_vaccinated.csv,  # post-vax\n                HR_full_model_age_spline_infected.csv,    # post-covid\n                HR_full_model_age_spline_all_vax_td.csv   # vaccination time-dependent\n                )\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv,     # post-covid\n  HR_full_model_categorical_all_vax_td.csv    # vaccination time-dependent\n  )\n\ndf_list_age_sex_adjusted <- list(\n                # age-sex adjusted model\n                HR_age_sex_adjusted_all.csv,        # primary\n                HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n                HR_age_sex_adjusted_vaccinated.csv, # post-vax\n                HR_age_sex_adjusted_infected.csv,   # post-covid\n                HR_age_sex_adjusted_all_vax_td.csv  # vaccination time-dependent\n                )\n\ndf_list_age_sex <-list(\n                # age-sex model with age splines\n                HR_age_sex_model_all.csv,        # primary\n                HR_age_sex_model_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_vaccinated.csv, # post-vax\n                HR_age_sex_model_infected.csv,   # post-covid\n                HR_age_sex_model_all_vax_td.csv  # vaccination time-dependent\n                )\ndf_list_age_sex_categorical <- list(\n                # age-sex model with categorical age\n                HR_age_sex_model_categorical_all.csv,        # primary\n                HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n                HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n                HR_age_sex_model_categorical_infected.csv,   # post-covid\n                HR_age_sex_model_categorical_all_vax_td.csv  # vaccination time-dependent\n                ) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage2_model_input_set_up.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R",
      "code_lines": "1-80\n1-80\n121-200\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,5,6,9],\"note\":\"Kept snippets that define the Cox model structures used for age–sex adjusted vs fully adjusted analyses and those that assemble hazard ratios and 95% CIs (including by cohort, e.g. pre-/post-vaccination), as well as the sampling and inverse probability weighting scheme underpinning the inference. These are directly relevant to how hazard ratios and confidence intervals are computed and combined for the post-vaccination cohort age–sex adjusted Cox models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "20",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Test validation code using a simple script for post-viral fatigue\n\nlibrary(readr); library(dplyr); library(rms); library(MASS); library(pseudo);library(fastDummies)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nsource(\"analysis/functions/function_int_ext_cross_validation.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\nanalysis = \"age_sex_fatigue_model\"\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\ninput <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\", \"sub_cat_region\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\nanalysis = \"fatigue_all\"\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\ninput <- read_rds(\"output/input_stage1_all.rds\")\n\n##--specify inverse probability weighting\ncases <- input %>% filter(fatigue_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n## remove region as a covariate\n#input <- rename(input, sub_cat_region = \"cov_cat_region\")\n\n## extract candidate predictors\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\n## remove categorical and continuous age, as continuous age will be added\n## later as with a spline function\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n## for computational efficiency, only keep the variables needed in fitting the model\nvariables_to_keep <- c(\"patient_id\", \"practice_id\",\n                       \"fatigue_surv\", \"fatigue_cens\", covariate_names,\n                       \"cov_num_age\", \"weight\")\n\n\ninput <- input %>% dplyr::select(all_of(variables_to_keep))\nprint(\"Part 1: load data, define inverse probability weighting is completed!\")\n\n################################################################################\n## Part 2: number of people in each covariate level                           ##\n################################################################################\nfunction_df_summary(input, analysis)\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n## Part 3: define survival analysis formula                                   ##\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(fatigue_surv, fatigue_cens) ~ \",\n  \"cov_cat_sex\",",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/for_future_development/testing_stage5_validation.R\nanalysis/stage0_data_cleaning.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage3_fatigue_survival_analysis.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,2,3,4,5],\"note\":\"Kept snippets that include executable code specifying how variables and models are coded: (1) stage3_long_covid_code_cox_model.R defines covariate selection, contrasts, spline specification, and survival formulas; (2) testing_stage5_validation.R similarly defines covariate sets, weighting, and model formulas; (3) stage0_data_cleaning.R contains explicit variable naming conventions and recoding for covariates and outcomes; (4) stage2_long_covid_code_data_setup.R defines survival/censoring variables, cohort-specific data setup, and inverse probability weights; (5) stage3_fatigue_survival_analysis.R parallels the long COVID model coding for a related outcome, including covariate coding, weighting, and spline specification.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "21",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  figure_hr_*svg for each analysis\n\nlibrary(readr); library(dplyr); library(ggplot2)\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nfigure_hr_generation <-function(analysis)\n{\n  selection <- read_csv(file=paste0(\"output/not_for_review/model/model_selection_\",analysis,\".csv\"))\n  which_model <- selection$which_model\n  hr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n  hr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n    # keep all characters before =\n    mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n    mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n    mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n    # keep all characters after =\n    mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n    mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n    mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n    mutate(term = sub(\"_\", \" \", term)) \n  \n  hr_plot <- ggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n    geom_point(shape=15, size = 3, color = \"darkblue\") +\n    geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n    xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n    #\n    ylab(label='Predictorss\\n') +\n    geom_vline(xintercept = 1, linetype = 2, color = \"#b16100\", size=1)\n  hr_plot\n  ggsave(file=paste0(\"output/review/model/figure_hr_\",which_model, \"_\", analysis, \".svg\"), \n         plot=hr_plot, width=12, height=10)\n}\n\nfor(analysis in c(\"all\", \"all_vax_c\", \"vaccinated\", \"all_vax_td\", \"infected\")){\n  try(figure_hr_generation(analysis))\n}\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/figure_hazard_ratio_plot.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R",
      "code_lines": "1-80\n1-40\n1-80\n61-140\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,2,5,7,8],\"note\":\"Kept snippets that implement or process Cox proportional hazards models and their hazard ratio outputs, specifically including cohort distinctions such as pre-/post-vaccination and post-COVID, which directly relate to the fully adjusted multivariable Cox model and adjusted hazard ratios described in the analysis. Excluded snippets that focus on other outcomes (fatigue-specific models), general project orchestration, or ancillary tests (e.g., chi-square) not central to the specified Cox/HR test specification for coded long COVID.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "21",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\ninput <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date),\n                                          \"non_hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- ifelse(!is.na(input$out_covid_date) & \n                                            !is.na(input$hospital_covid) &\n                                            (input$hospital_covid-input$out_covid_date>=0 &\n                                               input$hospital_covid-input$out_covid_date<29),\n                                          \"hospitalised\",input$sub_cat_covid_phenotype)\n  \n  input$sub_cat_covid_phenotype <- as.factor(input$sub_cat_covid_phenotype)\n\n  if(cohort == \"infected\"){\n    input <- input[!is.na(input$sub_cat_covid_phenotype),]\n  }\n\n  print(\"COVID19 severity determined successfully\")\n  \n  ## RK - not that it matters if you're not using it but this isn't how hospitalised covid has been defined in our current post covid\n  ## events opensafely projects\n  \n  ## YW response - a really good point - I have now amended and specified hospitalised covid as\n  ## admission to hospital within 28 days after covid positive test\n  \n  ## Step 2. Remove variables which are not included in the prediction------------\n  ## remove variables start with snomed\n  snomed_vars <- names(input)[which(grepl(\"snomed\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% snomed_vars)]\n  \n  tmp_vars <- names(input)[which(grepl(\"tmp\", names(input))==TRUE)]\n  input = input[,!(names(input) %in% tmp_vars)]\n  vars_to_drop <- c(\"sgss_positive\", \"primary_care_covid\", \"hospital_covid\",\n                    \"primary_care_death_date\",  \"ons_died_from_any_cause_date\", \n                    \"first_post_viral_fatigue_date\")\n  input = input[,!(names(input) %in% vars_to_drop)]\n  \n  ## partial sorting by variable names in the data frame, keep patient_id and practice_id at the front\n  input <- input %>% dplyr::select(patient_id, practice_id, index_date, death_date,\n                            colnames(input)[grepl(\"out_\",colnames(input))],\n                            colnames(input)[grepl(\"vax_\",colnames(input))],\n                            sort(tidyselect::peek_vars()))\n  \n  ## Step 3. specify date variables in the format of \"%Y-%m-%d\"----------------------------\n  vars_dates <- grep(\"date\", names(input))\n  vars_dates <- names(input)[vars_dates]\n  \n  convert_to_date <- function(x){\n    as.Date(x,format = \"%Y-%m-%d\")\n  }\n  input[vars_dates] = lapply(input[vars_dates], convert_to_date)\n  lapply(input[vars_dates], is.Date)\n  \n  ## if you wanted to simplify the above you could use something like\n  ## for (i in colnames(input)[grepl(\"date\",colnames(input))]) {\n  ## input[,i] <- as.Date(input[,i], format = \"%Y-%m-%d\")\n  ## }\n  ## Somehow this doesnt work on mine..and so have sticked to the above\n  \n  ################################################################################\n  ## Part 2 define multimorbidity                                                #\n\n##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n  input$cov_cat_age_group <- factor(input$cov_cat_age_group, ordered = TRUE)\n  \n  ##RK - this was defined in stage 0 - why are you redefining it here?\n  ##YW - A good question, this is because the dummy data have some observations under 18, and by removing them,\n  ##     the number of levels in cov_cat_age_group has changed\n  \n  #################################################################################\n  ##Part 2. Define follow-up end date and construct survival data for long covid ##\n  #################################################################################\n  \n  ## To improve efficiency: keep only necessary variables\n  variables_to_keep <-c(\"patient_id\", \"out_first_long_covid_date\", \"vax_covid_date1\",\n                        \"vax_covid_date2\", \"death_date\", \"cohort_end_date\", \"index_date\",\n                        \"out_covid_date\",\n                        \"out_first_post_viral_fatigue_date_after_pandemic\")\n  \n  input_select <- input%>% dplyr::select(all_of(variables_to_keep))\n  \n  ####################################################################################\n  ## Cohort 1 - all eligible adults; Cohort 3: post-vaccination; Cohort 4: post-covid\n  ## Construct time to long COVID \n  ## lcovid_cens: indicator for long covid  \n  ## lcovid_surv: days from index date to long COVID, without censoring by vaccination \n  ## for the following cohorts\n  ####################################################################################\n  input_select <- input_select%>% rowwise() %>% mutate(fup_end_date=min(out_first_long_covid_date, \n                                                                        death_date, \n                                                                        cohort_end_date,\n                                                                        na.rm = TRUE))\n  input_select <- input_select %>% filter(fup_end_date >= index_date & fup_end_date != Inf)\n  input_select$lcovid_surv<- as.numeric(input_select$fup_end_date - input_select$index_date)+1\n\n  ## define event indicator, without censoring for vaccination\n  input_select <- input_select %>% mutate(lcovid_cens = ifelse((out_first_long_covid_date <= fup_end_date & \n                                                                  out_first_long_covid_date >= index_date &\n                                                                  !is.na(out_first_long_covid_date)), 1, 0)) %>%\n    ## define 2nd vaccination as an intervening event - a time-dependent variable\n    ## add 14 days as buffer to allow vaccination to take effects\n    mutate(vax2_surv = ifelse((vax_covid_date2 +14)>= index_date & \n                                (vax_covid_date2 + 14) <= fup_end_date &\n                                !is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n1-80\n1-80\n61-140\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,2,6,7,8],\"note\":\"Selected snippets that (1) define the outcome and survival variables (lcovid_surv, lcovid_cens, vax2_surv), (2) specify covariate sets via cov_ naming, including gp-patient interaction and comorbidities, and (3) construct the full Cox model formula for long COVID. These are the most directly relevant to variable specification in the multivariable Cox analyses for long COVID across cohorts, including pre-/post-vaccination and post-COVID diagnosis settings.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "21",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Cox model: input set up, define survival formula through variables selection and AIC\n## Output:  survival formula \n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n## library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 20 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n## Part 1: load data, define inverse probability weighting                    ##\n################################################################################\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n##--specify inverse probability weighting\n\ncases <- input %>% filter(lcovid_cens==1)\n\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                                    non_case_inverse_weight, 1)\n\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n  input <- input[z$subs,] # replicates all variables\n  input$cov_cat_ie.status <- as.factor(ie.status)\n\n##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n  input$cov_cat_age_group <- factor(input$cov_cat_age_group, ordered = TRUE)\n  \n  ##RK - this was defined in stage 0 - why are you redefining it here?\n  ##YW - A good question, this is because the dummy data have some observations under 18, and by removing them,\n  ##     the number of levels in cov_cat_age_group has changed\n  \n  #################################################################################\n  ##Part 2. Define follow-up end date and construct survival data for long covid ##\n  #################################################################################\n  \n  ## To improve efficiency: keep only necessary variables\n  variables_to_keep <-c(\"patient_id\", \"out_first_long_covid_date\", \"vax_covid_date1\",\n                        \"vax_covid_date2\", \"death_date\", \"cohort_end_date\", \"index_date\",\n                        \"out_covid_date\",\n                        \"out_first_post_viral_fatigue_date_after_pandemic\")\n  \n  input_select <- input%>% dplyr::select(all_of(variables_to_keep))\n  \n  ####################################################################################\n  ## Cohort 1 - all eligible adults; Cohort 3: post-vaccination; Cohort 4: post-covid\n  ## Construct time to long COVID \n  ## lcovid_cens: indicator for long covid  \n  ## lcovid_surv: days from index date to long COVID, without censoring by vaccination \n  ## for the following cohorts\n  ####################################################################################\n  input_select <- input_select%>% rowwise() %>% mutate(fup_end_date=min(out_first_long_covid_date, \n                                                                        death_date, \n                                                                        cohort_end_date,\n                                                                        na.rm = TRUE))\n  input_select <- input_select %>% filter(fup_end_date >= index_date & fup_end_date != Inf)\n  input_select$lcovid_surv<- as.numeric(input_select$fup_end_date - input_select$index_date)+1\n\n  ## define event indicator, without censoring for vaccination\n  input_select <- input_select %>% mutate(lcovid_cens = ifelse((out_first_long_covid_date <= fup_end_date & \n                                                                  out_first_long_covid_date >= index_date &\n                                                                  !is.na(out_first_long_covid_date)), 1, 0)) %>%\n    ## define 2nd vaccination as an intervening event - a time-dependent variable\n    ## add 14 days as buffer to allow vaccination to take effects\n    mutate(vax2_surv = ifelse((vax_covid_date2 +14)>= index_date & \n                                (vax_covid_date2 + 14) <= fup_end_date &\n                                !is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage2_model_input_set_up.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n1-80\n1-80\n1-80\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,3,4,5,7],\"note\":\"Kept snippets that define key model and cohort parameters for the Cox analyses of coded long COVID: survival outcome setup, cohort and censoring rules (including pre-/post-vaccination and post-COVID cohorts), weighting, covariate selection, spline knot placement, and region stratification. Omitted snippets that focus on other outcomes (fatigue), downstream plotting, or external table assembly rather than parameter specification for the primary long COVID Cox models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "21",
    "dimension": "Inference Specification",
    "value": "# Purpose: to produce a contingency table to explore the relationship between gp_patient_interaction and smoking status\n# Programme by Yinghui Wei\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\nsource(\"analysis/functions/function_df_summary.R\")\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"           # all eligible population\n  #analysis <- \"all_vax_c\"    # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"   # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n## Analyses 1 and 4: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Analysis 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Analysis 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Analysis 6: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\ntbl_gp_interaction_smoking <- table(input$cov_cat_gp_patient_interaction, \n                                    input$cov_cat_smoking_status)\n\nresults <- chisq.test(tbl_gp_interaction_smoking)\n\ntbl_results <- data.frame(round(results$statistic,2), results$parameter,\n                          results$p.value, results$method)\n\ncolnames(tbl_results) <- c(\"test_statistic\", \"df\", \"P-value\", \"method\")\n\nwrite.csv(tbl_results, file=paste0(\"output/review/descriptives/table_gp_smoking_\", \n                                 analysis, \"_chi_square_test.csv\"), row.names = F )\n\nwrite.csv(tbl_gp_interaction_smoking, file=paste0(\"output/not_for_review/descriptives/table_gp_smoking_\", \n                                                   analysis, \".csv\"), row.names = F)\n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr(df_list_age_sex_adjusted, df_list_age_sex, \n                                           df_list_age_sex_categorical, csv_index,\n                                           cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Produce hazard ratio plot for predictors\n# Output:  \n\nlibrary(readr); library(dplyr)\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nwhich_model = \"full\"\n\n################################################################################\n# Part 1. Create a list of candidate predictors                                #\n################################################################################\n# input <- read_rds(\"output/input_stage1_all.rds\")\n# pred_name <- colnames(input)[grepl(\"cov_\",colnames(input))]\n# \n# hr_table <- data.frame(pred_name = pred_name, n_levels = NA)\n# for(i in hr_table$pred_name){\n#   print(table(input[,i]))\n#   if(grepl(\"_cat_\",i)){\n#   hr_table$n_levels[which(hr_table$pred_name==i)] = length(names(table(input[,i])))\n#   }\n# }\n# \n# n_rows = sum(hr_table$n_levels, na.rm = TRUE) + length(which(is.na(hr_table$n_levels) ==TRUE))\n# hr_table2 <- data.frame(pred_name = rep(NA, n_rows), pred_level = rep(NA, n_rows))\n# j=1\n# for(i in 1:nrow(hr_table)){\n#   print(hr_table[i,1])\n#   len = hr_table$n_levels[i]\n#   if(is.na(len)){\n#     len = 1\n#   }\n#   end = j+len-1\n#   hr_table2$pred_name[j:end] <- rep(hr_table$pred_name[i], len)\n#   if(len > 1){\n#   hr_table2$pred_level[j:end] = names(table(input[,hr_table$pred_name[i]]))\n#   }\n#   j = j+len\n# }\n# \n# hr_table2$term <- paste0(hr_table2$pred_name, \"=\", hr_table2$pred_level)\n# \nhr <- read_csv(file=paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis, \".csv\"))\n# \n# hr_combine <- full_join(hr_table2, hr, by = \"term\")\n# \n# hr <- hr_combine\n\nhr <- hr %>% dplyr::select(c(\"term\",\"hazard_ratio\", \"conf.low\", \"conf.high\")) %>%\n  # keep all characters before =\n  mutate(pred_name = sub(\"=.*\", \"\", term))  %>%\n  mutate(pred_name = sub(\"cov_cat_\", \"\", pred_name)) %>%\n  mutate(pred_name = sub(\"cov_num_\", \"\", pred_name)) %>%\n  # keep all characters after =\n  mutate(pred_level = sub(\".*=\", \"\", hr$term)) %>%\n  mutate(term = sub(\"cov_cat_\", \"\", term)) %>%\n  mutate(term = sub(\"cov_num_\", \"\", term)) %>%\n  mutate(term = sub(\"_\", \" \", term)) \n\nggplot(hr, aes(x = hazard_ratio, y = term)) +        # ggplot2 plot with confidence intervals\n  geom_point(shape=15, size = 3, color = \"darkblue\") +\n  geom_errorbar(aes(xmin = conf.low, xmax = conf.high), size=0.5) +\n  xlab(label='\\nHazard Ratio and 95% Confidence Interval')+\n  #\n  ylab(label='Predictorss\\n') +\n  geom_vline(xintercept = 1, linetype = 2, color = \"#E69F00\", size=1)",
    "source": "code",
    "evidence": {
      "code_path": "analysis/post_hoc.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v11_plot.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/dated code/stage6_hazard_ratio_plot_with_all_candidate_pred.R",
      "code_lines": "1-58\n61-140\n121-200\n1-78"
    },
    "llm_raw": "{\"keep_indices\":[5,7,8,9],\"note\":\"Kept snippets that explicitly implement inferential procedures or represent hazard ratios with 95% confidence intervals (chi-square test with p-values; HR tables and plots labeled as 95% CI). Other snippets focus on data setup/model specification without directly specifying inference criteria.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "21",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n  input$cov_cat_age_group <- factor(input$cov_cat_age_group, ordered = TRUE)\n  \n  ##RK - this was defined in stage 0 - why are you redefining it here?\n  ##YW - A good question, this is because the dummy data have some observations under 18, and by removing them,\n  ##     the number of levels in cov_cat_age_group has changed\n  \n  #################################################################################\n  ##Part 2. Define follow-up end date and construct survival data for long covid ##\n  #################################################################################\n  \n  ## To improve efficiency: keep only necessary variables\n  variables_to_keep <-c(\"patient_id\", \"out_first_long_covid_date\", \"vax_covid_date1\",\n                        \"vax_covid_date2\", \"death_date\", \"cohort_end_date\", \"index_date\",\n                        \"out_covid_date\",\n                        \"out_first_post_viral_fatigue_date_after_pandemic\")\n  \n  input_select <- input%>% dplyr::select(all_of(variables_to_keep))\n  \n  ####################################################################################\n  ## Cohort 1 - all eligible adults; Cohort 3: post-vaccination; Cohort 4: post-covid\n  ## Construct time to long COVID \n  ## lcovid_cens: indicator for long covid  \n  ## lcovid_surv: days from index date to long COVID, without censoring by vaccination \n  ## for the following cohorts\n  ####################################################################################\n  input_select <- input_select%>% rowwise() %>% mutate(fup_end_date=min(out_first_long_covid_date, \n                                                                        death_date, \n                                                                        cohort_end_date,\n                                                                        na.rm = TRUE))\n  input_select <- input_select %>% filter(fup_end_date >= index_date & fup_end_date != Inf)\n  input_select$lcovid_surv<- as.numeric(input_select$fup_end_date - input_select$index_date)+1\n\n  ## define event indicator, without censoring for vaccination\n  input_select <- input_select %>% mutate(lcovid_cens = ifelse((out_first_long_covid_date <= fup_end_date & \n                                                                  out_first_long_covid_date >= index_date &\n                                                                  !is.na(out_first_long_covid_date)), 1, 0)) %>%\n    ## define 2nd vaccination as an intervening event - a time-dependent variable\n    ## add 14 days as buffer to allow vaccination to take effects\n    mutate(vax2_surv = ifelse((vax_covid_date2 +14)>= index_date & \n                                (vax_covid_date2 + 14) <= fup_end_date &\n                                !is.na(vax_covid_date2) & \n                                ((vax_covid_date2+14) <= out_first_long_covid_date | is.na(out_first_long_covid_date)),\n                              as.numeric(vax_covid_date2 - index_date)+14, NA))\n  #RK - should line 139 vax_covid_date2 >= index_date instead be (vax_covid_date2 + 14) >= index_date otherwise you\n  #might not be inlcuding people who were vaccinated prior to index but by index were classed as vaccinated as the 14 days had\n  #passed by then\n  #YW - agree, and have changed to (vax_covid_date2 +14) >= index_date\n  ## Define survival data for eligible -----------------------------------------------------\n  if(cohort == \"all\"){\n    ################################################################################################\n    ## Cohort 2: pre-vaccination cohort - all eligible adults but censored by the first vaccination \n    ## lcovid_surv_vax_c: days from index date to long COVID, censored by the first vaccination \n    ## lcovid_cens_vax_c: indicator for long covid  \n    ###############################################################################################\n    input_select <- input_select %>% rowwise() %>% mutate(fup_end_date_vax_c=min(out_first_long_covid_date, \n                                                                                 vax_covid_date1,\n                                                                                 death_date, \n                                                                                 cohort_end_date,\n                                                                                 na.rm = TRUE))\n    input_select$lcovid_surv_vax_c<- as.numeric(input_select$fup_end_date_vax_c - input_select$index_date)+1",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n1-80\n121-200"
    },
    "llm_raw": "{\"keep_indices\":[0,3,4],\"note\":\"Kept snippets that define or modify variable codings or model contrast settings: (0) sets treatment contrasts for Cox models using covariates prefixed with cov_, (3) establishes naming conventions and recodes key variables (e.g., gp consultation, COVID infection indicator, COVID severity phenotype), and (4) explicitly recodes region factor reference level and derives categorical age groups from continuous age, directly documenting coding specifications used in the analyses.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "22",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n\"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+ cov_num_age\", \n  \"+ strat(sub_cat_region)\"\n)\n\nprint(\"Part 3: define survival analysis formula is completed!\")\n\n################################################################################\n# Part 4: Fit Cox model with long covid code as an outcome                  #\n################################################################################\n# age sex model, age splines\n\nfit_age_sex_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                         data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age splines, completed successfully!\")\n\n# age sex cox model, age linear\nfit_age_sex_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n                                        data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age linear, completed successfully!\")\n\n# full model, age splines\nfit_full_cox_model_splines <-rms::cph(formula= as.formula(surv_formula),\n                                      data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nwhich_model = paste0(outcome, \"_full_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age splines, completed successfully!\")\n\n# full model, age linear\nfit_full_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_lp),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_full_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age linear, completed successfully!\")\n# model selection\n\nfit_cox_model_selected <- fastbw(fit_full_cox_model_splines, sls=0.20)\n\nwhich_model = paste0(outcome, \"_selected_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"selected model:\")\nfit_cox_model_selected$names.kept\n\nselected_covariate_names <- fit_cox_model_selected$names.kept\n\n## Programmed by Yinghui Wei\n## Content: plot of long hazard ratio against continuous age\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nsource(\"analysis/stage2_model_input_set_up.R\")\ninput <- input %>% filter(input$cov_num_age<=100)\n\nsource(\"analysis/functions/function_HR_vs_age.R\")\nsplines_model <- read_rds(paste0(\"output/not_for_review/model/fit_cox_model_splines_\", analysis, \".rds\"))\n\nwrite.csv(table(input$cov_num_age),file=paste0(\"output/review/model/support_hr_vs_age_table_age_\", analysis, \".csv\"))\n\nfunction_figure_hr_vs_age(input, splines_model, analysis)\n\nfunction_figure_hr_vs_age <- function(input, fit_cox_model_splines, analysis){\n  dd <- datadist(input)\n  dd$limits$cov_num_age[2] <- 55\n  options(datadist = \"dd\")\n  pdata <- Predict(fit_cox_model_splines, cov_num_age, ref.zero = TRUE, fun = exp)\n  p <- ggplot(data = pdata)  +\n    geom_hline(aes(yintercept = 1), linetype = 3) +\n    xlab(\"\\nAge in years\\n\") + ylab(label = \"\\nHazard ratio (95% CI) compared to 55 years\\n\") \n  p_hr_vs_age <- p + coord_trans(y = \"log10\") + \n    scale_x_continuous(breaks = seq(20, 100, by = 20),limits=c(18,100)) +\n    scale_y_continuous(breaks = c(0.1, 0.5, 1, 2, 3),limits=c(0.1,3)) +\n    theme(axis.text = element_text(size =20),\n          axis.title = element_text(size =20),\n          panel.grid.major = element_blank(), \n          panel.grid.minor = element_blank(),\n          panel.background = element_blank(), \n          axis.line = element_line(colour = \"black\"))\n  \n  ggsave(file=paste0(\"output/review/model/figure_HR_vs_age_\",analysis, \".svg\"),\n         plot=p_hr_vs_age, width=15, height=10)\n}\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2022-11-02/\")\noutput_dir <- paste0(common_dir, \"2022-11-02/\")\n\ndat_age <- read.csv(paste0(results_dir, \"tbl_km_dat_age.csv\"))\ndat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n# Create cumulative incidence plot -----------------------------------------------------------\n#svglite::svglite(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence.svg\", width = 9, height = 5,)\n\nsvglite::svglite(paste0(results_dir,\"figures/km_incipende_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n    #limits = c(index_date, end_date), \n    date_breaks = \"6 months\", \n    date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(results_dir,\"figures/km_plot.svg\"), width = 10, height = 8)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\n# ggsave(file= \"km_plot.png\",\n#        path = paste0(output_dir, \"figures\"),\n#        plot=p2, width=10, height=8)\ndev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/figure_hr_vs_age.R\nanalysis/functions/function_HR_vs_age.R\nanalysis/externals/ext_figure_km.R",
      "code_lines": "1-80\n61-140\n1-15\n1-21\n1-79"
    },
    "llm_raw": "{\"keep_indices\":[5,6,4,8,1],\"note\":\"Selected snippets that directly implement the Cox proportional hazards models with age as a restricted cubic spline (test type and specification) and the Kaplan–Meier estimation by age group and sex. These include model formula definitions and fitting (5,6), spline-based hazard ratio vs age plotting (4,8), and KM plotting code (1).\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "22",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(\n               #limits = c(index_date, end_date), \n               date_breaks = \"6 months\", \n               date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n# Create cumulative probability plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_porobability_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Ploting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n  facet_grid(cols = vars(sex)) +\n  scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13),\n        panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n        panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n        strip.text = element_text(size = 13))\n\ndev.off()\n\n\"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+ cov_num_age\", \n  \"+ strat(sub_cat_region)\"\n)\n\nprint(\"Part 3: define survival analysis formula is completed!\")\n\n################################################################################\n# Part 4: Fit Cox model with long covid code as an outcome                  #\n################################################################################\n# age sex model, age splines\n\nfit_age_sex_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                         data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age splines, completed successfully!\")\n\n# age sex cox model, age linear\nfit_age_sex_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n                                        data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age linear, completed successfully!\")\n\n# full model, age splines\nfit_full_cox_model_splines <-rms::cph(formula= as.formula(surv_formula),\n                                      data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nwhich_model = paste0(outcome, \"_full_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age splines, completed successfully!\")\n\n# full model, age linear\nfit_full_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_lp),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_full_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age linear, completed successfully!\")\n# model selection\n\nfit_cox_model_selected <- fastbw(fit_full_cox_model_splines, sls=0.20)\n\nwhich_model = paste0(outcome, \"_selected_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"selected model:\")\nfit_cox_model_selected$names.kept\n\nselected_covariate_names <- fit_cox_model_selected$names.kept",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R",
      "code_lines": "1-80\n1-60\n1-80\n61-123\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,3,5,7,9],\"note\":\"Kept snippets that explicitly define or use variables in the Cox/KM analyses of coded long COVID with age (continuous via restricted cubic splines and categorical age groups) and key covariates (sex, region, etc.): indices 0 and 9 specify the survival outcome (lcovid_surv, lcovid_cens), age as spline vs linear, sex, other covariates, and stratification; indices 3, 5, and 7 construct Kaplan–Meier estimates by age group and sex for the long COVID outcome, matching the described age-group categorization (18–39, 40–59, 60–79, 80+/80–105). Other snippets focus on different outcomes (e.g., fatigue), external plotting, or pipeline wiring rather than core variable specification for this analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "22",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Programmed by Yinghui Wei\n## Content: plot of long hazard ratio against continuous age\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nsource(\"analysis/stage2_model_input_set_up.R\")\n\ninput <- input %>% filter(input$cov_num_age<=100)\n\n# Load data\nhr_est <- read.csv(paste0(\"output/review/model/HR_full_model_age_spline_\", analysis,\".csv\"))\nhr_est <- hr_est %>% filter(term == \"cov_num_age\" |  term == \"cov_num_age'\")\n\n# set age reference\nage_ref <- 55\n\n# output supporting document\nwrite.csv(table(input$cov_num_age),file=paste0(\"output/review/model/support_loghr_vs_age_table_age_\", analysis, \".csv\"))\n\n# compute restricted cubic splines\nc1 <- rms::rcs(input$cov_num_age,parms=knot_placement)\nc1 <- as.vector(c1[,2])\ncm <- rms::rcs(age_ref,parms=knot_placement)\ncm <- as.vector(cm[,2])\n\nb <- input$cov_num_age-age_ref\n\n# extract log HR\nest_a1 <- log(hr_est$hazard_ratio[1]) # loghr for age\nest_a2 <- log(hr_est$hazard_ratio[2]) # loghr for the second basis function\n\n# se for log HR\nse_logHR1 <- (est_a1 - log(hr_est$robust.conf.low[1]))/1.96\nse_logHR2 <- (est_a2 - log(hr_est$robust.conf.low[2]))/1.96\nvar_est_a1 <- se_logHR1^2\nvar_est_a2 <- se_logHR2^2\n# se_logHR1 <-log(hr_est$robust.se[1]) \n# se_logHR1 <-log(hr_est$robust.se[2]) \n\n# calculate log hazard ratio by age\nlog_hr_age <- est_a1*b+est_a2*(c1-cm)\n\nvar_log_hr_age <- var_est_a1*b^2 + var_est_a2*(c1-cm)^2\nse_log_hr_age <- sqrt(var_log_hr_age)\n\nlog_hr_age_low <- log_hr_age - 1.96 * se_log_hr_age\nlog_hr_age_high <- log_hr_age + 1.96 * se_log_hr_age\n\ndf<-data.frame(input$cov_num_age,log_hr_age, log_hr_age_low, log_hr_age_high)\n\n# output underlying data\nwrite.csv(df, file=paste0(\"output/review/model/table_loghr_age_\", \n                                 analysis, \".csv\"), row.names = F)\n\np = ggplot(df)+aes(input$cov_num_age,log_hr_age)+geom_line()+ \n  geom_hline(aes(yintercept = 0), linetype = 3)\n\np <- p +    scale_x_continuous(breaks = seq(20, 100, by = 20))  +\n  theme(axis.text = element_text(size =20),\n        axis.title = element_text(size =20),\n        panel.grid.major = element_blank(), \n        panel.grid.minor = element_blank(),\n        panel.background = element_blank(), \n        axis.line = element_line(colour = \"black\")) + \n  labs( x = '\\nAge in years', y = 'Log hazard ratio compared to age 55 years\\n')\n\nggsave(file=paste0(\"output/review/model/figure_loghr_age_\",analysis,\".svg\"), \n       plot=p, width=10, height=7)\n\n\np <- p + geom_ribbon(aes(ymin = log_hr_age_low, ymax = log_hr_age_high), alpha = 0.1) +\n  labs( x = '\\nAge in years', y = 'Log hazard ratio compared to age 55 years\\n')\n\nggsave(file=paste0(\"output/review/model/figure_loghr_age_ci_\",analysis,\".svg\"), \n       plot=p, width=10, height=7)\n\np <- p + geom_ribbon(aes(ymin = log_hr_age_low, ymax = log_hr_age_high), alpha = 0.1) +\n  labs( x = 'Age in years', y = 'Log hazard ratio compared to age 55 years')\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n\"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+ cov_num_age\", \n  \"+ strat(sub_cat_region)\"\n)\n\nprint(\"Part 3: define survival analysis formula is completed!\")\n\n################################################################################\n# Part 4: Fit Cox model with long covid code as an outcome                  #\n################################################################################\n# age sex model, age splines\n\nfit_age_sex_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex),\n                                         data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age splines, completed successfully!\")\n\n# age sex cox model, age linear\nfit_age_sex_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex),\n                                        data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_age_sex_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_age_sex_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: age sex Cox model - age linear, completed successfully!\")\n\n# full model, age splines\nfit_full_cox_model_splines <-rms::cph(formula= as.formula(surv_formula),\n                                      data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n\nwhich_model = paste0(outcome, \"_full_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_splines, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age splines, completed successfully!\")\n\n# full model, age linear\nfit_full_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_lp),\n                                     data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\nwhich_model = paste0(outcome, \"_full_linear\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"Part 4: full Cox model - age linear, completed successfully!\")\n# model selection\n\nfit_cox_model_selected <- fastbw(fit_full_cox_model_splines, sls=0.20)\n\nwhich_model = paste0(outcome, \"_selected_splines\")\noutput_file = paste0(\"output/review/model/hazard_ratio_estimates_\", which_model, \"_\", analysis)\ncox_output2(fit_full_cox_model_linear, which_model, output_file, save_output = TRUE)\n\nprint(\"selected model:\")\nfit_cox_model_selected$names.kept\n\nselected_covariate_names <- fit_cox_model_selected$names.kept",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/figure_log_hr_vs_age.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/dated code/stage3_long_covid_code_cox_model.R",
      "code_lines": "1-80\n1-80\n1-60\n61-140"
    },
    "llm_raw": "{\"keep_indices\":[0,3,5,8],\"note\":\"Kept snippets that directly specify parameters for the Cox spline age modeling and related age-parameterization: (0) and (8) define knot placement for restricted cubic splines, spline vs linear age terms, and full/age-sex Cox model formulas for long COVID; (3) computes and plots log hazard ratios over continuous age using spline coefficients and sets the age reference; (5) explicitly defines the categorical age groups (18–39, 40–59, 60–79, 80–105) used in analyses. Other snippets focus mainly on Kaplan–Meier plotting or on different outcomes (e.g., fatigue) and are less directly relevant to the requested parameter specification for the described long COVID Cox spline analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "22",
    "dimension": "Inference Specification",
    "value": "## Programmed by Yinghui Wei\n## Content: plot of long hazard ratio against continuous age\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\n\nsource(\"analysis/stage2_model_input_set_up.R\")\n\ninput <- input %>% filter(input$cov_num_age<=100)\n\n# Load data\nhr_est <- read.csv(paste0(\"output/review/model/HR_full_model_age_spline_\", analysis,\".csv\"))\nhr_est <- hr_est %>% filter(term == \"cov_num_age\" |  term == \"cov_num_age'\")\n\n# set age reference\nage_ref <- 55\n\n# output supporting document\nwrite.csv(table(input$cov_num_age),file=paste0(\"output/review/model/support_loghr_vs_age_table_age_\", analysis, \".csv\"))\n\n# compute restricted cubic splines\nc1 <- rms::rcs(input$cov_num_age,parms=knot_placement)\nc1 <- as.vector(c1[,2])\ncm <- rms::rcs(age_ref,parms=knot_placement)\ncm <- as.vector(cm[,2])\n\nb <- input$cov_num_age-age_ref\n\n# extract log HR\nest_a1 <- log(hr_est$hazard_ratio[1]) # loghr for age\nest_a2 <- log(hr_est$hazard_ratio[2]) # loghr for the second basis function\n\n# se for log HR\nse_logHR1 <- (est_a1 - log(hr_est$robust.conf.low[1]))/1.96\nse_logHR2 <- (est_a2 - log(hr_est$robust.conf.low[2]))/1.96\nvar_est_a1 <- se_logHR1^2\nvar_est_a2 <- se_logHR2^2\n# se_logHR1 <-log(hr_est$robust.se[1]) \n# se_logHR1 <-log(hr_est$robust.se[2]) \n\n# calculate log hazard ratio by age\nlog_hr_age <- est_a1*b+est_a2*(c1-cm)\n\nvar_log_hr_age <- var_est_a1*b^2 + var_est_a2*(c1-cm)^2\nse_log_hr_age <- sqrt(var_log_hr_age)\n\nlog_hr_age_low <- log_hr_age - 1.96 * se_log_hr_age\nlog_hr_age_high <- log_hr_age + 1.96 * se_log_hr_age\n\ndf<-data.frame(input$cov_num_age,log_hr_age, log_hr_age_low, log_hr_age_high)\n\n# output underlying data\nwrite.csv(df, file=paste0(\"output/review/model/table_loghr_age_\", \n                                 analysis, \".csv\"), row.names = F)\n\np = ggplot(df)+aes(input$cov_num_age,log_hr_age)+geom_line()+ \n  geom_hline(aes(yintercept = 0), linetype = 3)\n\np <- p +    scale_x_continuous(breaks = seq(20, 100, by = 20))  +\n  theme(axis.text = element_text(size =20),\n        axis.title = element_text(size =20),\n        panel.grid.major = element_blank(), \n        panel.grid.minor = element_blank(),\n        panel.background = element_blank(), \n        axis.line = element_line(colour = \"black\")) + \n  labs( x = '\\nAge in years', y = 'Log hazard ratio compared to age 55 years\\n')\n\nggsave(file=paste0(\"output/review/model/figure_loghr_age_\",analysis,\".svg\"), \n       plot=p, width=10, height=7)\n\n\np <- p + geom_ribbon(aes(ymin = log_hr_age_low, ymax = log_hr_age_high), alpha = 0.1) +\n  labs( x = '\\nAge in years', y = 'Log hazard ratio compared to age 55 years\\n')\n\nggsave(file=paste0(\"output/review/model/figure_loghr_age_ci_\",analysis,\".svg\"), \n       plot=p, width=10, height=7)\n\np <- p + geom_ribbon(aes(ymin = log_hr_age_low, ymax = log_hr_age_high), alpha = 0.1) +\n  labs( x = 'Age in years', y = 'Log hazard ratio compared to age 55 years')",
    "source": "code",
    "evidence": {
      "code_path": "analysis/figure_log_hr_vs_age.R",
      "code_lines": "1-80"
    },
    "llm_raw": "{\"keep_indices\":[5],\"note\":\"Snippet 5 computes log hazard ratios over age using the spline-based Cox model and explicitly derives 95% confidence intervals via ±1.96*SE, directly encoding the inference specification (normal approximation with alpha=0.05) for the log HR vs age curve described in the analysis.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "22",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/dated code/figure_cum_prob_km.R",
      "code_lines": "1-80\n1-80\n1-60"
    },
    "llm_raw": "{\"keep_indices\":[2,5,6],\"note\":\"Index 2 contains the core Cox proportional hazards model specification for long COVID, showing how age is coded as a continuous variable (cov_num_age) with restricted cubic splines and how covariates/contrasts are set. Indices 5 and 6 contain the coding of age-group categories (cov_cat_age_group) into 18–39, 40–59, 60–79, and 80+/80–105, plus sex coding, which are directly relevant to the age-group coding used in Kaplan–Meier and comparative age-group analyses. Other snippets mainly handle plotting or pipeline orchestration without adding additional coding-specification details for age or the hazard models.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "23",
    "dimension": "Test Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: to extract hazard ratios: 1) fully adjusted; 2) age-sex adjusted\n# Programmed by Yinghui Wei\n# Date: 2022-11-07\n\nlibrary(readr); library(dplyr); library(tidyverse); library(ggplot2); library(data.table)\nlibrary(stringr); library(grid); library(forestploter)\n\ncommon_dir = \"C:/Users/ywei3/University of Bristol/grp-EHR - Documents/Projects/long-covid-risk-factors/OS-outputs/\"\n\nresults_dir = paste0(common_dir, \"2023-04-15/\")\noutput_dir <- paste0(common_dir, \"2023-04-15/\")\n\ncohort = \"all\"\n#cohort = \"infected\"\n\n# km_plot <- function(results_dir, output_dir, cohort){\n  print(cohort)\n  dat_age <- read.csv(paste0(results_dir, file=paste0(\"tbl_km_dat_age_\",cohort,\".csv\")))\n  dat_age <- dat_age %>% mutate(x_date = as.Date(x_date))\n  # Create cumulative incidence plot -----------------------------------------------------------\n  svglite::svglite(paste0(results_dir,\"figures/km_incipence_plot_\", cohort,\".svg\"), width = 10, height = 8)\n  ggplot(dat_age, \n         aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n             linetype = cov_cat_age_group)) +\n    geom_path() + #Plotting\n    geom_line(aes(linetype=cov_cat_age_group),size=1)+\n    scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                   \"40-59\" = \"dotted\",\n                                   \"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(\n      #limits = c(index_date, end_date), \n      date_breaks = \"6 months\", \n      date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  dev.off()\n  \n  # Create cumulative probability plot -----------------------------------------------------------\n  svglite::svglite(paste0(results_dir, \"figures/km_plot_\",cohort,\".svg\"), width = 10, height = 8)\n  \n  ggplot(dat_age, \n         aes(x_date,y_prob, group = cov_cat_age_group, color=cov_cat_age_group, \n             linetype = cov_cat_age_group)) +\n    geom_path() + #Plotting\n    geom_line(aes(linetype=cov_cat_age_group),size=1)+\n    scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                   \"40-59\" = \"dotted\",\n                                   \"60-79\" = \"dotdash\",\n                                   \"80+\"   = \"solid\"))+\n    scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                  \"40-59\"=\"#FF8000\",\n                                  \"60-79\"= \"#B266FF\",\n                                  \"80+\" = \"red\"))+\n    labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative probability of long COVID code\\n\") +\n    labs(linetype='Age group', colour=\"Age group\") +\n    facet_grid(cols = vars(sex)) +\n    scale_x_date(date_breaks = \"6 months\", date_labels =  \"%b %Y\") +\n    theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n          legend.text=element_text(size=13),\n          axis.text = element_text(size = 13),\n          panel.grid.major = element_blank(), panel.grid.minor = element_blank(),\n          panel.background = element_blank(), axis.line = element_line(colour = \"black\"),\n          strip.text = element_text(size = 13))\n  \n  # ggsave(file= \"km_plot.png\",\n  #        path = paste0(output_dir, \"figures\"),\n  #        plot=p2, width=10, height=8)\n\n)\n  ),\n  # comment(\"Figure - Kaplan Meier plot\"),\n  # action(\n  #   name = \"figure_km_all\",\n  #   run = \"r:latest analysis/figure_kaplan_meier.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     plot_km = glue(\"output/review/descriptives/figure_kaplan_meier_*\"),\n  #     data_km = glue(\"output/review/descriptives/tbl_km_*\")\n  #   )\n  # ),\n  comment(\"Table - frequencies of snomed code for long covid diagnosis\"),\n  action(\n    name = \"table_snomed_codes_all\",\n    run = \"r:latest analysis/table_snomed_code.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      snomed_code = glue(\"output/not_for_review/descriptives/snomed_code*\")\n    )\n  ),\n  # comment(\"figure - long covid count by region\"),\n  # action(\n  #   name = \"figure_long_covid_region_all\",\n  #   run = \"r:latest analysis/figure_long_covid_count_region.R\",\n  #   needs = list(\"stage1_define_eligible_population_all\"),\n  #   moderately_sensitive = list(\n  #     long_covid_count_region = glue(\"output/not_for_review/descriptives/long_covid_count_*_region*\")\n  #   )\n  # ),\n  comment(\"Summarise survival data\"),\n  action(\n    name = \"summarise_survival_data_all\",\n    run = \"r:latest analysis/stage2_summarise_survival_data.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/descriptives/summarise_survival_data*\")\n    )\n  ),\n  comment(\"Part 4. Modelling\"),\n  comment(\"Development Cox model\"),\n  comment(\"post-viral fatigue\"),\n  action(\n    name = \"development_cox_model_fatigue\",\n    run = \"r:latest analysis/stage3_fatigue_survival_analysis.R\",\n    needs = list(\"stage1_define_eligible_population_all\"),\n    moderately_sensitive = list(\n      summary_survival_data = glue(\"output/review/model/analysis_data_summary_fatigue*\"),\n      cox_model_output = glue(\"output/review/model/HR_fatigue*\")\n    )\n  ),\n  # long COVID code\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex(analysis = x)), recursive = FALSE)\n  ),\n  # Age sex adjsuted Cox model\n  splice(\n    unlist(lapply(analysis, function(x) apply_development_cox_model_age_sex_adjusted(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Subset variables\"),\n  action(\n    name = \"identify_subset_variables\",\n    run = \"r:latest analysis/stage3_identify_subset_variables.R\",\n    needs = list(\"development_cox_model_all\", \"development_cox_model_all_vax_c\",\n                 \"development_cox_model_all_vax_td\", \"development_cox_model_vaccinated\",\n                 \"development_cox_model_infected\"),\n    moderately_sensitive = list(\n      subset_variables = glue(\"output/review/model/selected_vars.csv\")\n    )\n  ),\n  splice(\n    unlist(lapply(analysis, function(x) apply_cox_model_subset_variables(analysis = x)), recursive = FALSE)\n  ),\n  comment(\"Figure - log hazard ratio against continuous age\"),\n  splice(\n    unlist(lapply(analysis, function(x) apply_figure_loghr_age(analysis = x)), recursive = FALSE)\n\n\"HR_age_sex_categorical_vaccinated.csv\", \"HR_age_sex_categorical_infected.csv\")\n\nfile_list <- c(file_name1, file_name2, file_name3, file_name4, file_name5)\n\n\n#----------------------------------------------------------------------------------------------------------------------\n\ndf_list_full <- list(# full model with age splines\n  HR_full_model_age_spline_all.csv,         # primary\n  HR_full_model_age_spline_all_vax_c.csv,   # pre-vax\n  HR_full_model_age_spline_vaccinated.csv,  # post-vax\n  HR_full_model_age_spline_infected.csv     # post-covid\n)\n\ndf_list_full_categorical <- list(# full model with age categorical\n  HR_full_model_categorical_all.csv,          # primary\n  HR_full_model_categorical_all_vax_c.csv,    # pre-vax\n  HR_full_model_categorical_vaccinated.csv,   # post-vax\n  HR_full_model_categorical_infected.csv     # post-covid\n)\n\ndf_list_age_sex_adjusted <- list(\n  # age-sex adjusted model\n  HR_age_sex_adjusted_all.csv,        # primary\n  HR_age_sex_adjusted_all_vax_c.csv,  # pre-vax\n  HR_age_sex_adjusted_vaccinated.csv, # post-vax\n  HR_age_sex_adjusted_infected.csv   # post-covid\n)\n\ndf_list_age_sex <-list(\n  # age-sex model with age splines\n  HR_age_sex_model_all.csv,        # primary\n  HR_age_sex_model_all_vax_c.csv,  # pre-vax\n  HR_age_sex_model_vaccinated.csv, # post-vax\n  HR_age_sex_model_infected.csv   # post-covid\n)\ndf_list_age_sex_categorical <- list(\n  # age-sex model with categorical age\n  HR_age_sex_model_categorical_all.csv,        # primary\n  HR_age_sex_model_categorical_all_vax_c.csv,  # pre-vax\n  HR_age_sex_model_categorical_vaccinated.csv, # post-vax\n  HR_age_sex_model_categorical_infected.csv   # post-covid\n) \n\ninfected_cohort <- grep(\"infected\",file_list)\ntd_analysis <- grep(\"td\", file_list)\n\n################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\n\n## Programmed by Yinghui Wei\n## Purpose: to fit Cox model: age and sex adjusted and add one other predictor at a time\n\nlibrary(readr)\nsource(\"analysis/stage2_model_input_set_up.R\")\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_model_evaluation.R\")\n\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\n\ncombined_results_hr <- data.frame()\ncombined_results_pm <- data.frame()\n################################################################################\n## Part 1: Extract names for factors and numerical predictors                 ##\n################################################################################\ncov_factor_names <- names(input)[grepl(\"cov_cat\", names(input))]\ncov_num_names <- names(input)[grepl(\"cov_num\", names(input))]\n\nif(analysis != \"infected\"){\ncov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\",\n                                                              \"cov_cat_covid_phenotype\")] # remove sex and vaccination status\n}else{\n  cov_factor_names <- cov_factor_names[!cov_factor_names %in% c(\"cov_cat_sex\",\"cov_cat_ie.status\")] # remove sex and vaccination status\n}\ncov_num_names <- cov_num_names[! cov_num_names %in% \"cov_num_age\"] # remove age\n\ncov_names <- c(cov_factor_names, cov_num_names)\n\n################################################################################\n## Part 2: Assess if non-linear term is needed for continuous age             ##\n##         and redefine the survival analysis formula                         ##\n################################################################################\nfor(i in 1:length(cov_names)){\n  surv_formula_age_spl_sex_adjusted <- paste0(surv_formula_age_spl_sex,\"+\", cov_names[i])\n  surv_formula_age_linear_sex_adjusted <- paste0(surv_formula_age_linear_sex,\"+\", cov_names[i])\n  # age as splines\n  fit_cox_model <- fit_cox_model_splines <-rms::cph(formula= as.formula(surv_formula_age_spl_sex_adjusted),\n                                   data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  \n  # # age as linear predictor\n  # fit_cox_model_linear <-rms::cph(formula= as.formula(surv_formula_age_linear_sex_adjusted),\n  #                                 data= input, weight=input$weight,surv = TRUE,x=TRUE,y=TRUE)\n  # \n  # # a crude comparison\n  # if(AIC(fit_cox_model_linear) <= AIC(fit_cox_model_splines)){\n  #   surv_formula = surv_formula_age_linear_sex\n  #   #surv_formula_predictors = surv_formula_predictors_lp\n  #   fit_cox_model <- fit_cox_model_linear\n  #   print(\"Linear term is selected for age!\")\n  # } else{\n  #   surv_formula <- surv_formula_age_spl_sex\n  #   fit_cox_model <- fit_cox_model_splines\n  #   print(\"Restricted cubic splines is selected for age!\")\n  # }\n  # print(\"The selected model is\")\n  # print(fit_cox_model)\n  ################################################################################\n  # Part 3: Output results from the Cox Model                                    #\n  ################################################################################\n  which_model = cov_names[i]\n  output_file = paste0(\"output/review/model/HR_age_sex_adjusted_\", which_model, \"_\", analysis)\n  results <- cox_output2(fit_cox_model, which_model, output_file, save_output=FALSE)\n  results$model <- \"age_sex_adjusted\"\n  results$predictor <- cov_factor_names[i]\n  combined_results_hr <- rbind(combined_results_hr, results)\n  # saveRDS(fit_cox_model, file=paste0(\"output/not_for_review/model/cox_as_adj_\", cov_names[i],\"_\", analysis,\".rds\"))\n  # \n  ################################################################################\n  ## Part 4: Model evaluation                                                   ##\n  ################################################################################\n  subset_vars = \"age_sex_adjusted_\"\n  which_model = cov_names[i]\n  results_pm <- function_model_evaluation(input,fit_cox_model, which_model, analysis, subset_vars, graphics_output=FALSE, save_output=FALSE)\n  results_pm$predictors = cov_names[i]\n  print(\"Finished model valuation for age and sex adjusted model!\")\n  combined_results_pm <- rbind(combined_results_pm, results_pm)\n}\n\n## Output hazard ratio estimates",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/externals/ext_figure_km_v2.R\nanalysis/create_project_actions.R\nanalysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v13_plot.R\nanalysis/stage3_4_model_dev_eval_age_sex_adjusted.R",
      "code_lines": "1-80\n1-80\n481-560\n61-140\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[1,2,4,7,8],\"note\":\"Kept snippets that define or run Cox proportional hazards models and/or Kaplan–Meier-based survival analyses relevant to the test specification (hazard ratios and KM by age group). Index 1 sets up Cox model survival formulas for long COVID; 2 generates Kaplan–Meier plots by age group and sex; 4 defines project actions that execute Cox modeling and log hazard ratio vs age figures; 7 combines hazard ratio outputs from full and age–sex models including versions with categorical age; 8 fits age–sex adjusted Cox models and outputs hazard ratios, directly reflecting the specified test type. Other snippets were excluded as they were either purely plotting precomputed values without clear model specification, focused on different outcomes (e.g., fatigue), or only lightly touched on hazard ratios without substantial executable modeling code.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "23",
    "dimension": "Variable Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: Prepare variables\n## Output:  input_stage0.rds; table_0.csv; table_0.html\n\n##Variable naming principles: \n## 1. prefix \"cov_cat_\" indicate categorical covariates\n## 2. prefix \"cov_num_\" indicate numerical covariates\n## 3. prefix \"out_\" indicate outcome variables\n## 4. containing \"_date\" indicate date variables\n## 5. prefix \"sub_\" indicate patient characteristics which are not included\n##    in the analysis / modelling, but are of interest in exploration\n\nsource(\"analysis/functions/redactor2.R\")\nlibrary(readr); library(dplyr); library(\"arrow\"); library(lubridate); library(tidyr)\n\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\nstage0_data_cleaning <- function(cohort){\n  input <- read_feather(paste0(\"output/input_\", cohort, \".feather\"))\n  \n  # remove ra-sle-psoriasis because they have been included individually\n  input <- input %>% dplyr::select(-cov_cat_ra_sle_psoriasis) %>%\n    rename(cov_num_gp_patient_interaction = cov_num_gp_consultation)\n  \n  # Remove Healthcare Workers \n  input <- input %>% select(!cov_cat_healthcare_worker)\n  \n  ################################################################################\n  ## Part 1. define index date and remove variables, specify date variable       #\n  ################################################################################\n  ## define cohort start date:\n  index_date=as.Date(\"2020-01-29\")\n  #RK - in your study defs you've set index date as \"2020-01-29\"? Does this need to change?\n  #YW response - this is now changed to 2020-01-29\n  input$index_date = as.Date(index_date)\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  study_days = cohort_end - index_date\n  input$cohort_end_date = cohort_end\n  \n  # remove potential recording errors in the EHR data\n  # remove if covid date is before pandemic start \"2020-01-29\"\n  # Here index_date = \"2020-01-29\" though index_date is set to be cohort specific later\n  input <- input %>% filter(is.na(out_covid_date) |out_covid_date >= index_date) \n  \n  # remove if long covid date is before pandemic start \"2020-01-29\"\n  input <- input %>% filter(is.na(out_first_long_covid_date) |out_first_long_covid_date >= index_date) \n  \n  if(cohort == \"vaccinated\"){\n    # select people who were vaccinated during the study period\n    input <- input %>% filter(!is.na(vax_covid_date2) &vax_covid_date2 >= index_date & vax_covid_date2 <= cohort_end_date)\n    # reset index date to be 14 days following the second dose of vaccine\n    input$index_date = input$vax_covid_date2 + 14 \n  }\n  if(cohort == \"infected\"){\n    # select people who were infected during the study period\n    input <- input %>% filter(!is.na(out_covid_date) &out_covid_date>= index_date & out_covid_date <= cohort_end_date)\n    # reset index date to be the date of covid infection\n    input$index_date = input$out_covid_date\n  }\n  \n  ## Step 1. Define variables: COVID infection-------------------------------------\n  ## create an indicator variable for covid infection\n  input$out_covid <- ifelse(is.na(input$out_covid_date), FALSE, TRUE)\n  \n  ## Define COVID-19 phenotype / severity ---------------------------------------------------\n  ## three categories: no infection, non-hospitalised covid and hospitalised covid\n  ## this variable is not included in the Cox model unless it is the infected cohort of interest\n  ## Hospitalized COVID defined as admitted to hospitals within 28 days following COVID positive test\n  input$sub_cat_covid_phenotype <- \"no_infection\"\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: define eligible population\n## Output:  input_stage1_all.rds; input_stage1_vaccinated.rds; input_stage1_infected.rds\n\nlibrary(readr); library(dplyr); library(htmlTable)\nsource(\"analysis/functions/redactor2.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  cohort <- \"infected\"     # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\n\nstage1_eligibility <- function(cohort){\n  input <- read_rds(paste0(\"output/input_stage0_\", cohort, \".rds\"))\n  ## Specify study period\n  cohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\n  ## YW: in some cohort the cohort start date is different from the index date\n  ## for the vaccinated population and infected population, their index dates were 2nd vaccination + 14 days, and first covid dates during the follow-up\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  input$cohort_end_date = cohort_end\n  ##RK - I would move any hard coded dates to to the top of the script so you don't have to be searching through\n  ##scripts to find/check what dates you've been using\n  ##YW: this has now been moved to the top of the script\n  ################################################################################\n  ## Part 1. Define eligible population                                          #\n  ################################################################################\n  \n  steps <- c(\"starting point\",\"dead before index date\", \"missing region\", \"missing sex\", \n             \"missing age\", \"age <18y\", \"age>105y\",  \n             \"covid before index date\", \"long covid before index date\")\n  \n  ## starting point\n  flow_chart_n <- nrow(input)\n  \n  ## Dead: removed if dead before index date\n  input <- input%>%filter(death_date > index_date | is.na(death_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n\n  ## Region: remove if missing\n  ##input <- input%>%filter(!is.na(cov_cat_region))\n  input <- input%>%filter(cov_cat_region!=\"Missing\") # keep if not missing\n  table(input$cov_cat_region)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ##RK - in stage 0 you set NA's to 'Missing' so do you want to be removing 'Missing' instead?\n  ##YW - this is now changed to remove the \"Missing\" category for region and sex\n  ## Sex: remove if missing\n  input <- input%>%filter(cov_cat_sex!=\"Missing\")\n  table(input$cov_cat_sex)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/stage0_data_cleaning.R\nanalysis/stage1_define_eligible_population.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/figure_kaplan_meier_cohorts.R",
      "code_lines": "1-80\n1-80\n1-80\n1-60\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,1,6,8,9],\"note\":\"Selected snippets define and operationalize key variables and their roles in time-to-event models relevant to the described Cox analyses. Index 0 specifies the survival outcome and covariates, including age as a spline, in Cox PH formulas. Index 1 prepares and names covariates (distinguishing outcomes, numeric vs categorical predictors) and sets up key dates and COVID-related variables. Index 6 defines eligibility criteria using age, establishes cov_num_age, and constrains the analytic age range. Indices 8 and 9 use the categorical age variable (18–39, 40–59, 60–79, 80+/80–105) as a predictor in survival/Kaplan–Meier analyses, directly reflecting the age-group specification mentioned in the analysis description.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "23",
    "dimension": "Parameter Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status\n\n## Purpose: Long COVID risk factors and prediction models\n## Author:  Yinghui Wei\n## Content: define eligible population\n## Output:  input_stage1_all.rds; input_stage1_vaccinated.rds; input_stage1_infected.rds\n\nlibrary(readr); library(dplyr); library(htmlTable)\nsource(\"analysis/functions/redactor2.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  #cohort <- \"all\"           # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  cohort <- \"infected\"     # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ncohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\ncohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\ncohort_days = cohort_end - cohort_start + 1\n\nstage1_eligibility <- function(cohort){\n  input <- read_rds(paste0(\"output/input_stage0_\", cohort, \".rds\"))\n  ## Specify study period\n  cohort_start = as.Date(\"2020-01-29\", format=\"%Y-%m-%d\") # this is the same as the index date column - do you need both?\n  ## YW: in some cohort the cohort start date is different from the index date\n  ## for the vaccinated population and infected population, their index dates were 2nd vaccination + 14 days, and first covid dates during the follow-up\n  cohort_end = as.Date(\"2022-03-31\", format=\"%Y-%m-%d\")\n  input$cohort_end_date = cohort_end\n  ##RK - I would move any hard coded dates to to the top of the script so you don't have to be searching through\n  ##scripts to find/check what dates you've been using\n  ##YW: this has now been moved to the top of the script\n  ################################################################################\n  ## Part 1. Define eligible population                                          #\n  ################################################################################\n  \n  steps <- c(\"starting point\",\"dead before index date\", \"missing region\", \"missing sex\", \n             \"missing age\", \"age <18y\", \"age>105y\",  \n             \"covid before index date\", \"long covid before index date\")\n  \n  ## starting point\n  flow_chart_n <- nrow(input)\n  \n  ## Dead: removed if dead before index date\n  input <- input%>%filter(death_date > index_date | is.na(death_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n\n  ## Region: remove if missing\n  ##input <- input%>%filter(!is.na(cov_cat_region))\n  input <- input%>%filter(cov_cat_region!=\"Missing\") # keep if not missing\n  table(input$cov_cat_region)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ##RK - in stage 0 you set NA's to 'Missing' so do you want to be removing 'Missing' instead?\n  ##YW - this is now changed to remove the \"Missing\" category for region and sex\n  ## Sex: remove if missing\n  input <- input%>%filter(cov_cat_sex!=\"Missing\")\n  table(input$cov_cat_sex)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R\nanalysis/stage1_define_eligible_population.R",
      "code_lines": "1-80\n1-80\n1-60\n1-80\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,3,4,8,9],\"note\":\"Kept snippets that define the core parameterisation of the long COVID analyses: (0) specifies Cox model formulas and age handling (spline vs linear) for long COVID; (3) and (4) define the categorical age groups (18–39, 40–59, 60–79, 80+/80–105) used in survival/KM analyses; (8) sets up the long COVID survival dataset including cohort definitions, case–control sampling ratio, and inverse probability weights; (9) specifies eligibility criteria and key parameters such as age range (18–105) and study period. Other snippets focus on plotting, external HR table assembly, or non–long-COVID outcomes (e.g., fatigue), so were excluded.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "23",
    "dimension": "Inference Specification",
    "value": "################################################################################\n# Part 1. HR from fully adjusted models                                        #\n################################################################################\n#-------------------------------------------------------------------------------\n# Primary cohort\n#-------------------------------------------------------------------------------\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_primary <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_primary <- function_HR_df_v2(df_hr_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                    cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Pre-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 2\ncohort = \"Pre-vaccination\"\ndf_hr_prevax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_prevax <- function_HR_df_v2(df_hr_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                   cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-vaccination cohort\n#-------------------------------------------------------------------------------\ncsv_index = 3\ncohort = \"Post-vaccination\"\ndf_hr_vax <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vax <- function_HR_df_v2(df_hr_vax, csv_hr_order=\"hr_fixed2.csv\",\n                                cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Post-COVID cohort\n#-------------------------------------------------------------------------------\ncsv_index = 4\ncohort = \"Post-COVID\"\ndf_hr_infected <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_infected <- function_HR_df_v2(df_hr_infected, csv_hr_order=\"hr_fixed2.csv\",\n                                     cohort = cohort, common_dir = common_dir)\n\n#-------------------------------------------------------------------------------\n# Primary cohort: Vaccination time-dependent\n#-------------------------------------------------------------------------------\ncsv_index = 5\ncohort = \"Vaccination time-dependent\"\ndf_hr_vaxtd <- function_combine_hr(df_list_full, df_list_full_categorical, csv_index, cohort)\ntbl_hr_vaxtd <- function_HR_df_v2(df_hr_vaxtd, csv_hr_order=\"hr_fixed2.csv\",\n                                  cohort = cohort, common_dir = common_dir)\n\n################################################################################\n# Part 2. HR from age-sex adjusted models                                      #\n################################################################################\n# Primary cohort\ncsv_index = 1\ncohort = \"Primary\"\ndf_hr_as_primary <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                              df_list_age_sex_categorical, csv_index,\n                                              cohort)\ntbl_hr_as_primary <- function_HR_df_v2(df_hr_as_primary, csv_hr_order=\"hr_fixed2.csv\",\n                                       cohort = cohort, common_dir = common_dir)\n\ntbl_hr_as_primary$age_sex_aHR = tbl_hr_as_primary$`HR (95% CI)`\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_primary <- tbl_hr_as_primary %>% rename(as_hr = hazard_ratio) %>%\n  rename(as_hr_low = conf.low) %>%\n  rename(as_hr_high = conf.high)\ntbl_hr_as_primary <- tbl_hr_as_primary %>% select(c(row.num, age_sex_aHR, as_hr, as_hr_low, as_hr_high))\ntbl_hr_primary_combined <- merge(tbl_hr_primary, tbl_hr_as_primary, by=\"row.num\", all.x=T)\n\n# Pre-vaccination cohort\ncsv_index = 2\ncohort = \"Pre-Vaccination\"\ndf_hr_as_prevax <- function_combine_as_hr_v2(df_list_age_sex_adjusted, df_list_age_sex, \n                                             df_list_age_sex_categorical, csv_index,\n                                             cohort)\ntbl_hr_as_prevax <- function_HR_df_v2(df_hr_as_prevax, csv_hr_order=\"hr_fixed2.csv\",\n                                      cohort = cohort, common_dir = common_dir)\ntbl_hr_as_prevax$age_sex_aHR = tbl_hr_as_prevax$`HR (95% CI)`\n\n# rename hazard ratio, lower and upper bounds of the 95%CI\ntbl_hr_as_prevax <- tbl_hr_as_prevax %>% rename(as_hr = hazard_ratio) %>%\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()",
    "source": "code",
    "evidence": {
      "code_path": "analysis/externals/dated-code/ext_figure_hazard_ratio_read_data_for_v12_plot.R\nanalysis/dated code/figure_cum_prob_km.R",
      "code_lines": "121-200\n1-60"
    },
    "llm_raw": "{\"keep_indices\":[5,9],\"note\":\"Kept snippets that explicitly reflect inference specifications: index 5 processes hazard ratios with 95% confidence intervals (HR (95% CI), conf.low, conf.high) from Cox models across cohorts; index 9 fits Kaplan–Meier survival curves using survfit with a specified confidence type (conf.type = 'log'), indicating the form of confidence intervals used for cumulative probability estimates. Other snippets involve model setup or plotting without explicitly specifying inference criteria or CI settings.\"}"
  },
  {
    "paper_id": "wei_etal_2025",
    "analysis_id": "23",
    "dimension": "Coding Specification",
    "value": "# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: input set up, define survival formula through variables selection and AIC\n# Output:  survival formula for selected model\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\nsource(\"analysis/functions/function_cox_output.R\")\nsource(\"analysis/functions/function_df_summary.R\")\n\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  #analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\n################################################################################\n# Part 1: load data, define inverse probability weighting                      #\n################################################################################\ninput <- read_rds(paste0(\"output/input_stage2_\", analysis, \".rds\"))\ncovariate_names <- names(input)[grep(\"cov_\", names(input))]\n\ncovariate_names <- covariate_names[-grep(\"age\", covariate_names)]\n\nprint(\"candidate predictors\")\nprint(covariate_names)\n\n################################################################################\n# Part 2: number of people in each covariate level                             #\n################################################################################\n\nfunction_df_summary(input, analysis)\n\n## set up before using rms::cph\ndd <<- datadist(input) #\noptions(datadist=\"dd\", contrasts=c(\"contr.treatment\", \"contr.treatment\")) #\n\n################################################################################\n# Part 3: define survival analysis formula                                     #\n################################################################################\n## linear predictors + a restricted cubic spline for age + \n##  a restricted cubic spline for gp consultation rate + clustering effect for practice\nknot_placement=as.numeric(quantile(input$cov_num_age, probs=c(0.1,0.5,0.9)))\n\n## Age sex model, age as sline\nsurv_formula_age_spl_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\",\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## Age sex model, age linear\nsurv_formula_age_linear_sex <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  \"cov_cat_sex\", \"+ cov_num_age\", \"+ strat(sub_cat_region)\"\n)\n\n## Full model, age spline\nsurv_formula <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n  paste(covariate_names, collapse = \"+\"),\n  \"+rms::rcs(cov_num_age,parms=knot_placement)\", \n  \"+ strat(sub_cat_region)\"\n)\n\n## full model: age linear\nsurv_formula_lp <- paste0(\n  \"Surv(lcovid_surv, lcovid_cens) ~ \",\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer);library(tidyverse); library(ggplot2)\nsource(\"analysis/functions/function_round_km.R\")\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"descriptives\"))\n\nargs <- commandArgs(trailingOnly=TRUE)\n\nif(length(args)==0){\n  cohort <- \"all\"            # all eligible population\n  #cohort <- \"vaccinated\"    # vaccinated population\n  #cohort <- \"infected\"       # infected population\n}else{\n  cohort <- args[[1]]\n}\n\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% dplyr::select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80+\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\n\nindex_date=as.Date(\"2020-01-29\")\nend_date = as.Date(\"2022-03-31\")\n# Define a function to extract KM data ---------------------------------------------------------\nfunction_km_data <-function(input_sex, index_date){\n  dat_age <- round_km(input_sex, \"lcovid_surv\", \"lcovid_cens\", \"cov_cat_age_group\", threshold=6)\n  dat_age$x <- dat_age$y <- dat_age$y_prob <-NA\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    dat_age$x[which(dat_age$cov_cat_age_group ==i)] <- seq(length=length(which(dat_age$cov_cat_age_group ==i))) #Add case numbers (in order, since sorted)\n    dat_age$y[which(dat_age$cov_cat_age_group ==i)] <- cumsum(replace_na(dat_age$n.event[which(dat_age$cov_cat_age_group ==i)], 0))\n  }\n  for(i in c(\"18-39\", \"40-59\",\"60-79\", \"80+\")){\n    index = which(dat_age$cov_cat_age_group==i)\n    dat_age_select <- dat_age %>% filter(cov_cat_age_group == i)\n    dat_age_select$y_prob <- NA\n    total = dat_age_select$n.risk[1]\n    dat_age_select$y_prob = dat_age_select$y/total\n    dat_age$y_prob[index] <- dat_age_select$y_prob\n  }\n  dat_age$x_date = index_date + dat_age$time\n # dat_age$x_date = index_date + dat_age$x\n dat_age <- dat_age %>% filter(n.risk >=0)\n  return(dat_age)\n}\n# Male: Create KM data -----------------------------------------------------------------\ninput_sex_m <- input %>% filter(cov_cat_sex == \"M\")\ndat_age_m <- function_km_data(input_sex_m, index_date)\ndat_age_m$sex <- \"Male\"\n\n# Female: Create KM data ----------------------------------------------------------------\ninput_sex_f <- input %>% filter(cov_cat_sex == \"F\")\ndat_age_f <- function_km_data(input_sex_f, index_date) \ndat_age_f$sex <- \"Female\"\ndat_age <- rbind(dat_age_m, dat_age_f)\n# Create a supporting document to show evidence of rounding for small number suppression control\nwrite.csv(dat_age,paste0(\"output/review/descriptives/tbl_km_dat_age_\", cohort, \".csv\"), row.names=F)\n# Create cumulative incidence plot -----------------------------------------------------------\nsvglite::svglite(paste0(\"output/review/descriptives/figure_kaplan_meier_age_sex_cum_incidence_\", cohort,\".svg\"), \n                 width = 9, height = 5)\n\nggplot(dat_age, \n       aes(x_date,y, group = cov_cat_age_group, color=cov_cat_age_group, \n           linetype = cov_cat_age_group)) +\n  geom_path() + #Plotting\n  geom_line(aes(linetype=cov_cat_age_group),size=1)+\n  scale_linetype_manual(values=c(\"18-39\" = \"longdash\",\n                                 \"40-59\" = \"dotted\",\n                                 \"60-79\" = \"dotdash\",\n                                 \"80+\"   = \"solid\"))+\n  scale_color_manual(values = c(\"18-39\" = \"#808080\",\n                                \"40-59\"=\"#FF8000\",\n                                \"60-79\"= \"#B266FF\",\n                                \"80+\" = \"red\"))+\n  labs(title=\"\",x=\"\\nDate\", y = \"\\nCumulative incidence of long COVID code\\n\") +\n  labs(linetype='Age group', colour=\"Age group\") +\n\n## Age: remove if missing\n  input <- input%>%filter(!is.na(cov_num_age))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age < 18\n  input <- input%>%filter(cov_num_age>=18)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## Adult: remove if age > 105 years\n  input <- input%>%filter(cov_num_age <=105)\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n   \n  ## make age at 10 years increment \n  # input <- input %>% \n  #   rename(sub_num_age = cov_num_age) %>% # rename to keep in exploration but not to include in the model\n  #   mutate(cov_num_age = sub_num_age/10) \n  #YW 26/08/2022 no need to be at 10 year increment because we will present HR for categorical age\n  #   and log HR against continuous age\n  \n  ### Ethnicity: remove if missing\n  ## input <- input%>%filter(!is.na(cov_cat_ethnicity))\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  ##RK - same as for region, you set NA values to \"Missing or Other\" so what are you trying to remove?\n  ##Do you want to remove people with missing ethnicity or just checking for NA values because there shouldn't be any\n  ##and if there are any NA values then something has gone wrong in a previous step?\n  ##YW response - You are right! Missing ethnicity is incorporated as a category, and it is also\n  ## no supposed to be removed. I have commented these out\n  \n  ## COVID history\n  input <- input%>%filter(!(sub_cat_covid_history==TRUE))\n  input<- input%>% mutate(sub_cat_covid_history = as.character(sub_cat_covid_history)) %>%\n    mutate(sub_cat_covid_history = as.factor(sub_cat_covid_history))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  table(input$sub_cat_covid_history)\n  ##RK - copied from stage 0 comment: should sub_cat_covid_history be a TRUE/FALSE variable - I'm not sure that 'Missing' makes sense?\n  ##This is a date variable so if they don't have a date, its not that they're missing information (as you don't expect\n  ##the whole population to have a covid date), they just never had covid so should they actually be FALSE?\n  ##YW 2022/08/13: sub_cat_covid_history has been defined as a TRUE/FALSE variable in stage0 script\n  \n  ###table(input$cov_cat_previous_covid)\n  ## input <- input%>%filter(sub_cat_previous_covid == \"No COVID code\")\n  ## flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ### for individuals whose first long covid date is after cohort end date, set first long covid date as na\n  ## index <- which(input$out_first_long_covid_date > input$cohort_end_date)\n  ## input$out_first_long_covid_date[index] <- NA\n \n  ## sum(!is.na(input$out_first_long_covid_date))\n  \n  ##RK - fup_end_date is defined later in this script and hasn't been defined yet so index just returns\n  ##empty\n  ##YW response 2022/08/13: You are right, though now I think setting out_first_long_covid_date outside\n  ## cohort period is not needed, as the fup_end_date is defined later to compute the survival time.\n  ## So I have commented these out.\n  \n  ## previous long covid\n  ##RK- in your study defs your index date is \"2020-01-29\"? Does this need to be changed? If you want to use any date variables\n  ##it might be better to have them at the top of the script to make it easier to check which dates are being used?\n  ##Should the below criteria be for individual index date rather than cohort start date?\n  ##YW - the cohort start date is now changed to \"2020-01-29\", \n  ##also the specification of cohort start/end dates have now been moved to the top of the script\n  \n  input <- input%>%filter(out_first_long_covid_date >= index_date |\n                            is.na(out_first_long_covid_date))\n  flow_chart_n <- c(flow_chart_n, nrow(input))\n  \n  ## redefine factor level for region since NA is now removed\n  input <- input %>% mutate(cov_cat_region = as.character(cov_cat_region)) %>%\n    mutate(cov_cat_region = as.factor(cov_cat_region))\n  input$cov_cat_region <- relevel(input$cov_cat_region, ref = \"East\")\n  ##RK - need to check whether you want to be removing those who have region set to 'Missing'\n  ##as by this script no one should have any NA values in region\n  ##YW: 2022/08/13, you are right, na has been set to \"Missing\", and this is updated in above to remove missing region\n  \n  ## redefine age group\n  input <- input %>% mutate(cov_cat_age_group = ifelse(input$cov_num_age>=18 & input$cov_num_age<=39, \"18_39\",\n                                                       ifelse(input$cov_num_age>=40 & input$cov_num_age<=59,\"40_59\",\n                                                              ifelse(input$cov_num_age>=60 & input$cov_num_age<=79, \"60_79\",\n                                                                     \"80_105\"))))\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: cumulative probability of long COVID by age and  sex\n# Output:  Two KM plots by age and sex\nlibrary(readr); library(dplyr);library(survival);library(survminer)\nfs::dir_create(here::here(\"output\", \"review\", \"descriptives\"))\ncohort = \"all\"\ninput <- read_rds(paste0(\"output/input_stage1_\", cohort,\".rds\"))\ninput <- input %>% select(lcovid_surv, lcovid_cens, contains(\"sex\"), contains(\"cov_cat_age\"))\n\nlevels(input$cov_cat_age_group) <- c(\"18-39\", \"40-59\", \"60-79\", \"80-105\")\ntable(input$cov_cat_age_group)\ntable(input$cov_cat_sex)\nlevels(input$cov_cat_sex) <- c(\"Female\", \"Male\")\nKM <- survfit(Surv(lcovid_surv, lcovid_cens) ~ cov_cat_age_group, type=\"kaplan-meier\", \n              data = input,\n              conf.type=\"log\")\nN <- length(unique(input$cov_cat_age_group))\n\nsvg(file=\"output/not_for_review/descriptives/figure_cum_prob_age_sex.svg\", width = 9, height = 5,)\n\nggsurvplot_facet(KM, fun=function(x) { 1- x }, data=input, facet.by = \"cov_cat_sex\",\n                palette = \"jco\", \n                legend.labs = NULL,\n                pval = FALSE, short.panel.labs = TRUE, \n                panel.labs.background = list(color = \"gray\", fill = \"gray\"), \n                panel.labs.font = list(size=13),\n                legend.title=\"Age Group\") +\n  labs(title=\"\",x=\"\\nDays since 1 December 2020\", y = \"\\nCumulative probability of long COVID\\n\") +\n  theme(legend.position=\"bottom\", legend.title=element_text(size=13), \n        legend.text=element_text(size=13),\n        axis.text = element_text(size = 13))\ndev.off()\n\n# figure_km <- function(time, cens, covariate, fig.title){\n#   KM <- survfit(Surv(time, cens) ~ covariate, type=\"kaplan-meier\", \n#                 conf.type=\"log\")\n#   N <- length(unique(covariate))\n#   plot(KM, fun=function(x) { 1- x }, xlab=\"Days since 1 December 2020\", main = fig.title,\n#        ylab=\"\\nCumulative probability of long COVID\", \n#        cex.axis = 1.5, cex.lab = 1.5, cex.main=1.5,\n#        lty=1:N,lwd=2, col=1:N)\n#   legend(\n#     \"topleft\",\n#     legend=unique(covariate),\n#     lty=1:N, col=1:N,lwd=2,cex = 1.5, \n#     horiz=FALSE,\n#     bty='n')\n# }\n\n# input_male <- input %>% filter(cov_cat_sex == \"M\")\n# input_female <- input %>% filter(cov_cat_sex == \"F\")\n# svg(\"output/review/descriptives/km_age_sex.svg\", width = 15, height = 9,)\n# par(mfrow=c(1,2))\n# # Cumulative probability for long COVID - KM plot by age and sex\n# figure_km(input_female$lcovid_surv, input_female$lcovid_cens, input_female$cov_cat_age_group, fig.title=\"Female\")\n# figure_km(input_male$lcovid_surv, input_male$lcovid_cens, input_male$cov_cat_age_group, fig.title=\"Male\")\n# dev.off()\n\n# Purpose: Long COVID risk factors and prediction models\n# Author:  Yinghui Wei\n# Content: Cox model: survival data set up\n# Output:  survival data for long COVID code\n\nlibrary(readr); library(dplyr); library(rms); library(MASS)\n# library(survcomp) ## not yet available\nfs::dir_create(here::here(\"output\", \"not_for_review\", \"model\"))\nfs::dir_create(here::here(\"output\", \"review\", \"model\"))\noutcome = \"long_covid\"\n################################################################################\n#  Specify analysis cohort                                                     #\n################################################################################\nargs <- commandArgs(trailingOnly=TRUE)\nif(length(args)==0){\n  #analysis <- \"all\"          # all eligible population\n  #analysis <- \"all_vax_c\"        # all eligible population but censored them by the 1st vaccination\n  #analysis <- \"vaccinated\"   # vaccinated population\n  #analysis <- \"all_vax_td\"    # vaccination status is included as a time-dependent covariate\n  analysis <- \"infected\"\n}else{\n  analysis <- args[[1]]\n}\n\nratio_non_cases_to_cases = 10 # this is used in sampling non-cases to increase efficiency without loss of information\nset.seed(123456) # to ensure reproducibility in the sampling\n\n################################################################################\n#  load data, define inverse probability weighting                             #\n################################################################################\n## Cohort 1: all eligible patients, without censoring individuals by vaccination\nif(analysis == \"all\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\nif(analysis == \"all_vax_td\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n}\n\n## Cohort 2: all eligible patients, but censoring individuals by vaccination\nif(analysis == \"all_vax_c\"){\n  input <- read_rds(\"output/input_stage1_all.rds\")\n  input <- input %>% dplyr::select(-lcovid_surv, -lcovid_cens) %>%\n    dplyr::rename(lcovid_surv = lcovid_surv_vax_c, lcovid_cens = lcovid_cens_vax_c)\n}\n## Cohort 3: time origin is the first vaccination \nif(analysis == \"vaccinated\"){\n  input <- read_rds(\"output/input_stage1_vaccinated.rds\")\n}\n\n## Cohort 4: time origin is the first covid infection\nif(analysis == \"infected\"){\n  input <- read_rds(\"output/input_stage1_infected.rds\")\n}\n\n##--specify inverse probability weighting\ncases <- input %>% filter(lcovid_cens==1)\nnon_cases <- input %>% filter(!patient_id %in% cases$patient_id)\n\nif(nrow(cases)*ratio_non_cases_to_cases < nrow(non_cases)){\n  non_cases <- non_cases[sample(1:nrow(non_cases), nrow(cases)*ratio_non_cases_to_cases,replace=FALSE), ]\n}else if (nrow(cases)*ratio_non_cases_to_cases >= nrow(non_cases)){\n  non_cases=non_cases\n}\nprint(paste0(\"Number of cases: \", nrow(cases)))\nprint(paste0(\"Number of controls: \", nrow(non_cases)))\nnon_case_inverse_weight=(nrow(input)-nrow(cases))/nrow(non_cases)\n## recreate input after sampling\ninput <- bind_rows(cases,non_cases)\n## Add inverse probability weights for non-cases\nnoncase_ids <- unique(non_cases$patient_id)\ninput$weight <-1\ninput$weight <- ifelse(input$patient_id %in% noncase_ids,\n                       non_case_inverse_weight, 1)\n\n# create the time dependent variable for vaccination\nif(analysis == \"all_vax_td\"){\n  z <- ie.setup(input$lcovid_surv, input$lcovid_cens, input$vax2_surv)\n  S <- z$S\n  ie.status <- z$ie.status",
    "source": "code",
    "evidence": {
      "code_path": "analysis/dated code/stage3_long_covid_code_cox_model.R\nanalysis/figure_kaplan_meier_cohorts.R\nanalysis/stage1_define_eligible_population.R\nanalysis/dated code/figure_cum_prob_km.R\nanalysis/dated code/stage2_long_covid_code_data_setup.R",
      "code_lines": "1-80\n1-80\n61-140\n1-60\n1-80"
    },
    "llm_raw": "{\"keep_indices\":[0,4,5,7,9],\"note\":\"Selected snippets that define or operationalize variable codings used in the survival/Cox analyses: (0) specifies model contrasts and age treatment (spline/linear), (5) constructs categorical age groups (18–39, 40–59, 60–79, 80–105), (4) applies labeled age-group factor levels and uses them in survival/KM data, (7) similarly sets age-group and sex factor levels for KM, and (9) sets up survival outcome/censoring variables and sampling/weights for the Cox models, including event indicator coding.\"}"
  }
]