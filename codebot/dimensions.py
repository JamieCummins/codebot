"""Default comparison dimensions for CodeBot."""

from .config import DEFAULT_COMPARISON_DIMENSIONS

# Keep old medical-domain dimensions as canonical defaults.
DEFAULT_DIMENSIONS = dict(DEFAULT_COMPARISON_DIMENSIONS)
